[pytest]
# Collect all async test functions automatically — no @pytest.mark.asyncio needed.
asyncio_mode = auto
# Default loop scope for async fixtures that do not declare loop_scope explicitly.
# Set to "session" so fixture teardown warnings are suppressed and every async
# fixture shares the same session-level event loop unless overridden.
asyncio_default_fixture_loop_scope = session
