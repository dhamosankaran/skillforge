[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = session
testpaths = tests
markers =
    integration: integration tests requiring a seeded dev database (skipped in CI by default)
    integration_llm: integration tests requiring a live LLM API key (skipped in CI by default; run locally with `pytest -m integration_llm`)
