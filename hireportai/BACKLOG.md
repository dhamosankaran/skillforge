# SkillForge Backlog

> Single source of truth for all work items: bugs, enhancements, and tech debt.
> Spec files in `docs/specs/` describe the *what* and *how*. This file tracks the *what's-next* and *what's-done*.

## Rules
- **IDs are immutable.** Once assigned, a B-### or E-### never changes, never gets reused.
- **One row per item.** Don't split, don't merge. Spawn a new ID if scope splinters.
- **Status is the only field Claude Code may flip autonomously** (🔴 → 🟡 → ✅). All other fields require human edit.
- **Closing an item:** status → ✅ + append a one-liner to Notes: `closed by <commit-sha> on <YYYY-MM-DD>`.
- **Priority is a product decision.** Set by Dhamo only.
- **Spec column is the on-disk path** (`docs/specs/phase-N/NN-name.md`). Empty if no spec exists yet — create one before implementation.

## Status legend
- 🔴 Pending (not started)
- 🟡 In progress / partial / blocked
- ✅ Done
- ⚪ Won't do / deferred indefinitely
- 🟦 Back-burner — scoped out, awaiting activation gate (e.g., telemetry threshold). Not worked on until the gate trips.

## Priority legend
- **P0** — broken in production, blocking users, blocking next milestone
- **P1** — committed for this phase
- **P2** — wanted, not committed
- **P3** — someday / nice-to-have

---

## Active backlog

### Bugs (B-)

| ID | Area | Title | Priority | Status | Spec | Source slice | Notes |
|----|------|-------|----------|--------|------|--------------|-------|
| B-006 | settings | Existing registered users may lose settings when new fields are added | P1 | ✅ | | P5-S27 | Closed `c4132fb` 2026-05-03 by audit verdict — zero gaps. Mode-1 audit covered all 9 User columns + 4 per-user settings tables. NOT NULL columns ship with `server_default` (role / onboarding_completed / created_at) or signup-payload values (google_id / email / name). Nullable columns (avatar_url / persona / interview_target_* / downgraded_at / home_first_visit_seen_at) all have explicit None-handling at every reader (`(user.role or "user")` at study_service:188,291; `user.persona is None` gate at curriculum_visibility:51; `user.persona or "null"` at onboarding_checklist:127; isoformat-or-None at auth:47-48). EmailPreference + GamificationStats expose `_get_or_create` helpers at their read paths; Subscription auto-creates on first checkout; B-116's `cancel_at_period_end` migration `c2ba25f1d9a7` uses `server_default=text("false")` for safe backfill. **No fixes applied; no impl slice surfaced.** Cascade-closes E-015 same commit (audit was the harden surface; verdict = nothing to harden). R14 exception (b); R15(c): single-slice close; R17: no new IDs claimed. |
| B-008 | nav | `deprecated_route_hit` not wired in 10 Navigate redirect nodes | P2 | ✅ | docs/specs/phase-5/12-navigation-restructure.md §Analytics | (post-P5-S13) | Closed `49aee35` 2026-05-03. New `src/components/DeprecatedRedirect.tsx` wraps `<Navigate replace />` and fires PostHog `deprecated_route_hit` once on mount with `{from_path: useLocation().pathname, to_path: resolvedTo}`. Supports static `to` prop or `build(params)` for the 2 dynamic redirects (`/study/category/:id`, `/study/card/:id`). All 10 transitional redirects in `App.tsx` migrated; the local `RedirectWithParam` helper deleted (its only callers were the 2 dynamic deprecated routes). Analytics catalog gains a `deprecated_route_hit` row in the events table; the "P5-S14 note" gap-flag paragraph removed (now wired). **JC #1 (info-only):** spec #12 §Analytics specified payload `{old_path, new_path}`; shipped `{from_path, to_path}` to mirror the existing `nav_clicked` event's already-shipped property naming for catalog consistency. **Tests +6 (FE):** new `tests/DeprecatedRedirect.test.tsx` (3 — static `to`, dynamic `build`, mount-only fire); `tests/App.redirects.test.tsx` +3 (legacy `/analyze` fires event, dynamic `/study/card/:id` reports resolved id, non-deprecated `/home` does NOT fire). FE 480 → 486 (top of +3..+6 envelope); BE 858 carry-forward (zero BE surface). tsc clean modulo pre-existing `ScoreDeltaWidget.tsx` unused-import warning. R14 exception (b) — telemetry backfill, no spec authored (spec #12 §Analytics IS the design surface). R15(c): B-008 🔴 → ✅. R17: no new IDs. Two-commit pattern. |
| B-009 | docs | AGENTS.md:95 contradicts CLAUDE.md Rule 13 | P2 | ✅ | | | Closed `0cb10ad` 2026-05-03. The contradicting line had drifted from L95 to L120 ("Coverage target: 80%+") since B-009 was filed; CLAUDE.md R13 explicitly says coverage tooling is not installed. Replaced AGENTS.md L120 with a pointer to R13 (`Coverage tooling (pytest-cov) is deliberately NOT installed — see CLAUDE.md R13`). CLAUDE.md is authoritative per AGENTS.md source-of-truth hierarchy #3. R14 exception (b) — doc-only fix, no design surface, no tests. |
| B-010 | frontend | Orphan `Navbar.tsx` still in repo after TopNav migration | P3 | 🔴 | | | Dead code. Delete after confirming zero imports. |
| B-011 | docs | Spec numbering on disk diverges from v2.1 doc | P2 | ✅ | | | Closed `9db9b9f` 2026-05-03 by Mode-3 scout — divergences are by design, not bugs. v2.1 P5-S<N> session numbers were planning labels; spec authors picked the next-available numeric prefix at authoring time (P5-S12→`12-navigation-restructure.md` matches; P5-S15→`34-persona-picker-and-home.md`, P5-S23→`49-interview-question-storage.md`, P5-S26→`36-subscription-cancellation.md`, P5-S31→`38-admin-analytics.md` diverge — all consistent with the "next-free-prefix" pattern). Per AGENTS.md source-of-truth hierarchy #2 the on-disk specs win; per SESSION-STATE.md:320 v2.1 is a frozen snapshot. No code or other specs reference v2.1 session numbers as authoritative. R14 exception (b) — doc-only audit, no design surface. |
| B-012 | playbook | v2.1 doc-bug audit pending | P2 | ✅ | | | Closed `9db9b9f` 2026-05-03 per verdict (b). v2.1 (`claude-code-prompts-all-phases-v2.md`) is a historical planning artifact; all shipped Phase-0..5 work used amended prompts at execution time per R1 (amend, don't rewrite). Known doc bugs are moot — no retroactive patch needed. Live process docs (CLAUDE.md, AGENTS.md, on-disk specs, `docs/PHASE-5-STATUS.md`) are the source of truth. CLAUDE.md + AGENTS.md contain ZERO references to v2.1 (verified by grep); SESSION-STATE.md references are historical close-line context only. The 2026-04-19 reconciliation (SESSION-STATE.md:324) already closed v2.1 item 5.9 (doc audit + sync) and produced `docs/specs/phase-5/48-doc-audit-pattern.md` as the periodic-reconciliation pattern — that pattern, not a one-off v2.3 patch, is the answer. R14 exception (b). |
| B-013 | docs | Repo-root ambiguity in AGENTS.md + CLAUDE.md + prompt templates | P2 | ✅ | | (from D-004) | Closed `0cb10ad` 2026-05-03. Took option (a) per the row's own wording: added a new "## Repository layout" section to AGENTS.md (above the existing directory diagram) clarifying that git root = `SkillForge/` (parent), working dir = `SkillForge/hireportai/`, and that the diagram below intentionally elides the parent. Cross-refs CLAUDE.md C1 for the staging footgun. CLAUDE.md C1 already covered the staging implication; the gap was orientation in AGENTS.md, now closed. R14 exception (b) — doc-only fix, no design surface, no tests. |
| B-020 | home | TodaysReviewWidget should surface free-tier daily-wall-hit state | P3 | 🔴 | docs/specs/phase-5/50-free-tier-daily-card-wall.md | (follow-up to B-019 2026-04-22) | Free-tier users are capped at 10 card reviews per user-local-midnight day (spec #50 / E-030; LD-001 amendment 2026-04-26 lowered the cap from 15 → 10). A free user who completes 5 Today's Review cards + 5 cards via mission/category paths hits the wall; the widget should reflect that state distinctly from "done for today" (user-local-tz, different semantics than B-019's UTC-window completion). **Scope when picked up:** (a) decide whether widget fetches the 10-card counter itself (Redis round-trip for the widget — adds a new data dependency) or the DailyQueue endpoint surfaces a wall-aware flag (e.g., `wall_hit: bool` alongside `completed_today`); (b) decide distinct empty-state copy ("Daily limit reached — upgrade to continue" vs "Done for today"); (c) decide CTA — link to Pricing vs no CTA; (d) decide timezone reconciliation with B-019's UTC completion window — two flags with two tz semantics in the same response is confusing. **Dependency:** B-019 lands first so the empty-state surface has a home. **Cross-ref:** spec #50 §Counter Scope, B-015 (wall 402 payload + QuizPanel handling — already shipped). |
| B-025 | interview | Manual company-name entry UI when extraction returns null (three-layer design layer 2) | P2 | 🔴 | | (follow-up to B-024 2026-04-22) | Needs its own spec. Layer 2 of the three-layer design confirmed in B-024 review (LLM primary → regex fallback → user-entered). Scope when picked up: (a) decide surface — cover-letter composer modal vs Profile scan-edit vs inline field on /prep/results; (b) decide persistence — per-scan ephemeral, per-tracker-application, or per-user default override; (c) decide UI trigger — always-editable input, "Edit" affordance after generation, or prompt-on-null only; (d) decide the company_name contract on `extract_job_requirements` — add a `company_name_confidence: 'llm' | 'regex' | 'user' | None` discriminator so downstream consumers know which layer hit; (e) decide tracker autopopulate interaction — does a user-entered company_name during cover-letter generation propagate back to the tracker row? Out of scope here: LLM prompt tuning for better layer-1 recall (covered by B-024 closure). **Cross-ref:** B-024 (`b1b9ebe`) shipped layers 1 + 3; this row adds the layer 2 surface. Precision-first preserved: user sees "your company" in worst case today; that continues until this row's spec lands. |
| B-036 | telemetry | `scan_rehydrated` PostHog event missing `scan_age_days` property — deferred from P5-S59-impl judgment call #2 | P3 | 🟦 | docs/specs/phase-5/59-scan-persistence.md (§12 / §17) | (deferred from spec #59 / `0b35440`) | Spec §12 specified `scan_age_days: integer \| null` on the `scan_rehydrated` event; impl deferred per the §17 open question — `AnalysisResponse` does not carry `scan_created_at` and the FE has no other source for the tracker row creation timestamp. Without this property we cannot answer the only retention question the event exists to answer ("are users re-viewing day-old or month-old scans"). Today the event fires with just `{scan_id}`. **Fix shape:** (a) add `scan_created_at: datetime \| None` (additive, default-None) to `AnalysisResponse` in `app/schemas/responses.py`; (b) populate it in both write paths — `app/api/routes/analyze.py` constructs at scan time + `GET /analyze/{scan_id}` re-projects from the tracker row's `created_at`; (c) FE derives `Math.floor((Date.now() - new Date(scan_created_at).getTime()) / 86_400_000)` and passes as `scan_age_days` to `capture('scan_rehydrated', …)`; (d) update `.agent/skills/analytics.md` row. **Estimated diff:** ~10 LoC + 2-3 tests. **Out of scope here:** any FE display of scan age (this is telemetry-only); migration backfill of tracker `created_at` (column already populated). **Why P3 / 🟦 back-burner:** not user-facing, no activation gate; rehydration itself works correctly. Pick up when retention analytics demand age dimensioning. |
| B-038 | backend/users | Make `PATCH /api/v1/users/me/persona` partial-update OR add a dedicated `/users/me/interview-target-date` endpoint | P2 | 🔴 | | (filed alongside B-037, 2026-04-25) | **Gated on B-037 landing first.** Today `PersonaUpdateRequest` requires `persona` and unconditionally overwrites `interview_target_date` + `interview_target_company` (`hirelens-backend/app/api/v1/routes/users.py:90-92`). Any client that wants to update one field must read-modify-write all three or risk silently nulling the others. B-037's `InterviewDateModal` works around this by reading `persona` + `interview_target_company` from AuthContext and re-sending them with the new date — see the inline comment in `src/components/home/InterviewDateModal.tsx`. **Scope when picked up:** (a) decide between making `PATCH /persona` truly partial (Pydantic `Optional` everywhere + only-write-set-fields in the route) vs. minting `PATCH /users/me/interview-target-date` (single-purpose, simpler contract); (b) update FE clients (`InterviewDateModal`, future Profile editor) to drop the read-and-preserve dance; (c) regression test that omitted fields are not nullified; (d) keep dual-write to tracker rows intact (spec #57 §2.3 / AC-8). **Cross-ref:** B-037 closed `961ee3f`; spec #34 §API Contract; spec #57 §2.3 dual-write rule; E-017 Profile-side editor (would benefit from this fix). **Out of scope here:** any UX changes — pure BE refactor + minimal FE call-site cleanup. R14 default if going to refactor route, R14 exception (b) if minting the new endpoint as a focused fix. |
| B-040 | interview | Interview question cache key is `(user_id, jd_hash)` only — same JD with a different resume returns stale questions | P3 | 🟦 | docs/specs/phase-5/49-interview-question-storage.md (§6 deferral) | (filed by 2026-04-25 P5-S60 wiring slice) | Spec #49 §6 explicitly defers `resume_hash` from the cache key; "5.17b product call." Today the manual "Regenerate Questions" button (`Interview.tsx`, `forceRegenerate: true`) is the only escape hatch — there is no UX cue that the user's resume changed since the cached set was generated. **Activation gate:** 3+ user complaints about stale questions, OR when the subscription-cancellation flow ships (similar UX surface where resume edits are common). **Scope when picked up:** (a) decide whether to extend the cache key to `(user_id, jd_hash, resume_hash)` (any whitespace edit invalidates — possibly too aggressive) or to detect resume-change in the FE and surface a "Your resume changed since this set was generated — regenerate?" inline cue; (b) `interview_question_sets` does not currently store `resume_text` (only `jd_text`), so option (b) needs a new column or a separate hash field; (c) reconcile with spec #49's existing `force_regenerate` wiring — both today's manual button and any new auto-detect should funnel through the same code path. **Out of scope here:** any change to the prompt budget (already tuned to `[:3000]` in P5-S60). Closes-line will reference impl SHA when picked up. |
| B-041 | interview | `Interview.tsx` cold-load does not hydrate from `?scan_id` — banner copy "Using your latest resume…" never shows after a refresh | P3 | 🟦 | docs/specs/phase-5/59-scan-persistence.md (Results pattern reference) | (filed by 2026-04-25 P5-S60 wiring slice) | `AnalysisContext.tsx` is in-memory only (`useReducer` with empty initial state, no localStorage, no URL hydration). A user who lands cold on `/prep/interview` (refresh, deep link, email CTA) sees `hasContext=false` → manual textarea form, never the banner. Spec #59 just shipped Results-side hydration from `?scan_id` (commit `0b35440`); the same pattern would apply to `/prep/interview`. **Activation gate:** email campaigns that link directly to `/prep/interview`, OR a broad spec #59-style hydration rollout across `/prep/*` routes. **Scope when picked up:** (a) thread `scan_id` from `Results.tsx`'s "Interview Prep" CTA navigation; (b) on `/prep/interview` mount, if `?scan_id` is present and `AnalysisContext` is empty, fetch the scan via the existing `GET /api/v1/analyze/{scan_id}` endpoint (same hydration helper Results uses) and seed `AnalysisContext`; (c) test cold-load + warm-load + scan-not-found error paths. **Out of scope here:** any banner copy change beyond the existing softening (already shipped in P5-S60). Reference impl SHA when picked up. |
| B-042 | payments | Paywall grace parity for `interview_limit` — `should-show-paywall` + `WallInlineNudge` integration | P3 | 🟦 | docs/specs/phase-5/42-paywall-dismissal.md (LD-1 expansion) | (filed by 2026-04-25 P5-S60 wiring slice) | P5-S60 locked LD-1 hard-wall (modal-only, no `should-show-paywall` round-trip, no `WallInlineNudge`) for the interview-prep paywall to match the `scan_limit` / `rewrite_limit` / `cover_letter_limit` carve-outs in `paywall_service.py:180`. Spec #42 anticipated future triggers extending the dismissal-grace surface (LD-1: "Other PaywallTrigger values reuse the same endpoints when they later wall — zero backend change needed"). **Activation gate:** a single batched slice that flips ALL FOUR hard-wall triggers (`scan_limit`, `rewrite_limit`, `cover_letter_limit`, `interview_limit`) to spec #42 grace-aware UX simultaneously. NOT a standalone slice — the value is in coherent product behavior across all paywalls, not piecemeal. **Scope when picked up:** (a) remove the hard-wall set in `paywall_service.py:180` (or carve out the four triggers from it as needed); (b) flip each chokepoint to query `should-show-paywall` and dispatch to modal-or-nudge (today only `QuizPanel.tsx` does this for `daily_review`); (c) generalize `WallInlineNudge.tsx` copy to be trigger-aware (today hardcoded for daily-review); (d) add four new dismissal-and-grace test suites; (e) decide whether `interview_limit` `action_count_at_dismissal` semantics are "generations attempted" or "page visits while at cap." **Out of scope here:** unwinding the modal-only mount in `Interview.tsx`. Reference impl SHA when picked up. |
| B-043 | interview | Resume budget tuning beyond 3000 chars in `generate_interview_questions` prompt | P3 | 🟦 | | (filed by 2026-04-25 P5-S60 wiring slice) | P5-S60 bumped the active resume slice from `[:800]` to `[:3000]` (`gpt_service.py` — `resume_text = resume_data.get("full_text", "")[:3000]`). Tuning rationale: 3000 chars fits a typical experience section in the prompt envelope; deleted the dead `[:1500]` re-slice in the same commit. **Activation gate:** token-cost telemetry shows headroom in the reasoning-tier prompt envelope (Gemini 2.5 Pro currently — check token counts on `interview_questions` task in PostHog/Sentry), OR a Pro user complains that questions miss late-career experience visible only past the 3000-char mark in their resume. **Scope when picked up:** (a) measure prompt-token budget headroom; (b) decide between bumping the constant (3000 → 5000?), switching to a "summarize then prompt" two-call pattern (LLM summary of full resume → fed into question generator), or adding a structured-extraction step that picks the most JD-relevant resume sections; (c) tune the inline comment "do not change without re-tuning prompt" — the comment is the canary, the constant is the lever. **Out of scope here:** any prompt-shape change beyond a pure `[:N]` slice tweak. Reference impl SHA when picked up. |
| B-044 | docs | Backfill spec for interview_prep free-tier cap (3/month, monthly window, hard-wall via `interview_limit` trigger) | P3 | 🟦 | | (filed by 2026-04-25 P5-S60 wiring slice) | The `interview_prep: 3/month` free-tier cap currently lives only in `app/services/usage_service.py:19` (`PLAN_LIMITS["free"]["interview_prep"] = 3`) and `PaywallModal.tsx:59` copy ("3 free interview prep generations this month"). No authoring spec exists; spec #49 §3.4 mentions the existing BE pre-flight gate without specifying the cap value or its window. **Activation gate:** the cap value changes (3 → other), OR a second free-tier feature reuses the same spec template (e.g., during the all-4-triggers paywall-grace pass tracked in B-042). **Scope when picked up:** pure documentation backfill, no code. Document: (a) the cap value and rationale; (b) counter storage (`usage_logs` rows with `feature_used="interview_prep"`); (c) monthly window semantics via `_count_usage(window="monthly")`; (d) paywall trigger classification (`interview_limit`, hard-wall per LD-1 in P5-S60); (e) relationship to spec #49 §3.4 BE pre-flight gate. File at `docs/specs/phase-5/30-free-tier-interview-limits.md` (the slug originally cited in P5-S60's prompt). Reference impl SHA when picked up. |
| B-047 | frontend | Surface `scansRemaining` on `UsageContext` per spec #60 LD-4 — deferred from B-045 due to pre-existing `UsageContext.tsx` working-tree dirt | P3 | 🔴 | docs/specs/phase-5/60-analyze-page-preflight-gate.md (§4.1, LD-4) | (filed by 2026-04-25 P5-S62 impl slice — B-045 deferral) | **Gated on resolution of pre-existing `UsageContext.tsx` working-tree dirt** (auth-guard import + guest-redirect guard, ~5 slices old, source unknown — concurrent-session work that has not been committed by its original author). B-045's impl slice could not safely add `scansRemaining` to `UsageContext.tsx` because (a) editing that file would co-bundle the dirty hunks into an unrelated commit (C3 violation), (b) permission to stash the dirt was denied, (c) spec #60 §4.1 line 147 explicitly authorizes the deferral ("No consumer of this spec required to use `scansRemaining`"). **Scope when picked up:** (a) confirm what's happening with the pre-existing `UsageContext.tsx` dirt — either commit it as its own slice (talk to whoever authored it) or back it out; (b) once the dirt is cleared, add `scansRemaining: number` to `UsageState` mirroring `interviewPrepsRemaining` shape (spec §4.1) — extend `DEFAULT_STATE` (`scansRemaining: 1`), `loadDisplayCache` (`parsed.scansRemaining ?? 1`), `fromResponse` (read `r.scans_remaining`), `upgradePlan` (`isPaid ? -1 : Math.max(0, 1 - prev.scansUsed)`); (c) add the 3 fromResponse pin tests spec §6.1 specifies (free with quota / free at cap / Pro -1 sentinel) — likely in a NEW test file `tests/context/UsageContext.scansRemaining.test.tsx` to avoid the existing `UsageContext.hydration.test.tsx` 5-failure baseline. **Out of scope here:** any consumer change (no consumer in the gate code path needs `scansRemaining` per spec §4.1); fixing the unrelated 5-test `UsageContext.hydration.test.tsx` failures (separate blocker for someone else's slice). **Cross-refs:** spec #60 §4.1 LD-4 + AC-9, B-045 (closed `3c962d8`), `interviewPrepsRemaining` precedent (commit `56afd99` / P5-S60). R14 exception (b) — pure additive cleanup completing an authored spec. |
| B-046 | payments/ux | Apply pre-flight gate pattern (spec #60) to `/prep/results` Re-analyze sibling for symmetry | P3 | 🟦 | | (filed by 2026-04-25 P5-S61 spec slice — spec #60 §7 follow-up) | Today (per spec #55 / B-030, closed `5c20d53`) the Re-analyze button on `/prep/results` is left clickable for free users at scan cap; the gate fires at click-time via `setShowPaywall(true)` (`Results.tsx:74-78`). Spec #60 establishes the pre-flight pattern (button absent / replaced for free-at-cap) on `/prep/analyze`. Whether to retro-fit the same pattern to `/prep/results` is a UX-symmetry call: arguments for (a) consistent gating semantics across the two scan surfaces, (b) Re-analyze on the post-results page is a higher-intent click than the page-load on Analyze so click-time gating may be defensible. **Activation gate:** ALL THREE — (1) spec #60 / B-045 has shipped and is observed working in production, (2) PostHog data shows users clicking Re-analyze when at cap (i.e., the click-time gate is actually being hit), (3) Dhamo product call that the asymmetry is worth a slice. **Scope when picked up:** (a) decide between hide-button / disable-button / replace-with-inline-card; (b) reuse spec #60 §3.1 card pattern if (c); (c) re-fire `paywall_hit` with `surface: 'results_reanalyze_blocked'` (or similar — match the `surface` naming convention spec #60 establishes); (d) regression-pin `tests/pages/Results.reanalyze.test.tsx` so the existing click-time gate behavior is verified to either change or stay per the chosen path. **Out of scope:** any change to `paywall_service.py` or PaywallModal copy. **Cross-ref:** spec #60 §7 explicit out-of-scope; spec #55 (B-030 sibling). |
| B-056 | docs | Spec #22 + spec #56 still cite "15-card wall" / "15-card budget" — flip to 10-card or generalize | P3 | 🔴 | docs/specs/phase-5/22-plan-aware-missing-skills-cta.md, docs/specs/phase-5/56-free-tier-scan-lifetime-cap.md | (filed by 2026-04-26 Slice B LD-001 amendment — D-023) | Stale literal-15 references in two specs that were authored before LD-001 was tightened from 15 → 10 (Slice B, commit `b8d0c8c`, 2026-04-26). Drift-flag anchor: D-023 in SESSION-STATE Drift Flags table. **Affected lines (verified against disk at filing):** spec #22 — 7 hits at lines 20, 27, 40, 111, 117, 118, 157 (§Problem, free-user CTA copy, AC-5, §Edge Cases, §Dependencies, §Dependencies, §Out-of-Scope — frames the wall as "15-card-wall" / "15-card budget"); spec #56 — 1 hit at line 20 in §Problem ("the paywall downstream at the 15-card wall, not on scans"). **Scope when picked up:** (a) audit each hit and decide per-line — flip "15" → "10" verbatim, OR generalize the wording to "the daily-card wall" / "the free-tier daily review cap" to avoid future drift on the next LD-001 amendment; (b) preserve any historical-context / drift-narrative mentions of 15 (mirror the pattern Slice B used in spec #50 §Interpretation note — those describe prior state and stay verbatim); (c) no AC shape changes, no payload changes, no test changes — both specs describe an existing wall mechanism whose behavior is unchanged by the cap value. Effort: small (~15 min). **Out of scope:** any fresh LD-001 amendment, payment-service code edits, FE changes, BACKLOG/SESSION-STATE edits beyond closing this row. **Cross-refs:** spec #50 (canonical for the wall mechanic, already amended in Slice B `b8d0c8c`); SESSION-STATE LD-001 amendment 2026-04-26; D-023 drift flag. **Activation:** any time, no gate. R14 exception (a) docs-sync. R17 watermark verified at slice start (B-055 highest, B-056 free). |
| B-058 | nav | Duplicated chromeless-paths sets across AppShell + MobileNav — lift to shared constant | P3 | 🔴 | | (from 2026-04-26 B-057 R19 pushback — CC noted MobileNav.tsx duplicates AppShell's CHROMELESS_PATHS) | B-057 (`19326de`) fixed `/pricing` chrome carve-out in two places — `AppShell.tsx` (`CHROMELESS_PATHS`) and `MobileNav.tsx` (`HIDDEN_PATHS`). Both sets contain `['/', '/login', '/pricing', '/onboarding/persona', '/first-action']` (AppShell) / `['/', '/login', '/pricing']` (MobileNav — narrower since the bottom-bar isn't mounted on onboarding/first-action anyway). Drift risk: any future change to chromeless-paths policy must be made in two places. **Suggested cleanup:** lift to `src/components/layout/chromeless.ts` exporting `CHROMELESS_PATHS` (full list) + `MOBILE_HIDDEN_PATHS` (narrower subset) with shared `isChromeless(pathname, user)` helper that returns the same `pathname === '/pricing' && user === null` carve-out. Both AppShell + MobileNav consume from the helper. **Effort:** small (~20 min). **Out of scope:** any chromeless-policy change beyond consolidation. **Activation:** any time, no gate. R14 exception (a) docs/hygiene file. R17 watermark verified at slice start (B-057 highest, B-058 free). |
| B-125a | persona/email | Phase 5 spec #67 BE half — `user_career_intents` table + service + 3 routes + `pro_digest` composer/template extensions + 3 analytics events | P1 | ✅ | docs/specs/phase-5/67-career-climber-role-intent.md (§5–§7 + §9 + §11 BE-side ACs) | filed + closed `9082a7b` 2026-05-04 (Mode-2 impl-to-spec; R14 default) | Closed `9082a7b` 2026-05-04. BE half of the 2-slice E-052 split (B-125a BE / B-125b FE). Sub-ID per 6.4b / 6.10 precedent — does NOT consume numeric watermark. **What shipped:** new `app/models/user_career_intent.py` (UUIDPrimaryKeyMixin + user_id FK CASCADE + 2 composite indexes per spec §5.1/5.2), `app/schemas/career_intent.py` (`CareerIntentCreate` with `ALLOWED_ROLES` frozenset validator + current-or-future quarter validator + `AggregateStats` + `CategoryShare` per §5.3/5.4 + D-11), alembic `e052b125a4f1_phase5_e052_user_career_intents.py` with `down_revision=c2ba25f1d9a7` (re-confirmed at Step 0; spec §7's `f1a2b3c4d5e6` placeholder was stale by 1 head — current head was the B-116 cancel_at_period_end migration), `app/services/career_intent_service.py` (`set_intent` append-only supersede + `get_current_intent` + `clear_intent` + `get_aggregate_stats` with privacy-contract enforcement at `MIN_COHORT_SIZE=10` per §4.4), 3 routes at `app/api/v1/routes/career_intent.py` (POST 201 `career_intent_captured` first / `career_intent_updated` subsequent — keyed on prior `get_current_intent is None`; GET 200/404; DELETE 204; persona guard 422 with `X-Capture-Source` header support per D-13). **Composer extension:** `pro_digest_service._aggregate_intent_block` reads intent + aggregate (try/except → fires `pro_digest_intent_aggregate_failed` per §6.5 with graceful None fallback); strict-empty-rule extended additively per §6.3 (CC user with intent + cohort still gets digest); `_build_html` adds `intent_section_style` + `intent_role_label` + `intent_copy` substitutions; `_intent_block_copy` formats aggregate copy with the §8.5 ban-list-safe phrasing ("Engineers targeting {role} this quarter spend X% on {cat}…"); send-success path fires `career_intent_email_block_rendered` per D-13 (gated on `payload.aggregate_intent_block is not None`); `pro_digest_sent` gains `has_aggregate_block: bool` per §9.2. **Template extension:** `app/templates/pro_digest.html` adds 4th `<div data-section="intent">` block per D-7 (extends, NOT forks). **Analytics catalog:** 3 new event rows + `pro_digest_sent.has_aggregate_block` property + `pro_digest_intent_aggregate_failed` failure event in `.agent/skills/analytics.md`. **Tests +30 BE:** `test_career_intent_service.py` (14: model + set/supersede + get/none + clear + aggregate threshold/isolation/zero-reps + 3 schema validators); `test_career_intent_route.py` (10: auth + persona-guard + role-validation + past-quarter + captured-vs-updated + GET 200/404 + DELETE 204/no-op); `test_pro_digest_intent_template.py` (3: rendered/omitted + ban-list AC-X); `test_pro_digest_service.py` extended (3: no-intent unchanged / below-threshold omitted / at-threshold populated); `test_e052_user_career_intents_migration.py` (1 integration). BE 847 → 877 passing; 10 pre-existing failures unchanged (D-027/D-028 lineage, orthogonal). **JC #1:** spec §6.1 sketch said `quiz_review_events JOIN cards` but `QuizReviewEvent` is keyed on `quiz_items` (Phase-6 lessons surface, not legacy cards). Used `CardProgress.reps` per `Category` for the aggregate query — drift logged as D-033. **JC #2:** prompt named schema class `AggregateIntentStats`; spec §5.3 said `AggregateStats` — followed spec per AGENTS.md hierarchy. **JC #3:** prompt named model file `app/models/career_intent.py`; spec §5.2 said `app/models/user_career_intent.py` — followed spec. **JC #4 (test isolation):** route tests commit (route handlers call `db.commit()`), leaking `(staff/principal/em/director, 2026-Q3)` rows into the session-scoped engine. Service-level aggregate tests (3 of them) hit threshold-mismatch failures in full-suite runs. Switched the service tests' aggregate cohorts to `senior_staff/2099-Q1`, `sr_em/2099-Q2`, `distinguished+em/2099-Q3`, `director/2099-Q4` for full isolation; service tests pass cleanly in both isolated and full-suite runs. R14 default — implements authored spec #67. **R15(c):** B-125a filed + closed same slice; B-125 stays 🔴 (closes when B-125b FE half ships). **R17 watermark:** unchanged at B-126; B-127 next-free numeric ID (sub-IDs don't consume the watermark per 6.4b / 6.10 precedent). CR ❌ stale per Step 5 (new model + service + route + schema + migration). Two-commit pattern (impl + tests + BACKLOG + SS in commit 1; SHA backfill in commit 2 replacing `9082a7b` placeholders). |
| B-127 | process/hygiene | SESSION-STATE + BACKLOG compaction — RC archive cut (entries 4+) + Drift archive cut (5 ✅ RESOLVED) + BACKLOG closed-row archive cut (136 rows ≤2026-05-02) + Test Suite Status refresh + worst-RC trim | P3 | ✅ | | filed + closed `<this-slice>` 2026-05-04 (Mode-1 light hygiene; R14 exception (b)) | Closed `<this-slice>` 2026-05-04. Single-slice file+close per B-121 / B-109 precedent. Pre-flight: SS 444 lines / worst RC 298 words / BACKLOG 279 lines (2 warns + 2 fails: worst-RC + process-tax). Tasks: **(1)** SS RC archive cut — 9 entries 4+ cut to `docs/archive/session-state-history.md` §10 (B-119..B-124 + spec #66 author + §12 amendment + spec #67 author); active SS keeps 3 most recent (B-125+E-052, B-125a, spec #67 §12 amendment). **(2)** SS Drift archive cut — 5 ✅ RESOLVED rows (D-016, D-019, D-020, D-022, D-029) cut to archive §10. **(3)** Footer pointer line refreshed to mention §10 cut. **(4)** Test Suite Status refreshed: BE 823 passing / 64 failed (D-027/D-028/D-032 cascade carry-forward — B-125a's clean-env 877 measurement doesn't reproduce in full-suite local runs); FE 543 passing. **(5)** BACKLOG cut — 136 closed rows ≤2026-05-02 (full B-001..B-115 + E-002..E-048 cluster + secondary-table short-forms) cut to `docs/archive/backlog-closed.md` under new "Cut 2026-05-04 from `<this-slice>` — B-127 light-hygiene compaction" heading; new ↳ pointer row added. Prior B-121 cut had left active-table duplicates of already-archived rows — this slice clears them. **(6)** Worst-RC trim — B-125a entry compacted 298 → 245 words (under 250 hard ceiling) per R15(d) without removing JCs / R-rule fires. Drift-flags table footer note updated. **JC #1 (info-only):** process-tax fail (8/15 = 53% post-slice; was 7/15 = 46% pre-slice) carry-forward — structurally a feature-mix metric, not a doc-bloat metric, and adding this hygiene row to the active-table tail makes it WORSE. Same disposition as B-121 close-line "process-tax fail unchanged (orthogonal — out of scope)". Will clear naturally when feature work resumes. **JC #2 (info-only):** BE test count drift — B-125a documented 877 passing under prod-default env vars; my measurement at HEAD `de226b9` shows 823 passing / 64 failed. Distinct from D-027/D-028 documented baseline; matches the alembic-roundtrip cascade pattern surfaced under full-suite local runs. Recorded as-measured. R14 exception (b) — pure doc compaction. R15(c): B-127 🔴 → ✅ same slice. R17: B-127 claimed; B-128 next-free. Two-commit pattern. Files touched: SESSION-STATE.md, BACKLOG.md, docs/archive/session-state-history.md, docs/archive/backlog-closed.md. |
| B-126 | persona/email | Phase 5 spec #67 §12 amendment — D-1..D-14 locked from §14 OQ-1..OQ-14 | P3 | ✅ | docs/specs/phase-5/67-career-climber-role-intent.md | filed + closed `b2248d2` 2026-05-04 (Mode-1 §12 amendment; R14 exception (b)) | Closed `b2248d2` 2026-05-04. Single-slice file+close per B-095 / B-099 / B-103 / B-123 §12-amendment precedent. Locks D-1..D-14 from §14 OQ-1..OQ-14 1:1 (Dhamo single-admin disposition; zero ambiguous hints, no STOP). **Notable locks:** D-1 auto-expand CC card on select (mirrors IP precedent); D-2 inline-form Profile edit modality; D-3 live per-tick aggregate query (no cache v1); D-4 quarter ceiling = current + 7 future (~2 years); D-5 NO auto-supersede on `PATCH /persona` (preserve through CC↔IP↔CC churn); D-6 explicit `[Clear]` button writing `superseded_at` only; D-7 extend `pro_digest.html` (NO template fork); D-8 silent suppression below cohort threshold; D-9 NO auto-archive of past-quarter rows (explicit user action only); D-10 exact `(target_role, target_quarter)` bucket match v1 (no role-alone fallback); D-11 frozenset constant + `@field_validator` for `target_role` enum; D-12 passive Profile-only re-onboarding (no `/home` banner); D-13 fire `career_intent_email_block_rendered` ONLY on `email_service.send_email` success path; D-14 silent suppression telemetry (no `pro_digest_intent_below_threshold` event v1). §14 rewritten to RESOLVED form (heading + first-sentence question + `→ Locked at §12 D-N` per row). 4 body cross-refs flipped from `§14 OQ-N` → `§12 D-N` (lines 232 + 242 + 535 + footer per spec §5.4 / §5.5 / §8.2 / footer). Spec status header: 🔴 Drafted → 🟡 §12 amendment landed (impl-ready). Spec line count: 771 → 786 (+15; §12 expansion outweighs §14 compaction; still well under 800 target / 1200 trigger). R14 exception (b) — pure rule refinement on authored spec, no design surface, no test surface (BE 857 / FE 530 carry-forward). R15(c): B-126 filed + closed same slice; B-125 stays 🔴 (impl-ready post-amendment); E-052 stays 🔴 (closes at B-125 impl-ship). **R17 watermark:** B-126 claimed; B-127 next-free numeric ID. Two-commit pattern (amendment + BACKLOG + SS in commit 1; SHA backfill in commit 2 replacing `b2248d2` placeholders). |
| B-125 | persona/email | Phase 5 spec #67 — Career-Climber role-intent capture (target_role + target_quarter + aggregate email framing; impl; closes E-052) | P1 | ✅ | docs/specs/phase-5/67-career-climber-role-intent.md | `d407e6e` spec-author; `b2248d2` §12 amendment; BE `9082a7b`; FE closed `e3fdf01` 2026-05-04 | Closed `e3fdf01` 2026-05-04. Both halves shipped — BE 2026-05-04 `9082a7b`, FE this slice. Cascade-closes E-052 same commit. **FE half:** new `src/components/profile/CareerGoalSection.tsx` mounted in `Profile.tsx` between Theme and Subscription (CC-gated per §8.2 D-2 inline-form + D-6 explicit `[Clear]` via `window.confirm`); `PersonaPicker.tsx` extended with auto-expand CC card (D-1) carrying optional `target_role` + `target_quarter` selects mirroring IP precedent — submit fires `setCareerIntent` after `updatePersona` only when both fields filled (AC-19/20/21 per §8.1; failure toasts "Goal not saved — set it from Profile." + nav still proceeds); new `src/utils/careerIntent.ts` (CAREER_ROLES + CAREER_ROLE_LABELS + `quarterOptions(now, 7)` per D-4 + `quarterLabel`); `src/services/api.ts` adds `setCareerIntent` (POST + `X-Capture-Source` per D-13) / `getCareerIntent` (404 → null via `validateStatus` so the response interceptor does NOT toast on the expected no-intent path) / `clearCareerIntent` (DELETE 204); `src/types/index.ts` gains `CareerIntent` + `CareerIntentCreateRequest`. PostHog `career_intent_captured` (first) / `career_intent_updated` (subsequent + `cleared` semantics) fire with `source: 'persona_picker' | 'profile_edit'`. Tests +13 FE: `tests/PersonaPicker.career-intent.test.tsx` (5; AC-19/20/21 + auto-expand + partial-fill skip), `tests/Profile.career-goal.test.tsx` (5; AC-22/23/24/25 + Edit save), `tests/services/api.careerIntent.test.ts` (3; POST + GET 404→null + DELETE). Existing 3 Profile.* tests gain a one-line `vi.mock('@/components/profile/CareerGoalSection', ...)` stub matching the precedent for XPBar / EmailPreferences / ThemePicker. FE 530 → 543 ✅ +13 (§10.2 floor exactly). BE 877 carry-forward. tsc clean modulo pre-existing `ScoreDeltaWidget.tsx` warning (carry-forward). R14 default. R17 watermark unchanged at B-126 (B-127 next-free); FE half does NOT consume watermark per 6.4b precedent. CR ❌ stale (PersonaPicker + Profile + types + api + new util + new component). Two-commit pattern (impl + tests + BACKLOG + SS in commit 1; SHA backfill in commit 2). |
| B-124 | testing/hygiene | Date-bomb fix in `test_pro_digest_service::test_compose_digest_populates_all_fields_when_present` — `mission_days_left` off-by-one when local date and UTC date crossed midnight | P3 | ✅ | | filed + closed `ed7784d` 2026-05-03 (Mode-1 light test fix; R14 exception (b)) | Closed `ed7784d` 2026-05-03. Resolves B-122 JC #1 carry-forward. Pinned the service's "today" via `patch("app.services.pro_digest_service.datetime")` returning `datetime(2026, 5, 1, 12, 0, tzinfo=timezone.utc)` and inlined the Mission seed with `target_date = date(2026, 5, 1) + 5d` so `(target - today_utc).days == 5` regardless of pytest runtime. `_seed_mission` helper + `pro_digest_service.py` byte-untouched (Q2). 3× sequential runs of the target test all green; full pro_digest file 14/14 passing. Full CI subset under prod-default env vars: pre-fix 856 passed / 1 failed → post-fix 857 passed / 0 failed (delta exactly +1, the date-bomb). **JC #1 (info-only):** SS Test Suite Status documents BE 865 from B-120; measured baseline at HEAD `2a72ebc` is 856 — accumulated close-line accounting drift from B-120 / B-122, not introduced here. **JC #2 (info-only):** without env vars 9 additional pre-existing failures surface across `test_usage_limits` / `test_interview_storage` / `test_payments_usage_route` / `test_study_api` — exact D-027 / D-028 lineage (env-var cache + alembic-roundtrip), orthogonal to this slice. Not fixed per Q2 / C3. R14 exception (b). R15(c): single-slice file+close per B-121 / B-105 / B-123 precedent. R17: B-124 claimed; B-125 next-free numeric ID. Two-commit pattern (test fix + BACKLOG + SS in commit 1; SHA backfill in commit 2 replacing `ed7784d` placeholders). |
| B-123 | home/ux | Phase 5 spec #66 §12 amendment — D-1..D-14 locked from §14 OQ-1..OQ-14 | P3 | ✅ | docs/specs/phase-5/66-appshell-loop-progress-strip.md | filed + closed `042f92c` 2026-05-03 (Mode-1 §12 amendment; R14 exception (b)) | Closed `042f92c` 2026-05-03. Single-slice file+close per B-095 / B-099 / B-103 §12-amendment precedent. Locks D-1..D-14 from §14 OQ-1..OQ-14 1:1 (Dhamo single-admin disposition; zero ambiguous hints, no STOP). **Notable locks:** D-1 score-history-row-count heuristic for step 3 'done' (no unlock-time persistence); D-2 mobile = icon-only single-row at <md / stacked at md+; D-3 category lookup only (skill-name → category, card-tag forward work); D-4 suppress `loop_frame_rendered` when surface='appshell'; D-5 hardcoded constants v1 (MIN_DAYS_SINCE_SCAN=3, MIN_PERCENT_REVIEWED=50); D-6 verify token at impl Step 1; D-7 mount-level useRef keyed on tracker_id (refire on remount); D-8 sibling-of-TopNav placement; D-9 render with step 1 future even when last_scan_date is on a different tracker; D-10 deriveCurrentStep helper locked in §8.4; D-11 explicit `compact` prop separate from `surface`; D-12 useRef<Set>/useRef<boolean> Strict-Mode idempotency pattern; D-13 flat URL `/api/v1/learn/loop-progress?tracker_id={id}`; D-14 step 3 cannot unlock when useLoopProgress errors (documented limitation, not bug). §14 rewritten to RESOLVED form (heading + first-sentence question + `→ Locked at §12 D-N` per row). 4 body cross-refs flipped from `§14 OQ-N` → `§12 D-N` (lines 176, 233, 240, 289). Spec status header: 🔴 Drafted → 🟡 §12 amendment landed (impl-ready). Spec line count: 416 → 435 (+19; §12 expansion slightly outweighs §14 compaction; still well under 800 target / 1200 trigger). R14 exception (b) — pure rule refinement on authored spec, no design surface, no test surface (BE 865 / FE 508 carry-forward). R15(c): B-123 filed + closed same slice; B-122 stays 🔴 (impl-ready post-amendment); E-051 stays 🔴 (closes at B-122 impl-ship). **R17 watermark:** B-123 claimed; B-124 next-free numeric ID. Two-commit pattern (amendment + BACKLOG + SS in commit 1; SHA backfill in commit 2 replacing `042f92c` placeholders). |
| B-122 | home/ux | Phase 5 spec #66 — live loop-progress strip in AppShell for Interview-Prepper (impl; closes E-051) | P1 | ✅ | docs/specs/phase-5/66-appshell-loop-progress-strip.md | `8dcdccd` (spec-author); §12 amendment `042f92c`; closed at `261dc66` | Closed `261dc66` 2026-05-03. Implements spec #66 D-1..D-14 verbatim. AC-1..AC-14 all green. **What shipped:** new `app/schemas/loop_progress.py` (LoopProgressResponse) + `app/services/loop_progress_service.py` (`get_loop_progress` per D-3 skill→category lookup) + `app/api/v1/routes/loop_progress.py` (`GET /api/v1/learn/loop-progress?tracker_id={id}` per D-13) + `main.py` router mount. New `src/components/layout/LoopProgressStrip.tsx` mounted in `AppShell.tsx` as sibling below `<TopNav />` (D-8); render gate `persona === 'interview_prepper' && next_interview != null` (chromeless gated by parent `showChrome`). Hook composition: `useAuth` + `useHomeState` + `useScoreHistory(tracker_id)` + new `useLoopProgress(tracker_id)`. Step-state derivation per §8.2: step 1 from history.length, step 2 from percent_reviewed vs 50, step 3 from D-1 heuristic (history.length≥2 AND step-2 done = done; days≥3 AND step-2 done = current; else locked), step 4 from days-until-interview (alert <0; current ≤7d; else future). Step 3 click → `/prep/tracker?focus={tracker_id}&action=rescan` deep-link. **LoopFrame extension** (§4.1 hybrid a+c): backward-compatible `stepStates?: Partial<Record<step, state>>` + `onStepClick?` + `compact?` + `surface` widened `'results' | 'appshell'`; Results.tsx call site byte-untouched (LoopFrame existing 9 tests stay green). `loop_frame_rendered` analytics suppressed when `surface === 'appshell'` per D-4 (no double-counting). **4 new PostHog events**: `loop_strip_rendered` (once-per-mount keyed on tracker_id per D-7) / `loop_strip_step_clicked` (step 3 only) / `loop_strip_rescan_unlocked` (locked→current transition; useRef<boolean>) / `loop_strip_step_completed` (per-step useRef<Set<step>> per D-12). Hardcoded constants per D-5: `MIN_DAYS_SINCE_SCAN=3`, `MIN_PERCENT_REVIEWED=50`, `INTERVIEW_ALERT_WINDOW_DAYS=7`. D-6: used existing `border-danger`/`text-danger` (no new token added). D-9 honored: render with step 1 future when latest scan is on a different tracker. D-11: explicit `compact={true}` prop passed by strip; not implied by surface. D-14: `useLoopProgress` error → step 2 stays future → step 3 stays locked (documented limitation). Mobile per D-2: `compact` prop tightens layout (no sublines, smaller padding). **JC #1 (info-only):** pre-existing date-bomb test failure in `test_pro_digest_service::test_compose_digest_populates_all_fields_when_present` (`mission_days_left == 4` vs expected `5`; off-by-one on UTC-edge run today 2026-05-03). Zero overlap with this slice's blast radius (`pro_digest_service.py` is from spec phase-6 #14 / B-098, byte-untouched). Surfaces today as time-of-day-sensitive failure; carries forward as orthogonal triage candidate (similar to D-027 / D-028 lineage). **Tests +10 BE** (4 service `test_loop_progress_service.py` + 4 route `test_loop_progress_route.py` + 1 cross-user 404 + 1 ownership): BE 865 → 866 passing under prod-default env (1 unrelated date-bomb fail in test_pro_digest unchanged in direction; 17 deselected; total collected 875). **Tests +22 FE** (12 LoopFrame existing + 3 new LoopFrame extension = 15 LoopFrame; 16 LoopProgressStrip; 3 useLoopProgress; 8 AppShell unchanged): FE 508 → 530. tsc clean modulo pre-existing `ScoreDeltaWidget.tsx` warning (carry-forward). Analytics catalog gains 4 new event rows in `.agent/skills/analytics.md`; existing `loop_frame_rendered` row note updated to reflect §9.2 surface widening + appshell suppression. R14 default. R15(c): B-122 🔴 → ✅; cascade-closes E-051 same commit. R17: B-122 stays highest claimed; B-123 was claimed by §12 amendment slice; B-124 next-free. Two-commit pattern (impl + tests + BACKLOG + SS + analytics in commit 1; SHA backfill in commit 2). | Forward-filed for spec #66 impl per R15(c). **Pending §12 amendment slice before impl pickup** — locks D-1..D-N from §14 OQ-1..OQ-14 (14 OQs surfaced) mirroring slice 6.0 / 6.4.5 / 6.5 / 6.6 / 6.7 / 6.14 / 6.15 / spec-#64 precedent. **Scope (per spec §3 + §13):** new `<LoopProgressStrip>` at `src/components/layout/LoopProgressStrip.tsx` mounted in `AppShell.tsx` between `<TopNav />` and `<main>{children}</main>` (NOT `<Outlet />` — disk reality per spec §4.4 JC #1). Render gate: `showChrome && persona === 'interview_prepper' && next_interview != null`. Per-step state derivation per spec §8.2: step 1 from `useScoreHistory(tracker_id)` latest score; step 2 from new `useLoopProgress(tracker_id)` hook; step 3 derived from step 2 + `days_since_last_scan`; step 4 from `next_interview.date`. Click-through on step 3 (when `'current'`) → `/prep/tracker?focus={tracker_id}&action=rescan` per E-043 deep-link contract. **LoopFrame extension** (spec §4.1 hybrid a+c decision): backward-compatible `stepStates?` + `onStepClick?` + `compact?` props + `surface` widened to `'results' \| 'appshell'`; Results.tsx call site byte-untouched. **One new BE endpoint** `GET /api/v1/learn/loop-progress?tracker_id={id}` returning `{total_gap_cards, reviewed_gap_cards, percent_reviewed, days_since_last_scan}`; new service `learning_progress_service.compute_loop_progress`. Zero migrations, zero new tables, no schema changes. **4 new PostHog events** per spec §9.1: `loop_strip_rendered`, `loop_strip_step_clicked`, `loop_strip_rescan_unlocked`, `loop_strip_step_completed`. `loop_frame_rendered` suppressed when `surface='appshell'` per §9.2 (no double-counting). **Dependencies:** spec #34 / #57 / #63 / #64 / #65 (all shipped); B-086a + B-086b (foundation + orchestrator). **Out of scope** (per spec §13): CC variant (E-053), team_lead variant, animations, click on steps 1/2/4, dismissal, multi-interview support, caching, Phase 6 read consolidation. **Test envelope estimate per spec §10.1 + §10.2:** +22 FE (floor +18, ceiling +25) + +8 BE (floor +6, ceiling +10). R14 default — implements authored spec (#66). **R15(c):** B-122 filed 🔴 by this slice; closes at impl-slice merge. **R17 watermark:** B-122 claimed by this slice; B-123 next-free numeric ID. E-054 next-free numeric E-ID (E-053 still highest in-use). |
| B-121 | process/hygiene | SESSION-STATE + BACKLOG compaction — clears health-check warns (RC archive cut + Ops-Log April archive + Drift D-019/D-022 RESOLVED + Next Slice / Test Suite / Last hand-edit / Deferred Hygiene compaction + 8 BACKLOG closed-row archive cut) | P3 | ✅ | | filed + closed `c83bc07` 2026-05-03 (Mode-1 light hygiene; R14 exception (b)) | Closed `c83bc07` 2026-05-03. Single-slice file+close per B-109 precedent. Pre-flight: SESSION-STATE 510 lines / worst RC 248 words / BACKLOG 279 lines (2 warns + 1 fail on process-tax). Tasks 1-5 executed: **(1)** RC archive cut — entries 4+ from `## Recently Completed` cut to `docs/archive/session-state-history.md` §9 cut #1 (38 entries spanning E-049 design-review batch through B-102 close, 2026-05-02..03); active SS keeps 3 most recent (B-120/E-050, B-119/E-049, spec #64). **(2a)** Next Slice section replaced with one-line pointer to BACKLOG. **(2b)** Pre-archive-era Ops-Log entries from 2026-04-19/20 cut to archive §9 cut #2 (18 entries). **(2c)** Test Suite Status counts refreshed to BE 865 / FE 508. **(2d)** Last hand-edit field compacted from 25-line wall to one-line. **(3)** Drift D-019 + D-022 flipped to ✅ RESOLVED with compact resolution column (mitigation codified in SOP-8). **(4)** BACKLOG closed-row archive cut — 8 rows (B-100, B-098, B-099, B-097, B-078, B-096, B-094b, B-094a) cut to `docs/archive/backlog-closed.md` under new "Cut 2026-05-03 from `c83bc07` — B-121 light-hygiene compaction" heading; new ↳ pointer row added. **(5)** Deferred Hygiene compacted — `~~5.17b~~ (CLOSED)` bullet deleted; `[5.17-follow] tracker jd_hash dedupe` flipped to ✅ Closed (D-020 resolved by B-086a `210dcb2`); `[5.17-follow] flat /api/* cleanup` retained as-is. **JC #1 (prompt-vs-disk drift, info-only):** prompt cited "rows dated 2026-05-01 or earlier (i.e., everything through B-098/B-099/B-100 and older)" — disk shows B-098/B-099/B-100 all carry 2026-05-02 dates. Used row-name boundary (B-100 inclusive and below) per evident intent (clear the BACKLOG.md line-count warn). Documented in archive cut header. **JC #2 (warns partially clear, info-only):** post-execution health-check shows SESSION-STATE line warn + BACKLOG line warn + worst-RC-words warn all improved but not fully cleared by the prompt's listed task scope alone; process-tax fail unchanged (orthogonal — out of scope). User can request follow-up compaction (e.g., active-row note budget tightening, Drift archive cut to `docs/drift-archive.md`, decision-log archive) if full clearing required. R14 exception (b) — pure doc compaction, no design surface, no test runs (BE 865 / FE 508 carry-forward). R15(c): B-121 🔴 → ✅ same slice. R17: B-121 claimed; B-122 next-free numeric ID. Two-commit pattern (compaction + B-121 file/close in commit 1; SHA backfill in commit 2). Files touched: SESSION-STATE.md, BACKLOG.md, docs/archive/session-state-history.md, docs/archive/backlog-closed.md. |
| B-120 | home/ux | Phase 5 spec #65 — status-sentence hero on `/home` (combined spec+impl; closes E-050) | P1 | ✅ | docs/specs/phase-5/65-home-status-hero.md | filed + closed `58bb9a9` 2026-05-03 | Closed `58bb9a9` 2026-05-03. Combined Mode-2 spec+impl slice — spec #65 authored at Step 1 with §12 carrying D-1..D-10 pre-locked; implementation shipped same commit per single-slice file+close precedent (mirrors B-105 / B-113..B-118). New `src/components/home/HomeStatusHero.tsx` — one-line `<p>` mounted in `HomeDashboard.tsx` between greeting `<h1>` (line 213-215) and `<StateAwareWidgets>` (line 216) per D-1; sibling of persona-mode JSX (not inside InterviewPrepperMode / CareerClimberMode). Render gate: `persona === 'interview_prepper' \|\| 'career_climber'` (D-8). IP copy `${company} interview in ${days}d. ${dueCount} cards due today. Last scan was ${score}%.` (D-3); CC copy `${streak}-day streak. ${dueCount} cards due today.` (D-4) — each clause omits gracefully on null/zero/error per spec §8.2/8.3 tables. Component reads `useAuth()` for persona, `useHomeState()` for `next_interview` + `current_streak` + `plan`, `fetchDailyQueue()` for `total_due`, `fetchUserApplications()` (IP only) for latest `ats_score`. Greeting + B-016/B-027 fork byte-untouched (D-2 — additive only this slice). One new PostHog event `home_status_hero_rendered {persona, plan, clauses_shown[]}` fires once on mount via `useRef` after at least one clause resolves; `home_status_hero_data_missing` second event collapsed into `clauses_shown` array (D-6 — fewer events, same telemetry signal). Styling: `text-lg`, `text-text-secondary`, `mb-6`, design-token-only (D-7, R12). team_lead returns null (D-8). **JC #1 (info-only, D-3 disk-vs-prompt drift):** prompt-body D-3 cited "latest tracker ats_score from homeState.context"; on-disk `HomeStateContext` carries `last_scan_date` only, no `ats_score`. Spec body locks `fetchUserApplications()` mirroring `LastScanWidget.tsx:24-40` (sort desc by `created_at`, take `[0].ats_score`). Same fetcher already mocked in `tests/HomeDashboard.test.tsx`. **JC #2 (info-only, test path):** prompt §15 cited `tests/components/home/HomeStatusHero.test.tsx`; disk convention is `tests/home/<X>.test.tsx` mirroring `src/components/home/<X>.tsx` (see `StateAwareWidgets`, `DashboardWidget` precedents). Filed at `tests/home/HomeStatusHero.test.tsx`; spec §15 amended to match disk convention. **JC #3 (info-only, test count over ceiling by 1):** spec §15 envelope target +9, ceiling +11; delivered +12 (4 IP + 4 CC + 2 gate + 2 analytics). The +1 over ceiling is the "no-fire when component renders null" telemetry-regression test — genuinely useful (catches a bug where a team_lead user would still fire analytics) and was kept. **JC #4 (info-only, BE baseline drift):** prompt expected BE 858; disk shows BE 865 (E-037 closure at `686a624` shipped +7 not absorbed into prompt baseline). Not a slice issue — FE-only work, BE carry-forward. **Tests +12 FE:** new `tests/home/HomeStatusHero.test.tsx` (12 tests). FE 496 → 508. BE 865 carry-forward (zero BE surface). tsc clean modulo pre-existing `ScoreDeltaWidget.tsx` unused-import warning (carry-forward from B-119 / E-049). **Regression:** `tests/HomeDashboard.test.tsx` patched with one-line `vi.mock('@/components/home/HomeStatusHero', ...)` stub (Q2 surgical, mirrors how `StateAwareWidgets` and `StudyGapsPromptWidget` are stubbed in that file); existing 21 HomeDashboard cases untouched. Analytics catalog gains `home_status_hero_rendered` row in `.agent/skills/analytics.md` between `home_study_gaps_clicked` and `loop_frame_rendered`. R14 default (implements authored spec #65). R15(c): B-120 🔴 → ✅ same commit; cascade-closes E-050. R17 watermark: B-120 claimed by this slice; B-121 next-free post-slice. E-053 still highest in-use E. Two-commit pattern (impl + tests + spec + BACKLOG + SS + analytics in commit 1; SHA backfill in commit 2). Closes E-050 in full per single-combined-slice scope. **Files Touched:** `docs/specs/phase-5/65-home-status-hero.md` (new) ▪ `hirelens-frontend/src/components/home/HomeStatusHero.tsx` (new) ▪ `hirelens-frontend/src/pages/HomeDashboard.tsx` (import + mount, +2 lines) ▪ `hirelens-frontend/tests/home/HomeStatusHero.test.tsx` (new) ▪ `hirelens-frontend/tests/HomeDashboard.test.tsx` (one-line vi.mock stub) ▪ `.agent/skills/analytics.md` (new event row) ▪ `BACKLOG.md` (B-120 row + E-050 close) ▪ `SESSION-STATE.md` (Recently Completed entry). |
| B-119 | results/ux | Phase 5 spec #64 — static loop frame on Results page (impl; closes E-049 part (b)) | P1 | ✅ | docs/specs/phase-5/64-results-loop-frame.md | `8b70037` (spec-author); closed at `b58a42d` | Closed `b58a42d` 2026-05-03. New `src/components/dashboard/LoopFrame.tsx` (4-step strip — "Scanned / Studying / Re-scan / Interview" verbatim per §14 OQ-2; horizontal at md+, vertical stack <md; design-token-only per R12). Mounted in `Results.tsx` ABOVE the dashboard grid container (sibling element, not 12th direct grid child — see JC #1). `useHomeState` newly imported into Results.tsx for D-8 `next_interview` source. Render-gated on `result.ats_score != null`. Fires `loop_frame_rendered {surface, current_step, has_interview_date, plan}` once-on-mount via `useRef` guard. New ordering invariant in `Results.ordering.test.tsx` asserts `loop-frame` before `missing-skills`. §12 amended same commit — locks D-1..D-14 from §14 OQs. Analytics catalog gains `loop_frame_rendered` row in `.agent/skills/analytics.md`. **JC #1 (info-only — D-1 deviation from spec §4.2 / OQ-1 hint):** mounted ABOVE the grid container (not as 12th direct grid child) to avoid cascading row-start shifts on 11 existing children + breakage of `Results.layout.test.tsx::test_improvements_spans_two_rows_at_xl_to_decouple_row3_height`. Visual outcome ("frame above missing-skills") + DOM-ordering invariant identical; AC-9 regression set stays green untouched. **JC #2 (info-only — disk-reality field names):** prompt D-6 cited `overall_score`, D-7 cited `missing_skills`; disk `AnalysisResponse` carries `ats_score` + `skill_gaps`. Used disk reality. **Tests +10 FE:** 9 in new `tests/components/LoopFrame.test.tsx` + 1 new ordering invariant in `Results.ordering.test.tsx`; FE 486 → 496 (in spec §10.4 envelope target +9, ceiling +11). BE 858 carry-forward (zero BE surface). tsc clean modulo pre-existing `ScoreDeltaWidget.tsx` unused-import warning per §14 OQ-9 hint (a). **Regression set verified green** — `Results.{ordering,layout,tooltips,reanalyze,optimize,hydration}.test.tsx` + `MissingSkillsPanel.cta.test.tsx` all pass; 9-value `results_tooltip_opened` enum unchanged; `MissingSkillsPanel.tsx` byte-untouched. 6 Results test files patched with one-line `vi.mock('@/hooks/useHomeState', ...)` stub each (Q2 surgical). R14 default. R15(c): B-119 🔴 → ✅; cascade-closes E-049. R16 leaf-surface honored. R17 unchanged: B-119 highest in-use, B-120 next-free. Two-commit pattern (impl + tests + spec §12 amendment + BACKLOG + SS in commit 1; SHA backfill in commit 2). Closes E-049 part (b); part (a) shipped at E-011 ✅ `fd4ca3d`. **Original scope (preserved for audit):** AC-1..AC-11 from spec §11. (a) New `src/components/dashboard/LoopFrame.tsx` — 4-step horizontal strip on `lg+` / vertical stack on mobile, design-token-only styling (R12), `useRef`-guarded `loop_frame_rendered` PostHog event once-on-mount. Step labels locked verbatim per §14 OQ-2: "Scanned" / "Studying" / "Re-scan" / "Interview". (b) `Results.tsx` — wire `useHomeState()` to read `homeState.data?.context.next_interview.date`, derive `daysUntilInterview` (null when no date / loading / errored), insert a 12th direct grid child between Job Fit (`#job-fit`) and Missing Skills (`#missing-skills`) with `id="loop-frame"` spanning `lg:col-span-2 xl:col-span-3`. (c) `.agent/skills/analytics.md` — new `loop_frame_rendered` row per spec §9.3 (insert near `home_*` rows). (d) Tests per spec §10 — new `tests/components/LoopFrame.test.tsx` (~+8) + 1 new ordering invariant in `tests/pages/Results.ordering.test.tsx`; envelope +9 FE (floor +7, ceiling +11), BE unchanged. (e) AC-9 regression: spec #21 / spec #22 test files (Results.ordering / Results.layout / Results.tooltips / Results.reanalyze / Results.optimize / Results.hydration / MissingSkillsPanel.cta) stay green; the 9-value `results_tooltip_opened` enum unchanged (loop-frame is NOT a PanelSection). (f) AC-10: zero hardcoded hex / inline color `style` in `LoopFrame.tsx`. **Dependencies:** spec #21 (results layout, shipped) ▪ spec #22 / E-011 (`fd4ca3d` MissingSkillsPanel three-state CTA — DO NOT TOUCH) ▪ spec #57 (`homeState.context.next_interview` envelope, shipped at `b13f410`) ▪ existing `useHomeState()` hook. **Out of scope:** live state, animation, click-through, AppShell mount, persona variants — all owned by **E-051**. No edits to MissingSkillsPanel, PaywallModal, PanelSection, AppShell, App.tsx. **R16 leaf-surface prediction:** `LoopFrame.tsx` (new) + `Results.tsx` (insert one grid child + read `homeState`) + `analytics.md` (catalog row) + 1 new test file + 1 new test case in existing ordering file. No shared types touched beyond `LoopFrame`'s own exports. No BE / schema / migration. **R14 default** — implements an authored spec (#64). **R15(c):** B-119 filed 🔴 by this slice; closes at impl-slice merge per spec §11 ACs and R15(c) close-line. **R17 watermark:** B-119 claimed by this slice (next-free per disk grep at slice start; B-118 was last claimed despite the prompt body's stale `B-120` reference — corrected per starter-line R19 STOP + H4 watermark precedence). B-120 next-free numeric ID post-slice. |
| B-118 | billing/auth | AuthContext staleness fix — `refreshUser()` re-fetches `/auth/me` on post-Stripe redirects | P1 | ✅ | docs/audits/stripe-integration-audit-2026-05.md (#6 + #8 + #17 + #18 + #20 from 2026-05-03 scout) | `ace4584` (filed + closed in same slice) | Closed `ace4584` 2026-05-03. Closes 5 cancel-pending scout findings (#6/#8/#17/#18/#20) sharing one root cause: `useAuth().user` was set once at AuthProvider mount and never re-fetched, so post-Stripe redirects (checkout `success_url`, portal `return_url`) saw stale plan / `subscription` data until a hard reload. **Fix:** new `refreshUser(): Promise<void>` in `AuthContext.tsx` re-calls `GET /auth/me` (raw axios, mirrors hydration path), updates context user state + `localStorage`, and dedupes concurrent calls via a `useRef<Promise<void>>` inflight flag — second call returns the in-flight promise rather than firing a duplicate request. Errors are swallowed so callers can fire-and-forget post-redirect; the `api.ts` refresh interceptor still owns the stale-token path. Added to `AuthContextValue` interface (additive — no destructure breakage across the 30+ `useAuth()` consumers). Wired into 2 callsites: `Pricing.tsx` post-checkout effect (`?upgrade=success` URL-param branch alongside the existing `upgradePlan('pro')` optimistic flip) and `Profile.tsx` mount (portal `return_url`). 4 existing test mocks (`Pricing.cancelPending` / `Profile.subscription` / `Profile.experience` / `Profile.account`) extended with `refreshUser: vi.fn()`. **Tests +7 (FE):** new `tests/AuthContext.refresh.test.tsx` (4 — re-fetch updates user, concurrent-dedup, no-token no-op, swallow errors); Pricing +2 (post-checkout fires refresh, plain visit doesn't); Profile +1 (mount fires refresh). FE 473 → 480 (+7, in +6..+10 envelope); BE 858 carry-forward (zero BE surface). tsc clean modulo pre-existing `ScoreDeltaWidget.tsx` unused-import warning. **JC #1 (info-only):** prompt cited B-119 next-free; R17 watermark grep at slice start showed B-117 was last claimed and B-118 was the actual next-free. Claimed B-118 per H4 (watermark precedence over prompt-stated ID). **JC #2 (info-only):** UsageContext refresh deferred per prompt — separate blast radius (rate-limit-sensitive + multi-endpoint shape). **Deferred:** UsageContext staleness in same scout cluster — separate slice if needed. R14 exception (b) — bug fix bundle, no spec authored (scout doc IS the design surface). R15(c): B-118 filed + closed same slice. R17: B-118 claimed; B-119 next-free. Two-commit pattern (impl + tests + BACKLOG + SS in commit 1; SHA backfill in commit 2 replacing `ace4584` placeholders). |
| B-117 | billing | Cancel-pending UI bundle — Profile Reactivate CTA + Pricing cancel-pending tile + Pricing Reactivate→portal redirect + Pro tile cursor scope | P1 | ✅ | docs/audits/stripe-integration-audit-2026-05.md (#7 + #14 + #15 + #21 from 2026-05-03 scout) | `2ae9a69` (filed + closed in same slice) | Closed `2ae9a69` 2026-05-03. Bundles 4 cancel-pending UX gaps surfaced by the 2026-05-03 Stripe billing flow scout (this session, Mode 3): scout-#7 cosmetic Pro-tile cursor (already in working tree), scout-#14 functional /pricing cancel-pending invisibility, scout-#15 functional reactivation 409-trap on Upgrade-click, scout-#21 functional missing in-app reactivation surface. Single slice — same blast radius (`Pricing.tsx` + `Profile.tsx` subscription UI). **Fix A (#7):** `Pricing.tsx:355-401` — `disabled:cursor-wait` was applying the macOS beachball cursor to current-plan disabled state; scoped to `isCheckoutLoading` only via `clsx`. **Fix B (#21):** `Profile.tsx:376-390` — when `cancelAtPeriodEnd===true`, render a secondary outline "Reactivate Pro" button above "Manage subscription"; both invoke the existing `handleManageSubscription()` (opens Stripe portal where the un-cancel UX lives). **Fix C (#14):** `Pricing.tsx` — new `isCancelPendingPro` derived flag (`plan.planKey==='pro' && isCurrentPlan && cancelAtPeriodEnd`); current-plan badge swaps "Current Plan" → "Cancels <date>" via `toLocaleDateString` (mirrors Profile pattern); button label flips to "Reactivate"; button stays clickable in cancel-pending state (disabled gate `(isCurrentPlan && !isCancelPendingPro) || isCheckoutLoading`); shimmer effect re-enabled for the reactivation case. **Fix D (#15):** `handleCta` — when `cancelAtPeriodEnd && plan.planKey==='pro'`, opens billing portal instead of attempting checkout, sidestepping B-113 `AlreadyProError` 409. Fires `subscription_portal_opened` PostHog event with `source: 'pricing_reactivate'` for entry-point disambiguation. **Plumbing:** `Pricing.tsx` now imports `useAuth` + `createBillingPortalSession`; reads `user?.subscription?.cancel_at_period_end` + `current_period_end` (B-116 plumbing reused — Step 1 was a no-op). **Tests +5 (FE):** Profile +2 (`Reactivate Pro` button renders + invokes portal; hidden when `cancel_at_period_end===false`), Pricing +3 (Pro-tile shows "Cancels <date>"; Pro-tile shows "Currently Active" otherwise; `handleCta` opens portal not checkout when cancel-pending — asserts `createCheckoutSession` not called). New file `tests/Pricing.cancelPending.test.tsx`. **Tests:** FE 468 → 473 (+5); BE 858 carry-forward (zero BE surface). tsc clean modulo pre-existing `ScoreDeltaWidget.tsx` unused-import warning. **JC #1 (info-only):** Step 1 (plumb subscription fields) was a no-op — B-116 already exposed `cancel_at_period_end` + `current_period_end` via `/auth/me` → `useAuth().user.subscription`. Saved a slice's worth of plumbing work. **JC #2 (info-only):** Fix A's working-tree edit was preserved per prompt; commit 1 stages it alongside the new work rather than landing it as a separate commit. Q2 (touch only what slice requires) holds — single concern is "cancel-pending UX bundle" and the cursor fix is the same surface. **Deferred to a separate slice:** scout #6/#8/#17/#18/#20 (AuthContext staleness cluster) — different blast radius (provider-level changes to AuthContext.tsx + UsageContext.tsx). R14 exception (b) — bug fix bundle, no spec authored (audit + scout doc IS the design surface). R15(c): B-117 filed + closed same slice. R17: B-117 claimed; B-118 next-free. Two-commit pattern (impl + tests + BACKLOG + SS in commit 1; SHA backfill in commit 2). |
| B-116 | billing | F-2 + F-4 audit fix — `customer.subscription.updated` webhook handler + `current_period_end` populate (cancellation-pending state from billing portal) | P1 | ✅ | docs/audits/stripe-integration-audit-2026-05.md (F-2 + F-4) | `06ea5f6` (filed + closed in same slice) | Closed `06ea5f6` 2026-05-03. Implements audit-2026-05 F-2 + F-4 (P1). Audit-cited F-3 (`invoice.payment_failed`) **deferred** — Q3 ambiguity caught at Step 0 (prompt body described the F-2/F-4 cancellation surface, not F-3's dunning surface; scope (a) confirmed with Dhamo before Step 1). **F-2 fix:** new `_handle_subscription_updated(data, db)` in `app/services/payment_service.py` reads `cancel_at_period_end` + `current_period_end` (unix → naive UTC) + `status` via `_field()` (B-114 SDK-parity helper); plan stays `pro` until later `customer.subscription.deleted` fires. New `_find_subscription_by_stripe_ids` helper looks up by `stripe_subscription_id` first then `customer_id` (mirrors Stripe's event-payload identity shape). Dispatch wired between `checkout.session.completed` and `customer.subscription.deleted` branches. **F-4 fix:** `current_period_end` populated on every `customer.subscription.updated` (audit noted the column was only ever cleared, never set). **Schema:** new `Subscription.cancel_at_period_end Boolean NOT NULL server_default false` via alembic `c2ba25f1d9a7_add_subscription_cancel_at_period_end.py` (downgrade head `c2b8a4d9e6f1`); existing rows backfill safely. `_handle_subscription_deleted` extended with `cancel_at_period_end = False` clear at final cancellation for shape consistency (surgical 1-line addition mirroring existing `current_period_end = None` clear). **FE:** `/auth/me` subscription dict gains `cancel_at_period_end`; `AuthUser` type extended with optional `subscription: AuthSubscription`; `Profile.tsx` Subscription section renders "Cancels <date>" via `toLocaleDateString` when `cancel_at_period_end===true`, falls back to "Active" when the field is absent (BE-not-yet-upgraded path; back-compat). **Tests +6:** BE 4 (`test_subscription_updated_records_pending_cancellation` / `_reactivation_clears_cancel` / `_status_past_due_propagates` / `_real_stripe_event_object` — the last uses `stripe.Event.construct_from` per B-114 precedent), FE 2 (Cancels-<date> render path + back-compat fallback). BE 854 → 858 (+4 collected); FE 466 → 468 (+2). **Pre-existing carry-forward:** 10 BE failures from D-027 (env-var-cache lru_cache fragility on `interview_prep` limits) + 1 FE order-dependent flake in `StudyGapsPromptWidget` (passes in isolation post-stash test) + 1 tsc unused-import warning in `ScoreDeltaWidget.tsx` — all byte-isolated from this slice (verified via stash test on the tsc warning). **JC #1 (info-only):** prompt mis-cited audit F-3 — actual scope is F-2 + F-4 per the audit doc; resolved at Step 0 before Step 1. **JC #2 (info-only):** audit also recommended populating `current_period_end` from `_handle_checkout_completed`, but the Checkout Session payload doesn't carry that field — it lives on the Subscription object. Limited to `_handle_subscription_updated` this slice; population happens on first portal-cancel update OR monthly renewal. R14 exception (b) — implements audit recommendation, no spec authored (audit doc IS the design surface). R15(c): B-116 filed + closed same slice. R17: B-116 claimed; B-117 next-free. Two-commit pattern (impl + tests + BACKLOG + SS in commit 1; SHA backfill in commit 2 replacing `06ea5f6` placeholders). **Cross-ref E-036** (Stripe E2E test suite, 🟦) — this slice provides webhook + portal cancellation paths E-036 will assert against. |
| B-104 | docs/audit | B-010 follow-up — Navbar.tsx is NOT orphan; row needs update-or-close decision | P3 | 🟦 | | `174e479` (filed at slice 6.15 §12 amendment per D-7) | Triage B-100 (`5291d9e`) flagged: B-010 row at BACKLOG.md:42 says "Orphan `Navbar.tsx` still in repo after TopNav migration" + "Dead code. Delete after confirming zero imports." Disk shows 2 live importers: `hirelens-frontend/src/pages/LandingPage.tsx` + `hirelens-frontend/src/pages/LoginPage.tsx`. Navbar is NOT orphan; B-010 row is stale. **Decision needed:** (a) close B-010 as ⚪ (won't-do — premise false) and amend B-010 Notes to record the false premise; OR (b) rewrite B-010 to track a real cleanup target (e.g. "consolidate Navbar + TopNav usage so only one nav lives in the repo"). Status 🟦 (parked) — chat-Claude triages activation. R15(b) caveat: status updates are the only edits CC may make autonomously, so this row's resolution requires either chat-Claude direction or a Dhamo-authorized prompt. R17 watermark advances: B-104 claimed; B-105 next-free. |

### Enhancements (E-)

| ID | Area | Title | Priority | Status | Spec | Source slice | Notes |
|----|------|-------|----------|--------|------|--------------|-------|
| E-001 | docs | Master doc audit + sync (playbook ↔ codebase) | P0 | 🔴 | | P5-S0, P5-S0b | Mandatory before more enhancement work. Output: `docs/audit/2026-04-doc-sync-audit.md`. |
| E-008 | persona | Existing-user PersonaPicker migration UX (banner + opt-in) | P1 | 🔴 | docs/specs/phase-5/34-persona-picker-and-home.md | P5-S19 | Don't auto-default. |
| E-012 | interview | Persist generated interview question sets per (user, JD hash) | P1 | 🔴 | docs/specs/phase-5/35-interview-question-storage.md | P5-S23, S24, S25 | Free-tier counter applies to fresh generations only, not cache hits. |
| E-013 | billing | Subscription cancellation flow (with reason picker) | P1 | 🔴 | docs/specs/phase-5/36-subscription-cancellation.md | P5-S26 | `cancel_at_period_end=true`. Optional win-back modal. |
| E-015 | settings | Settings persistence audit + harden | P1 | ✅ | | P5-S27 | Cascade-closed `c4132fb` 2026-05-03 by B-006 audit verdict — zero gaps surfaced; nothing to harden. See B-006 close-line for the surveyed surface (9 User columns + 4 per-user settings tables). |
| E-016 | study | Chat with AI on flashcards (per-card chat panel) | P2 | 🔴 | docs/specs/phase-5/37-chat-with-ai-flashcards.md | P5-S28, S29 | Free-tier daily message cap. Uses LLM router → Pro. |
| E-018 | admin | LLM-driven analytics insights dashboard | P2 | 🟡 | docs/specs/phase-5/38-admin-analytics.md | P5-S31, S32, S33 | Umbrella row. Five sections: metrics, performance, behavior, enhancement signal, feedback themes. Spec authored in `c7ff5b8` (2026-04-23) with 4-slice rollout (E-018a..d). Flips ✅ when E-018d closes. |
| E-018c | admin | Admin analytics — behavior + signals sections (4 funnels via PostHog Query API + paywall dismissals / zero-result searches / NPS / locked-category demand) | P2 | 🔴 | docs/specs/phase-5/38-admin-analytics.md | P5-S32 (slice 3/4) | Slice 3 of 4 under E-018. Covers AC-4 (behavior funnels via PostHog Query API), AC-5 (enhancement signals), and wires the new `search_no_results` frontend event in the card-browser search. Requires `POSTHOG_PROJECT_ID` + `POSTHOG_PERSONAL_API_KEY` env vars; `SKIP_POSTHOG=true` fallback degrades these cards to "unavailable" rather than 500. |
| E-018d | admin | Admin analytics — feedback themes section (LLM cluster of card_feedback.comment with PII scrub + 24h cache + 10/hr rate limit) | P2 | 🔴 | docs/specs/phase-5/38-admin-analytics.md | P5-S33 (slice 4/4) | Slice 4 of 4 under E-018. Covers AC-6 (themes endpoint, `generate_for_task(task="admin_feedback_cluster", tier="fast")`), AC-11 (10/hr rate limit, mirrors `/admin/cards/generate`). 24h Redis cache keyed by `(date_range, comment_row_count_bucket)`. Regex PII scrubber runs on every comment pre-LLM and re-verifies on the way back. Skip LLM when fewer than 5 comments in range. Closes E-018 umbrella on merge. |
| E-018b-follow | admin | Admin analytics — wire backend latency source for `api_latency` top-10 | P2 | 🔴 | docs/specs/phase-5/38-admin-analytics.md | (Slice 2 spin-off, 2026-04-23) | Spec #38 AC-3 includes `api_latency: list[RouteLatency]` for the top 10 routes by volume (p50/p95/p99). Deferred from E-018b because Slice 2 forbids PostHog Query API and no backend latency source exists. Scope when picked up: decide between (a) PostHog `$pageview` / backend-instrumented latency events via the Query API (natural fit for Slice 3 which already pulls PostHog), or (b) a new `request_log` table + middleware that samples and records per-route latency (no external dep but adds write traffic). Today the response returns `api_latency: []` with `api_latency_available: false`; UI renders a "Coming soon" tile. Close shape: schema stays the same, marker flips to true, tiles populate. |
| E-018b-follow-errors | admin | Admin analytics — add source for `error_rate_24h_pct` | P2 | 🔴 | docs/specs/phase-5/38-admin-analytics.md | (Slice 2 spin-off, 2026-04-23) | Spec #38 AC-3 includes `error_rate_24h_pct` (5xx / total requests, last 24h). Deferred from E-018b — today there is no backend error-event source and the global-exception-handler in `app/main.py` only returns an HTTP response without logging to a table. Scope when picked up: decide between (a) adding Sentry Client API integration (cheap, depends on `SENTRY_DSN` being set) or (b) adding to the `request_log` table proposed under E-018b-follow if that lands first. Today the response returns `error_rate_24h_pct: null` with `error_rate_available: false`; UI renders a "Coming soon" tile. Cross-ref: BACKLOG row E-018b-follow (same infrastructure decision may resolve both). |
| E-019 | admin | Content Feed Flow (bulk topic → AI draft → review queue) | P2 | 🔴 | docs/specs/phase-5/39-content-feed-flow.md | P5-S34 | Lowest user impact, ship last in Phase 5. |
| E-020 | results | Audit geo-pricing visibility on signup vs checkout | P1 | 🟡 | docs/specs/phase-5/27-geo-pricing.md | P5-S8 | Audit complete (`4aaeda4`): see `docs/audit/2026-04-E-020-geo-pricing.md`. Spec #27 authored (`c16f544`): see `docs/specs/phase-5/27-geo-pricing.md` — Gap B anchored at §7, Gap D anchored at §5 (60s failure-TTL per LD-4 / DD-2), Gap E anchored at §6 (literal `Free` per LD-6 / DD-1). Status stays 🟡 until per-gap impl slices ship (B → §7, D → §5, E → §6). Finding 0 closed by spec #27 authoring slice. |
| E-021 | docs | Backfill spec — Multi-model LLM router | P3 | 🔴 | docs/specs/phase-5/25-llm-router.md | P5-S1 | Spec backfill for already-shipped feature. Optional. |
| E-022 | docs | Backfill spec — Three-theme design system | P3 | 🔴 | docs/specs/phase-5/26-design-system.md | P5-S2 | Optional. |
| E-024 | docs | Backfill spec — Anti-abuse registration block | P3 | 🔴 | docs/specs/phase-5/28-anti-abuse-registration.md | P5-S4 | Optional. |
| E-025 | docs | Backfill spec — Job tracker auto-populate from ATS | P3 | 🔴 | docs/specs/phase-5/29-tracker-autopopulate.md | P5-S5 | Optional. |
| E-026 | docs | Backfill spec — Free-tier interview question limits | P3 | 🔴 | docs/specs/phase-5/30-free-tier-interview-limits.md | P5-S6 | Optional. Verify the actual limit value matches intent. |
| E-027 | docs | Backfill spec — Branding + Midnight Forge landing page | P3 | 🔴 | docs/specs/phase-5/31-branding-skillforge.md, docs/specs/phase-5/32-landing-page-midnight-forge.md | P5-S7 | Optional. |
| E-029 | learn | Lift `/learn/progress` out of Profile into its own page | P2 | 🔴 | | (deferred hygiene) | Skill radar + heatmap currently live inside Profile. |
| E-031 | billing | Paywall win-back email (deferred from E-014) | P2 | 🟦 | docs/specs/phase-5/42-paywall-dismissal.md (§5.5, §6, §9) | (parent: P5-S26b) | Win-back email triggered at 3 dismissals in 30 days, 30% off via Stripe coupon. Synchronous Resend send. Deferred from E-014 pending wall-hit telemetry. **Activation gate:** when `paywall_dismissed` PostHog event volume is sufficient to validate the 3-in-30 threshold — suggest minimum **50 dismissal events across distinct users**. **Already satisfied prereqs (shipped with E-014 BE):** `paywall_dismissals` table, `user.downgraded_at` column + `customer.subscription.deleted` webhook wire-up. **Outstanding work:** Stripe coupon creation (test + live) + `STRIPE_WINBACK_COUPON_ID` env + `app/core/config.py` Settings field; `paywall_service.is_winback_eligible` + `send_winback_email`; `app/templates/paywall_winback.html`; dismissal endpoint amendment (add `win_back_eligible` to response, add eligibility + send call-out); `GET /should-show-paywall` amendment (add `win_back_offered` to response shape); 5+ test cases (eligibility, fresh-churner exclusion, Pro short-circuit, send-time plan re-check, failure non-raising); PostHog events `winback_email_sent`/`winback_email_clicked`/`winback_converted`. Parent: E-014. Dependency: paywall dismissal telemetry. |
| E-032 | billing | Post-checkout redirect lands on /pricing instead of a celebratory welcome page | P3 | 🔴 | | (P5-S26b-impl-BE smoke) | Stripe `success_url` in `create_checkout_session` currently points at `/pricing`. User lands on Pricing seeing "Currently Active" on the Pro tile — functionally correct but misses the welcome moment. Fix: either add a `/welcome` route (celebratory state) or use `/profile?upgraded=true` with a conditional banner. Effort: small (<1h). Surfaced during P5-S26b-impl-BE smoke testing. |
| E-034 | ux | Generic error toasts don't give users actionable recovery paths | P3 | 🔴 | | (P5-S26b-impl-BE smoke) | Billing portal failure surfaces as "Couldn't open billing portal. Please try again." — no guidance on what to try or what went wrong. Pattern likely repeats elsewhere. After E-033 fix lands, audit error-surface sites and establish a recovery-copy pattern (e.g., "Subscription management is temporarily unavailable. Contact support."). Effort: small-to-medium depending on audit scope. Dependency: after E-033. Out of scope: replacing all toasts with inline banners — that is larger UX work. |
| E-036 | billing | Stripe integration E2E test suite | P1 | 🟦 | | (Path B verification debt from P5-S26b) | Full Stripe-lifecycle end-to-end integration test suite (against Stripe test mode + Stripe CLI webhook forwarding). Coverage targets: (a) checkout flow — `POST /payments/create-checkout-session` → hosted-checkout `success_url` → `checkout.session.completed` webhook → Subscription row created with `stripe_customer_id` persisted (guards E-033 regression); (b) webhook delivery — all live event types the handler branches on: `checkout.session.completed`, `customer.subscription.updated`, `customer.subscription.deleted` (includes `user.downgraded_at` stamp assertion), `invoice.paid`, `invoice.payment_failed`; (c) upgrade/downgrade state transitions (free → pro → free), plan short-circuits on paywall-dismiss + should-show-paywall endpoints after each transition; (d) billing portal session creation — requires working `stripe_customer_id` (smoke for E-033 fix); (e) paywall dismissal endpoints — POST `/payments/paywall-dismiss` + GET `/payments/should-show-paywall` 1-9 behavioral scenarios (LD-8 60s dedup, Strategy A grace counter, Pro bypass, admin bypass) — **direct Path B verification debt from P5-S26b-impl-BE where smoke tests 1-9 were never run**; (f) webhook idempotency (B-005 regression guard — duplicate delivery with same Stripe `evt_*` id short-circuits). Uses `@pytest.mark.integration` marker (CI-deselected; live-key-gated). **Effort:** medium-to-large (~1-2d) depending on Stripe CLI scaffolding reuse. **Return trigger:** before any production deploy of paywall feature OR before next Stripe-touching feature work, whichever first. Dependency: E-033 fix lands first (test suite will exercise billing portal). Cross-reference: overlaps with E-035 audit but complementary — audit is Mode 1 read-only code review, this is executable Mode 2 verification. |
| E-037 | security | Auth + rate limit on unauth Pro-tier Lens endpoints (rewrite, rewrite/section, cover-letter, interview-prep) | P2 | ✅ | | `686a624` (interview-prep half + rate-limit family); rewrite + cover-letter auth half closed by B-033 (`4922333`) | Closed `686a624` 2026-05-03. Closes the interview-prep half + adds slowapi `10/minute` per-IP rate-limit defense-in-depth across all four Pro-tier LLM endpoints (rewrite, rewrite/section, cover-letter, interview-prep). Auth half: `app/api/routes/interview.py` flipped from `get_current_user_optional` → `get_current_user`; the anonymous fallback branch (which silently bypassed the existing `interview_storage_service` quota by passing `user_id=None`) is removed. Both `/api/interview-prep` and `/api/v1/interview-prep` enforce auth via the same router (v1 is a re-export of the legacy module). Rate-limit half: `@limiter.limit("10/minute")` decorator + `request: Request` parameter added to `/rewrite`, `/rewrite/section`, `/cover-letter`, `/interview-prep` handlers. Limit applies to authenticated callers only — `Depends(get_current_user)` resolves before slowapi's wrapper, so unauthed calls short-circuit to 401 without bumping the per-IP counter (anonymous abuse is gated by auth, not by rate limit; documented in test docstring). Tests: BE 858 → 865 (+7) under prod-default env vars (`FREE_DAILY_REVIEW_LIMIT=10 FREE_LIFETIME_SCAN_LIMIT=1 FREE_MONTHLY_INTERVIEW_LIMIT=3`); 7 deselect-stable failures previously documented in D-027 carry forward (env-var-cache fragility on `interview_prep` limit, unrelated). New `tests/test_interview_prep_security.py`: 4 auth tests (anonymous→401 v1 + legacy, free-user under-cap 200 with mocked LLM, Pro-user 200) + 3 rate-limit tests (interview-prep / rewrite / cover-letter all return 429 on 11th authed request). FE 486 carry-forward (zero FE surface). B-033 regression suite (`test_rewrite_quota.py` 11 tests) green. R14 exception (b) — security replay of B-033 pattern. R15(c): E-037 🔴 → ✅; R17 watermark unchanged (status flip only). Two-commit pattern. |
| E-038 | landing | Anon-scan funnel on `/` | — | 🟦 | | (deferred 2026-04-21 per Path A decision) | BE supports anon scan via `get_current_user_optional` on `POST /api/v1/analyze`; FE does not expose it on `/`. Deferred 2026-04-21 in favor of auth-first funnel (Path A — see `docs/PHASE-5-STATUS.md` Canonical E2E Flow). Activation gate: a product decision that the acquisition funnel should allow anon scan before login (e.g., conversion data argues the auth wall drops too many visitors). Surfaced by `docs/status/E2E-READINESS-2026-04-21.md` §5; closest existing artifact is spec #22 (plan-aware Missing Skills CTA) which *presupposes* an anon-scan path in its "anonymous → Sign in to study" branch but does not define how anon users reach a scanned result. **Scope when activated:** needs a spec slice first (no existing spec covers the anon-scan landing experience). **Priority:** unassigned — set when/if activation gate trips. |
| E-040-follow | security | JWT revocation on admin demotion | P3 | 🔴 | | (follow-up to E-040 / spec #54 §Known Limitations) | A demoted admin retains live-session admin power until their access-token expires (up to 30 min per `ACCESS_TOKEN_EXPIRE_MINUTES=30`). Reconciliation on next login corrects DB state, but no active JWT invalidation exists. **Scope when picked up:** decide between (a) Redis blocklist keyed on `user_id` + last-demotion timestamp, checked in `get_current_user`, (b) short-lived admin-scoped access tokens (e.g. 5 min TTL for admins, 30 min for regular users) + refresh-on-every-admin-call, (c) per-request DB role re-check (drops LD-3 and trades latency for immediacy). Each has cost/complexity tradeoffs — needs its own spec. **Not blocking launch:** the 30-min window is intentional per spec #54 LD-3. File as security hardening, not a bug. |
| E-044 | content | Educational video for product walkthrough | P3 | 🟦 | | (from 2026-04-23 product-scope conversation) | Back-burnered per explicit rationale "product still changing weekly; video goes stale." **Activation gate — ALL THREE:** (1) Phase 5 core flows stable (no 🟡 PARTIAL rows in billing / study / results areas); (2) ≥ 20 paying Pro users (growth-validation floor); (3) no feature rename / reroute planned in the next 30 days (freeze window so the recorded flow doesn't go stale mid-production). **No spec** — when the gate opens, produce a script + shot list as part of activation; this row does NOT authorize pre-production work. |
| E-045 | research | Lifecycle CRM validation probe (single in-product feedback question) | P3 | 🟦 | | (from 2026-04-23 product-scope conversation) | Single in-product microsurvey question asking paying Pro users whether they'd pay $X more/month for end-to-end interview lifecycle tracking (recruiter calls, scheduling, round-by-round notes). **Activation gate:** ≥ 50 paying Pro users (Month-3 OKR milestone). **Rationale:** revealed-preference validation BEFORE any lifecycle-CRM scope expansion commitment. **Option D strategic lock stays in force** — this row is probe-only, NOT a commitment to build the lifecycle feature. Location TBD at activation (candidates: post-upgrade thank-you surface, Profile page banner, or in-app intercept on tracker page — pick based on which has the highest-intent dwell time when the gate opens). **No spec** — 3-question microsurvey + dashboard tile to read results is the full shape; author at activation. |
| E-049 | payments/ux | Pro upsell on missing-skills with static loop frame on `/prep/results` (closes PRD scan→study upsell gap; visual frame for the closed-loop differentiator) | P1 | ✅ | docs/specs/phase-5/64-results-loop-frame.md | (from 2026-05-03 design-review session, ordered #1 by Dhamo "revenue first") | **Closed `b58a42d` 2026-05-03** (cascade-closed at B-119 ship). Part (a) shipped earlier as E-011 ✅ (`fd4ca3d`, spec #22). Part (b) — static loop frame on `/prep/results` — shipped at B-119 (`b58a42d`); spec #64 authored at `8b70037`. **Highest-leverage revenue gap in the product.** Free user lands on `/prep/results`, sees their missing skills mapped to study cards (the PRD's killer flywheel per playbook 1.8), but has no in-page path to convert into a paid study session — the gaps are visible, the upsell isn't. Today: free user closes the page, conversion lost; Pro user sees the same surface with no differentiation. **Scope (single combined slice — both halves ship together):** **(a) Pro upsell button** in the missing-skills section of `Results.tsx` — for free users routes to `PaywallModal` with `trigger='skill_gap_study'` (REUSE existing trigger per spec #61 LD-S6/LD-S7 — DO NOT coin a new trigger; spec must explicitly verify this in §12 Locked Decisions); for Pro users routes to `/learn?source=last_scan` (consumes spec #62 hint banner already shipped); fires new `missing_skills_study_clicked {plan, gap_count}` PostHog event. **(b) Static four-step loop frame** (`Scanned → Studying → Re-scan → Interview`) rendered above the missing-skills section, no animation, no live state, no `tracker_application_scores` integration — pure visual framing that justifies the upsell ask by showing the user where this scan sits in the closed loop. Live wired version is forward-filed as **E-051** (different scope, different mount point, AppShell). **Out of scope (must be in spec §13):** live state on the loop frame, animation, click-through on loop steps, `tracker_application_scores` reads, AppShell mount, persona variants of the frame, any home-dashboard surface. **Cross-refs:** spec #61 (composition rules — `skill_gap_study` trigger origin); spec #62 (`?source=last_scan` consumer — Pro CTA target); B-030 / B-032 (paywall-trigger taxonomy — DO NOT fragment); E-051 (forward-link, live loop strip). **Spec-graph note:** this is the FIRST surface to render the loop visualization — establishes the visual contract that E-051 inherits. Spec must lock the four-step copy verbatim ("Scanned" / "Studying" / "Re-scan" / "Interview") so E-051 doesn't drift. **Test envelope estimate:** ~+5 BE (PostHog event payload contract) + ~+8 FE (free vs Pro routing × button click + loop-frame render gate + analytics fire). R14 default — new feature + new analytics event, spec-first. R17 watermark verified at filing: B-119 / E-048 highest in-use; E-049 claimed for this row, E-050 next-free. |
| E-050 | home | Status-sentence hero on `/home` for both B2C personas (replaces "Welcome back, Dhamodharan" greeting) | P1 | ✅ | docs/specs/phase-5/65-home-status-hero.md | Closed `58bb9a9` 2026-05-03 (cascade-closed at B-120 ship). Spec authored + impl shipped same commit per Mode 2 combined slice. Original "replaces greeting" framing softened at impl: hero ships ADDITIVE (renders BELOW the greeting, not instead of it) per spec #65 §12 D-2 — greeting + hero coexist as paired hero block; greeting stays as emotional hook (`text-text-primary`, font-display), hero is state summary (`text-text-secondary`, body line). Replacement is forward work gated on telemetry. CC 30-day topic-trend clause forward-filed to E-053 per D-4. Widget suppression of StreakWidget / TodaysReviewWidget / LastScanWidget per the BACKLOG row's risk-surface paragraph: NOT applied this slice (D-5 — additive only); future amendment to spec #61 §3 if telemetry shows multiple-emphasis hurts engagement. | Highest leverage-per-effort item in the design-review batch — hero status sentence above the widget grid replaces the affective "Welcome back" greeting with a single line summarizing the user's loop position. **Persona-driven copy** — Interview-Prepper sees "$company interview in $N days. $M cards due today. Last scan was $score%." (data sources: `homeState.context.next_interview` from E-042, `DailyReviewResponse.daily_status` from B-059, latest `tracker_applications_v2.ats_score`); Career-Climber sees "$N-day streak. $M cards due. $topic up $delta% this month." (data sources: `gamification_service.get_stats`, same daily_status, plus a NEW BE field for 30-day topic-trend — see Risk note below). **Scope:** new `<HomeStatusHero>` component at `src/components/home/HomeStatusHero.tsx`, mounted in `HomeDashboard.tsx` ABOVE both `InterviewPrepperMode` and `CareerClimberMode` JSX (not inside either — composition contract). Greeting useState/useRef snapshot pattern from B-016/B-027 preserved (do not regress first-visit/return-visit fork — see B-027 commit `e792bb4`). 2 new PostHog events `home_status_hero_rendered {persona, plan, has_data}` once-on-mount + `home_status_hero_data_missing {persona, missing_fields[]}` for telemetry on cold-start emptiness. **Risk surface — widget consolidation cascade:** with the hero on top, `StreakWidget` ("Streak: 0 days"), `TodaysReviewWidget` ("5 cards due"), and `LastScanWidget` ("ATS score 71%") will partially restate the hero on IP mode. Spec MUST decide explicitly: do those widgets stay (multiple emphasis on same metric — current pattern), get suppressed when hero renders (composition rule — extends spec #61 §3 matrix), or get re-scoped (e.g., StreakWidget shows trend graph instead of count)? **Default lean:** keep widgets, accept restatement — minimum-viable scope. Suppression matrix is forward-work tracked in this row's §13. **Spec-graph note:** this is the FIFTH spec governing `HomeDashboard.tsx` (after #34 / #40 / #44 / #61). Spec MUST include a "Composition interactions" section naming each predecessor spec and which rules it extends vs preserves. **Career-Climber 30-day topic-trend dependency:** the CC copy ("RAG up 18% this month") needs a NEW BE field on the gamification stats endpoint OR a new endpoint reading `quiz_review_events` (Phase 6 slice 6.0 surface). Spec must decide: ship CC copy without trend ("$N-day streak. $M cards due.") and forward-file the trend addition, OR bundle the BE work in this slice (heavier). **Default lean:** ship without trend, forward-file as part of E-053. **Cross-refs:** spec #34 (persona base); spec #40 (state-aware composition); spec #44 (widget empty states); spec #61 (free-tier composition rules); B-016 (first-visit greeting fork); B-027 (greeting flip race condition); B-019 (TodaysReviewWidget completed-state precedent). **Out of scope:** any change to the four widgets named above; widget suppression rules; the CC trend-data BE field (forward-file to E-053); a "third B2C persona" branch; any change to mobile layout beyond what the existing AppShell handles. **Test envelope estimate:** ~+12 FE (IP copy × 4 data states + CC copy × 4 data states + analytics fire-once + first-visit-vs-return-visit fork preservation). R14 default — new feature + new component + new analytics events. R17 watermark verified at filing: E-049 claimed by this batch, E-050 claimed for this row, E-051 next-free. |
| E-051 | home | Live loop-progress strip in AppShell for Interview-Prepper (consumes E-049 visual contract; the closed-loop moat made persistent) | P1 | ✅ | docs/specs/phase-5/66-appshell-loop-progress-strip.md | spec authored `8dcdccd`; §12 amendment `042f92c`; closed at `261dc66` (cascade from B-122 ship) | **Closed `261dc66` 2026-05-03** (cascade-closed at B-122 impl ship). Spec #66 authored `8dcdccd`; §12 amendment `042f92c` locked D-1..D-14; impl `261dc66` ships LoopProgressStrip in AppShell + new BE endpoint `GET /api/v1/learn/loop-progress` + LoopFrame extension. The closed-loop moat is now persistent across `/home`, `/learn/*`, `/prep/*` for Interview-Prepper users with an upcoming interview. See B-122 close-line for full delivery details. (Spec §4.4 JC #1 surfaced disk-vs-prompt drift on AppShell mount idiom — `{children}` not `<Outlet />`; same DOM position.) Spec §4.1 locks the LoopFrame refactor decision = hybrid (a)+(c): extend in place with optional `stepStates` + `onStepClick` + `compact` props, widen `surface` to `'results' \| 'appshell'`; Results.tsx call site byte-untouched (backward-compatible). Spec §4.4 JC #1: prompt cited "between TopNav and `<Outlet />`" — disk reality is `{children}` (not `<Outlet />`). Spec §6.1 introduces one new BE endpoint `GET /api/v1/learn/loop-progress`. 14 OQs surfaced in §14; §12 EMPTY pending amendment slice. (from 2026-05-03 design-review session, ordered #3 by Dhamo "Prepper conversion") | Brings the static loop frame from E-049 to life as a persistent strip in `AppShell` for `persona === 'interview_prepper' && next_interview != null`. The four-step visualization (`Scanned → Studying → Re-scan → Interview`) reads live state from `homeState.context.next_interview` (from E-042) + `tracker_application_scores` history (from E-043 / B-086a / B-086b). Persistent across `/home`, `/learn/*`, `/prep/*` — every page reinforces the loop. **THIS is the differentiator that justifies $49/mo over Jobscan's $29** — single component, surfaces three specs of shipped infrastructure (E-042 + E-043 + B-086b) that today have no top-of-app visibility. **Scope:** new `<LoopProgressStrip>` at `src/components/layout/LoopProgressStrip.tsx`, mounted in `AppShell.tsx` between `TopNav` and `<Outlet />`. Render gate: persona-IP AND has next_interview AND not on chromeless paths (`/`, `/login`, `/pricing` guest, `/onboarding/persona`, `/first-action` — see B-057 + B-058 chromeless path pattern). State machine for the four steps: **(1) Scanned** ✓ when most-recent tracker scan exists, shows score; **(2) Studying** ● active when scan exists AND has missing-skill-mapped cards AND <50% reviewed; ✓ when ≥50% reviewed; **(3) Re-scan** unlocked when ≥50% gaps reviewed AND ≥3 days since last scan (lock predicate locked in §12, configurable via env per `d19103c` env-tunable precedent); ● when unlocked, ✓ when re-scan completed; **(4) Interview** countdown shows days remaining; ● within 7 days, alert when overdue. Click on step #3 (when unlocked) opens `Tracker.tsx?focus={tracker_id}&action=rescan` (consumes E-043 rescan endpoint). 4 new PostHog events: `loop_strip_rendered {persona, current_step}`, `loop_strip_step_clicked {step, current_step, plan}`, `loop_strip_rescan_unlocked` (fires once when predicate flips), `loop_strip_step_completed {step, days_in_step}`. **Dependencies:** E-042 ✅ (`b13f410`); E-043 ✅ (`4aab0bf`); B-086a ✅ (foundation); B-086b ✅ (orchestrator + UI); spec #57 (`next_interview` envelope contract); B-057 + B-058 (chromeless paths — strip MUST NOT render on those routes). **Risk surface — visual contract drift with E-049:** the static frame in E-049 establishes the four-step copy + visual order. THIS slice MUST consume E-049's component or duplicate its copy verbatim — spec must choose and lock. **Default lean:** extract a shared `<LoopFrame>` primitive at this slice with E-049's static usage migrated to consume it (one-line refactor in `Results.tsx`); single source of truth for the four-step visualization. **Spec-graph note:** new file under `components/layout/` — touches AppShell composition. Spec must name interactions with B-057 / B-058 (chromeless paths) + B-029 (TopNav profile drop) + the desktop/mobile chrome split. **Activation gate:** E-049 (visual contract) + E-050 (status hero pattern proven on `/home`) BOTH shipped first — this slice consumes both. **Out of scope:** Career-Climber variant (E-053 owns the habit-ribbon equivalent); team_lead persona variant; any change to existing widgets; loop strip on chromeless / public pages; mobile-specific compact variant beyond responsive breakpoint. **Test envelope estimate:** ~+15 FE (4 step states × 2 personas × 2 plans + chromeless-path suppression + click-through + analytics fires + stale-data envelope handling). R14 default — new feature + new component + new analytics events. R17 watermark verified at filing: E-051 claimed for this row, E-052 next-free. |
| E-052 | persona | Career-Climber role-intent capture (target_role + target_quarter) and email-pipeline integration (CC retention lever — peer-aspiration framing in daily digest) | P1 | ✅ | | filed 2026-05-03 design-review; cascade-closed `e3fdf01` 2026-05-04 by B-125 FE merge (BE B-125a `9082a7b` + FE this slice) | Cascade-closed `e3fdf01` 2026-05-04 by B-125 FE merge. CC retention lever shipped end-to-end: capture surfaces (PersonaPicker auto-expand on CC select + Profile inline-form Career-goal section) + dedicated `POST/GET/DELETE /api/v1/users/me/career-intent` endpoints + append-only `user_career_intents` history + privacy-safe aggregate-only daily-digest framing (≥10 cohort minimum cell size; ban-list snapshot AC-X). [Forward-fill scope retained below for archaeology.] The single field that unlocks the Career-Climber value prop. Today CC users get generic "5 cards due" reminders identical to every other user; this row captures their target role (Staff / Senior Staff / Principal / EM) + target quarter + introduces aggregate peer-aspiration framing in the daily email ("engineers targeting Staff this quarter spend 40% of study time on system design"). NOT individual identification — aggregate-only framing per privacy-safe contract (Career-Climbers are senior engineers, some under NDAs, individual peer-comparison copy is legally fraught). **Scope:** **(a) Data model** — append-only history table `user_career_intents` (NOT a User column — supports intent history for the longitudinal narrative; one row per user per intent change, `superseded_at NULL` = current). Decision locked at spec-author per design review risk #3: append-only history shape from day one. Columns: `id`, `user_id` FK, `target_role` ENUM-as-String (`'staff' | 'senior_staff' | 'principal' | 'distinguished' | 'em' | 'sr_em' | 'director'`), `target_quarter` String (`'YYYY-Q[1-4]'` format, validated), `created_at`, `superseded_at` nullable. **(b) Capture surfaces** — extend `PersonaPicker.tsx` Career-Climber card with optional role + quarter fields; add Profile section "Career goal" with Edit affordance; both PATCH a new `POST /api/v1/users/me/career-intent` endpoint (NOT extending the buggy full-replace `PATCH /persona` — see B-038). **(c) Email pipeline** — extend daily-digest template (Phase 2.6 Resend) with conditional aggregate stats block when current `user_career_intents` row exists. Stats source: aggregate query over all current intents grouped by `(target_role, target_quarter)` with study-time distribution by deck topic. **(d) Privacy contract** locked at spec §12 — copy uses ONLY aggregate framing, NEVER names companies / individuals / peers. AC explicitly bans phrases like "your peers at $company" or "engineers from $company". **(e) Analytics** — 3 new events: `career_intent_captured {target_role, target_quarter, source: 'persona_picker' | 'profile_edit'}`, `career_intent_updated {from_role, to_role, ...}`, `career_intent_email_block_rendered {target_role, has_aggregate_data}`. **Dependencies:** existing email pipeline (Phase 2.6 ✅); existing PersonaPicker + Profile surfaces; B-038 (full-replace persona PATCH bug — DO NOT extend it; mint a dedicated endpoint per B-038 option (b)). **Risk surface — data privacy and copy:** aggregate framing is THE binding constraint. Spec §12 must lock 3 example copy variants and ban list of forbidden phrases. Implementation must include a copy-review test (snapshot test on rendered email block). **Spec-graph note:** new BE table + new endpoint + new email template variant + new FE form fields in two surfaces. Touches persona contract (spec #34) — spec must name what `interview_target_*` fields look like for CC (answer: NULL / unused). **Activation gate:** none for capture surface; aggregate stats block in email auto-activates when ≥10 users have intents in the same `(target_role, target_quarter)` bucket (privacy-safe minimum cell size — locked at spec §12). **Out of scope:** ML-driven personalization beyond aggregate buckets; LinkedIn integration; resume-derived target role inference; team_lead persona variants; in-app surfaces of the aggregate stats (the value is in email per Dhamo's "data gold" framing); changing existing daily-digest copy for users without intents. **Test envelope estimate:** ~+15 BE (model + 1 endpoint + email template + aggregate query + copy ban-list snapshot) + ~+10 FE (PersonaPicker extension + Profile section + analytics fires) + 1 alembic migration. R14 default — new feature + data model change + new endpoint, spec-first. R17 watermark verified at filing: E-052 claimed for this row, E-053 next-free. |
| E-053 | home | Career-Climber habit ribbon + skill-radar 30-day trend block on `/home` (LTV play, slower-burn persona, CC moat device) | P2 | 🔴 | | (from 2026-05-03 design-review session, ordered #5 by Dhamo "LTV play") | Recomposes existing `StreakWidget` + `SkillRadar` into a single `/home` hero block for `persona === 'career_climber'` — surfaces the *consistency × growth-over-time* contract that distinguishes CC from IP. **CC moat device** the same way the loop-progress strip is the IP moat device. Without it, CC users see a generic FSRS dashboard; with it, they see "14-day streak + RAG coverage up 18% this month" — the longitudinal story no point-tool offers. **Scope:** **(a) Habit ribbon** — 14-cell horizontal strip showing review density per day for the last 14 days (intensity quantized to 5 levels: 0 / 1-2 / 3-4 / 5-9 / 10+ reviews). Today's cell outlined for emphasis. Reads from existing `quiz_review_events` (Phase 6 slice 6.0 dual-write surface). **(b) Skill-radar trend** — extends existing `<SkillRadar>` component with a "30-day delta" overlay showing per-axis change. Reads same `quiz_review_events` aggregated by deck/category. NEW BE endpoint `GET /api/v1/progress/skill-trend?window=30d` returning per-axis-{current, baseline_30d_ago, delta} (NOT extending legacy `/progress/radar` — phase-5 legacy stays untouched per Phase 6 separation discipline). **(c) Composition** — new `<CareerClimberHeroBlock>` component mounted at top of `CareerClimberMode` JSX, ABOVE existing widget grid. Existing CC widgets (StreakWidget standalone, SkillRadar on Profile) STAY — this is a NEW surface, not a replacement. **(d) Click-through** — clicking the ribbon opens `/profile#study-history` (existing); clicking radar opens `/learn?focus=$weakest_axis`. **(e) Analytics** — 3 new events: `cc_hero_block_rendered {streak_days, weakest_axis, has_trend_data}`, `cc_hero_ribbon_clicked {clicked_day_offset, density_bucket}`, `cc_hero_radar_clicked {weakest_axis, delta}`. **Dependencies:** Phase 6 slice 6.0 ✅ (`quiz_review_events` table + dual-write); E-050 status hero ABOVE this block; E-052 (NOT a hard dependency — block renders without role intent, but radar weighting becomes role-aware only when E-052 ships). **Risk surface — Phase 5 / Phase 6 entanglement (design-review risk #5):** this row reads from a Phase 6 table (`quiz_review_events`). Spec must explicitly lock this as the data source AND document the read-only contract (no writes, no schema changes to the Phase 6 surface). Phase 6 slice 6.11 (FSRS retention dashboard, scout shipped at `09-fsrs-dashboard.md`) reads the same table — spec must name the relationship and confirm read-only co-existence. **Spec-graph note:** introduces a NEW composition surface on `HomeDashboard.tsx` (the SIXTH governing spec after #34 / #40 / #44 / #61 / E-050). Spec must name interactions with E-050 (renders ABOVE this block) and the existing CareerClimberMode widget grid (renders BELOW this block, no suppression). **Activation gate:** Phase 6 slice 6.10 ✅ (already shipped — `quiz_review_events` populated); CC user cohort ≥50 (slower-burn persona — habit visualization needs density to feel meaningful; with <50 CC users the ribbon is mostly empty cells and undermines the moat). **NOT a launch blocker** — IP-side work (E-049 / E-050 / E-051) ships first; this row picks up when CC cohort gate trips. **Out of scope:** mobile-specific layout (responsive breakpoint reuses existing); team_lead persona variant; ML-driven weak-axis prediction; any write to Phase 6 surfaces; replacing existing StreakWidget or SkillRadar on Profile (additive only). **Test envelope estimate:** ~+12 BE (new endpoint + aggregate query + window calculations + co-existence with Phase 6 slice 6.11 reads) + ~+15 FE (ribbon × 5 density buckets + radar trend overlay + click-through × 2 + analytics fires + persona-CC-only render gate). R14 default — new feature + new endpoint + new component, spec-first. R17 watermark verified at filing: E-053 claimed for this row, E-054 next-free. |
| E-046 | tracker | Real-time recruiter-call AI copilot | P3 | 🟦 | | (from 2026-04-23 product-scope conversation) | **DO-NOT-BUILD (permanent rejection).** Explicit rejection row exists so future sessions do not re-litigate this scope. **Rationale:** (a) TOS-violation surface on target employers' interview processes (recording / AI-listening on a recruiter call without explicit consent violates most major employers' interview conduct policies and some states' one-party-recording-law liability frameworks); (b) one-party-recording-law liability varies by jurisdiction (California / Illinois / Maryland + several others require two-party consent — a nationwide product cannot quietly assume one-party); (c) commoditized LLM-wrapper space — no moat extension from current study-loop differentiation; (d) support burden for the 5% of users who'd use it outweighs revenue upside at our scale. **No gate. No spec. No future unlock condition.** This row exists to be pointed at when the idea resurfaces. Close only if Dhamo explicitly flips the strategic lock — at which point the row must be REWRITTEN, not casually reopened. |

---

## Closed (most recent first)

| ID | Title | Closed by | Date | Notes |
| B-011 | Spec numbering on disk diverges from v2.1 doc | `9db9b9f` | 2026-05-03 | Closed `9db9b9f` 2026-05-03 by Mode-3 scout — cosmetic. Spec authors picked next-available numeric prefix at authoring time, not the v2.1 session number; on-disk wins per source-of-truth hierarchy #2. R14 exception (b). |
| B-012 | v2.1 doc-bug audit pending | `9db9b9f` | 2026-05-03 | Closed `9db9b9f` 2026-05-03 per verdict (b) — v2.1 is a historical planning artifact. CLAUDE.md + AGENTS.md have zero v2.1 references; live process docs are the source of truth. Periodic doc-audit pattern locked at `docs/specs/phase-5/48-doc-audit-pattern.md`; no v2.3 patch needed. R14 exception (b). |
| B-008 | `deprecated_route_hit` not wired in 10 Navigate redirect nodes | `49aee35` | 2026-05-03 | Closed `49aee35` 2026-05-03. New `src/components/DeprecatedRedirect.tsx` wraps `<Navigate replace />` and fires `deprecated_route_hit` once on mount (`{from_path, to_path}`); all 10 transitional redirects in `App.tsx` migrated; legacy `RedirectWithParam` helper deleted. Analytics catalog gains `deprecated_route_hit` row + the gap-flag paragraph removed. Tests +6 (FE 480 → 486); BE 858 carry-forward. JC: shipped `from_path/to_path` payload (mirrors `nav_clicked`) instead of spec #12's original `old_path/new_path` for catalog consistency. R14 exception (b); R15(c): single-slice close; R17: no new IDs. Two-commit pattern. |
| B-009 | AGENTS.md:95 contradicts CLAUDE.md Rule 13 | `0cb10ad` | 2026-05-03 | Closed `0cb10ad` 2026-05-03. Contradicting line had drifted from L95 to L120 ("Coverage target: 80%+") vs CLAUDE.md R13 ("Coverage tooling deliberately NOT installed"). Replaced AGENTS.md line with a pointer to R13. CLAUDE.md authoritative per source-of-truth hierarchy. R14 exception (b). |
| B-013 | Repo-root ambiguity in AGENTS.md + CLAUDE.md + prompt templates | `0cb10ad` | 2026-05-03 | Closed `0cb10ad` 2026-05-03. Added "## Repository layout" section to AGENTS.md above the directory diagram clarifying git root = `SkillForge/`, working dir = `SkillForge/hireportai/`, with cross-ref to CLAUDE.md C1 for the `git add -A` footgun. R14 exception (b). |
| B-006 | Existing registered users may lose settings when new fields are added | `c4132fb` | 2026-05-03 | Closed `c4132fb` 2026-05-03 by audit verdict — zero gaps. Mode-1 audit covered all 9 User columns + 4 per-user settings tables (EmailPreference, GamificationStats, Subscription, paywall_dismissals). NOT NULL columns ship with `server_default` or signup-payload values; nullable columns have explicit None-handling at every reader; settings tables have `_get_or_create` helpers or auto-create on first event/checkout. No fixes applied. Cascade-closes E-015 same commit. R14 exception (b); R15(c): single-slice close. |
| E-015 | Settings persistence audit + harden | `c4132fb` | 2026-05-03 | Cascade-closed by B-006 audit — zero gaps surfaced; nothing to harden. See B-006 close-line for the surveyed surface. |
| B-118 | AuthContext staleness fix — `refreshUser()` re-fetches `/auth/me` on post-Stripe redirects | `ace4584` | 2026-05-03 | Closed `ace4584` 2026-05-03. Closes 5 cancel-pending scout findings (#6/#8/#17/#18/#20). New `refreshUser()` in `AuthContext.tsx` re-calls `/auth/me` and updates context user; concurrent calls dedupe via `useRef<Promise>` inflight flag. Wired into `Pricing.tsx` (`?upgrade=success` post-checkout branch) + `Profile.tsx` mount (portal return_url). 4 existing useAuth() test mocks extended with `refreshUser: vi.fn()`. Tests +7 (FE 473 → 480); BE 858 carry-forward. UsageContext refresh deferred — separate blast radius. R14 exception (b); R15(c): single-slice file+close; R17: B-118 claimed, B-119 next-free. Two-commit pattern. |
| B-117 | Cancel-pending UI bundle — Profile Reactivate CTA + Pricing cancel-pending tile + Pricing Reactivate→portal redirect + Pro tile cursor scope | `2ae9a69` | 2026-05-03 | Closed `2ae9a69` 2026-05-03. Bundles 4 cancel-pending gaps from the 2026-05-03 Stripe scout (this session): scout-#7 cosmetic cursor (working-tree), scout-#14 /pricing cancel-pending invisibility, scout-#15 reactivation 409-trap, scout-#21 missing in-app reactivation surface. Profile gains "Reactivate Pro" button when `cancelAtPeriodEnd===true` (opens Stripe portal). Pricing Pro tile swaps "Current Plan" → "Cancels <date>" + button label "Reactivate"; `handleCta` opens portal instead of checkout in cancel-pending state, sidestepping B-113 AlreadyProError. New `subscription_portal_opened` event with `source: 'pricing_reactivate'`. Tests: FE 468 → 473 (+5); BE 858 carry-forward. AuthContext staleness cluster (#6/#8/#17/#18/#20) deferred — different blast radius. R14 exception (b); R15(c): single-slice file+close; R17: B-117 claimed, B-118 next-free. Two-commit pattern. |
| B-116 | F-2 + F-4 audit fix — `customer.subscription.updated` webhook handler + `current_period_end` populate (cancellation-pending state from billing portal) | `06ea5f6` | 2026-05-03 | Closed `06ea5f6` 2026-05-03. Implements audit-2026-05 F-2 + F-4 (P1) — F-3 deferred to follow-up slice (prompt-vs-audit Q3 ambiguity caught at Step 0; scope confirmed with Dhamo). New `_handle_subscription_updated` dispatcher reads `cancel_at_period_end` / `current_period_end` (unix→naive UTC) / `status` via B-114 `_field()` helper; plan stays `pro` until `customer.subscription.deleted` fires. Schema: new `Subscription.cancel_at_period_end Boolean NOT NULL` via alembic `c2ba25f1d9a7`. FE: `/auth/me` exposes the new flag; `AuthUser` gains optional `subscription`; Profile renders "Cancels <date>" via toLocaleDateString with "Active" fallback for back-compat. Tests +6 (BE 854 → 858; FE 466 → 468). D-027 BE failures + StudyGapsPromptWidget order-dependent flake + ScoreDeltaWidget tsc warn carry-forward as pre-existing. R14 exception (b); R15(c): single-slice file+close; R17: B-116 claimed, B-117 next-free. Two-commit pattern. Cross-ref E-036. |
| ↳ ··· | older closures (44 rows, 2026-04-18..2026-04-28) archived to [`docs/archive/backlog-closed.md`](docs/archive/backlog-closed.md) on 2026-05-01 by B-092 process compaction | — | — | Search archive for any pre-2026-04-29 closure. |
| ↳ ··· | 14 closures (2026-04-29..2026-05-01) archived to [`docs/archive/backlog-closed.md`](docs/archive/backlog-closed.md) on 2026-05-03 (Mode-1 light hygiene) | — | — | Search archive for any pre-2026-05-02 closure. |
| ↳ ··· | 8 closures (B-100, B-098, B-099, B-097, B-078, B-096, B-094b, B-094a — all 2026-05-02) archived to [`docs/archive/backlog-closed.md`](docs/archive/backlog-closed.md) on 2026-05-03 by B-121 light-hygiene compaction | — | — | Boundary by row name (B-100 inclusive and below), not date — see archive cut JC. |
| ↳ ··· | 136 closures (all dated 2026-05-02 or earlier — full B-001..B-115 + E-002..E-048 cluster + secondary-table short-forms) archived to [`docs/archive/backlog-closed.md`](docs/archive/backlog-closed.md) on 2026-05-04 by B-127 light-hygiene compaction | — | — | Bulk catch-up cut. Prior B-121 cut had left active-table duplicates of already-archived rows in place — this slice clears them. |

---

## Open decisions awaiting Dhamo

These block specific items. Mirrors the v2.2 patch list — keep both in sync.

| Decision | Affects | Default if not decided |
|----------|---------|------------------------|
| Persona switch UX: full-page reroute or modal? | E-003 (P5-S17) | Full-page reroute (current spec) |
| Daily review consumes 15-card free budget, or browse-only? | E-011, Phase 1 paywall logic | Browse-only (more generous) |
| Auto-save scan to tracker — automatic or "Save?" prompt? | E-025 (P5-S5 spec) | Automatic (current behavior) |
| Old Phase 2 email deep links — covered by P5-S13 redirects? | E-002 (P5-S13) | Yes, must be covered |

---

*Last updated: 2026-04-19. Initial seed from v2.1 Phase 5 status table + v2.2 patch + known bugs in memory.*
