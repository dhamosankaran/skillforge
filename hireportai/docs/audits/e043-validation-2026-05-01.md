# E-043 closed-loop validation probe — 2026-05-01

**Mode:** validation-probe (not Mode 3 strictly — this slice exercises the shipped
E-043 closed-loop at `4aab0bf` against the local stack and reports findings).

**Target:** `POST /api/v1/analyze/rescan` + `GET /api/v1/tracker/{id}/scores` +
`HomeScoreDeltaWidget` composition + `/api/v1/home/state` next_interview envelope
shipped at `4aab0bf` (E-043 / B-086 / B-086b cascade close).

**Authoritative spec:** `docs/specs/phase-5/63-ats-rescan-loop.md` (read in full —
906 lines, 17 ACs, §12 D-1..D-12 locks).

**Source-tree changes by this slice:** 1 NEW (this report) + 1 MODIFIED
(`SESSION-STATE.md` Recently Completed entry + Session Header HEAD). No production
code touched. No BACKLOG row filings (default per prompt — chat-Claude triages
findings into rows post-report).

---

## §0 — Spin-up output

Brew-native local-dev stack (chat-Claude's earlier docker assumption corrected on
re-issue; canonical per CLAUDE.md Quick Reference).

| Step | Command | Result |
|---|---|---|
| 1 | `lsof -nP -iTCP:5432 -sTCP:LISTEN` | postgres PID 830, `[::1]:5432` + `127.0.0.1:5432` ✓ |
| 1 | `psql -U hireport -d hireport -c '\dt'` | OK — `tracker_applications_v2` + `tracker_application_scores` + 30+ tables ✓ |
| 2 | `lsof -nP -iTCP:6379 -sTCP:LISTEN` (initial) | empty — Redis not running |
| 2 | `brew services start redis` | `Successfully started redis (label: homebrew.mxcl.redis)` |
| 2 | `redis-cli ping` | `PONG` ✓ |
| 3 | `alembic current` | `e043a1b2c3d4 (head)` ✓ matches E-043 foundation expected head |
| 4 | `uvicorn app.main:app --reload --port 8000` (then re-launched via `/tmp/e043_launch_be.py` for analytics monkey-patch — see Section A.3) | `Application startup complete` ✓ |
| 4 | `curl -s localhost:8000/health` | `{"status":"healthy","service":"hireport-ai"}` ✓ |
| 5 | `npm run dev -- --port 5199` | `VITE v5.4.21 ready in 483 ms; ➜ Local: http://localhost:5199/` ✓ |
| 5 | `curl -sI localhost:5199` | `HTTP/1.1 200 OK; Vary: Origin; Content-Type: text/html; ...` ✓ |

All spin-up steps cleared. No JCs.

**Baselines (SOP-3, R14 exception (b) — process slice with no test surface; report and continue per SOP-3 read-only/docs rule):**
- BE: 757 collected (prompt expected 752; +5 baseline drift since slice 6.13 spec-author final report — chat-Claude acknowledged in re-issue).
- FE: 445 passed / 77 files ✓ matches expected.

---

## §A — Test user provisioning

### A.1 — JWT-mint authorization (chat-Claude explicit, slice-scoped)

No `/auth/dev-token` route exists on disk. For this validation-probe slice
ONLY, chat-Claude explicitly authorized minting JWTs via:

```
python -c "from app.core.security import create_access_token; \
           print(create_access_token({'sub': '<user-id>', 'email': '<email>'}))"
```

This bypasses the Google-OAuth + IP-rate-limit + admin-reconciliation paths the
real `POST /auth/google` runs. The probe is testing `/rescan` behavior, not the
auth surface; the bypass is in-scope for this slice only. Authorization does NOT
extend beyond this slice. No `POST /auth/dev-token` route was introduced;
`auth.py` and `security.py` were not modified.

### A.2 — User + subscription provisioning (idempotent)

```sql
-- Pro user (P1 + P2 + P3 + P5 + setup B; subscription bypasses paywall)
INSERT INTO users (id, google_id, email, name, role, onboarding_completed)
VALUES ('e0430000-0001-0000-0000-000000000001', 'gid-e043-pro',
        'e043-probe@local.test', 'E-043 Probe Pro', 'user', true)
ON CONFLICT (id) DO NOTHING;

INSERT INTO subscriptions (id, user_id, plan, status)
VALUES ('e0430000-1001-0000-0000-000000000001',
        'e0430000-0001-0000-0000-000000000001', 'pro', 'active')
ON CONFLICT (id) DO NOTHING;

-- Cap user (P4 paywall test only; no subscription row → defaults to free)
INSERT INTO users (id, google_id, email, name, role, onboarding_completed)
VALUES ('e0430000-0002-0000-0000-000000000002', 'gid-e043-cap',
        'e043-probe-cap@local.test', 'E-043 Probe Cap', 'user', true)
ON CONFLICT (id) DO NOTHING;
```

Verification (`SELECT u.id, u.email, u.role, COALESCE(s.plan,'free'), COALESCE(s.status,'-') FROM users u LEFT JOIN subscriptions s ON s.user_id=u.id WHERE u.email LIKE 'e043-probe%@local.test'`):

| user_id | email | role | plan | status |
|---|---|---|---|---|
| `e0430000-0001-...0001` | e043-probe@local.test | user | **pro** | active |
| `e0430000-0002-...0002` | e043-probe-cap@local.test | user | **free** | - |

JWTs minted (redacted to alg.payload.<sig-truncated>):
- `PRO_JWT = eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.{sub:e0430000-0001-...}.lLLs...nKE`
- `CAP_JWT = eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.{sub:e0430000-0002-...}.PHo7...CdY`

Round-trip via `GET /api/v1/auth/me` confirmed both JWTs decode through
`get_current_user` and resolve to the correct subscription (`pro/active` for
PRO_JWT, `free/active` for CAP_JWT) ✓.

### A.3 — PostHog event capture mechanism

`POSTHOG_API_KEY` is set in `.env`; events route to PostHog cloud, not BE logs.
To make event payloads inspectable at runtime without modifying any project file,
BE was relaunched via `/tmp/e043_launch_be.py` — a launcher script that imports
`app.core.analytics`, monkey-patches `analytics.track` to also emit a
`PROBE_EVENT uid=... event=... props=...` log line, then runs `uvicorn`. Zero
source change to `app/core/analytics.py`. All event captures cited below are
verbatim from `/tmp/be-e043-probe.log` `PROBE_EVENT` lines.

The patched track wrapper still calls the original `analytics.track`, so events
also reached PostHog cloud — visible side effect for chat-Claude to triage out
of the probe analytics if undesirable.

---

## §B — Tracker row + prior scan setup

### B.1 — Pro user `/analyze` (lands tracker row + scan #1)

```
POST /api/analyze   (multipart: resume_file=/tmp/e043-resume-v1.docx + job_description=<JD>)
Authorization: Bearer <PRO_JWT>
```

Response 200 (15.2s — cold LLM call):
- `scan_id = 39c715ed-d63a-4fb2-a771-431ef525ff3a`
- `ats_score = 64`, `grade = D`
- `score_breakdown = {keyword_match: 42.0, skills_coverage: 86.0, formatting_compliance: 96.0, bullet_strength: 46.0}`
- `matched_keywords[:8] = ['backend', 'backend engineer', 'engineer', 'migrations', 'python', 'senior', 'senior backend', 'skills']`
- `missing_keywords[:8] = ['stripe', 'payments', 'platform', 'design', 'payments platform', 'systems', 'api', 'api design']`

DB delta (auto-created tracker row):

| tracker_id | user_id | company | role | ats_score | scan_id | jd_text_set | jd_text_len | jd_hash[:8] |
|---|---|---|---|---|---|---|---|---|
| `a7c92c40-e62e-4321-b20e-7d9834e680a6` | `e0430000-0001-...` | Unknown Company | Senior Backend Engineer — Stripe Payments Platform | 64 | 39c715ed | t | 1050 | c89af151 |

`tracker_application_scores` count for this tracker after `/analyze`: **0**.
This confirms the prompt's open question — **B-086a foundation does NOT
auto-write a baseline scores row; only `/rescan` writes to history** (B-086b
orchestrator territory). Implication: first `/rescan` will land
`history.length = 1`, not 2; delta will be `null` per spec §4.2 step 11.
This propagates as IFD-5 below.

### B.2 — Test resumes + JD payload

Generated three artifacts via Python at `/tmp/`:

| file | size | role |
|---|---|---|
| `/tmp/e043-jd.txt` | 1051 chars | Stripe Payments backend JD |
| `/tmp/e043-resume-v1.docx` | 37252 bytes | Baseline resume (Python/FastAPI/PostgreSQL — missing Stripe/Redis/Kafka) |
| `/tmp/e043-resume-v2.docx` | 37514 bytes | v1 + RECENT PROJECTS section adding Stripe webhook integration, Redis, Kafka, K8s |
| `/tmp/e043-resume-v1.txt` | 1120 chars | Plain-text v1 (not used; v1 went via .docx upload) |
| `/tmp/e043-resume-v2.txt` | 1599 chars | Plain-text v2 — used as `resume_text` body for `/rescan` POST |

---

## §P1 — Rescan happy path (AC-4 + AC-9 + AC-10 + AC-11 + corrected per re-issue)

**Request:**
```
POST /api/v1/analyze/rescan
Authorization: Bearer <PRO_JWT>
Content-Type: application/json
{ "tracker_application_id": "a7c92c40-e62e-4321-b20e-7d9834e680a6",
  "resume_text": "<resume v2 plain text, 1599 chars>" }
```

**Response 200 (3.7s):** keys = `['ats_score', 'bullet_analysis', 'formatting_issues', 'grade', 'job_fit_explanation', 'keyword_chart_data', 'matched_keywords', 'missing_keywords', 'resume_text', 'scan_id', 'score_breakdown', 'skill_gaps', 'skills_overlap_data', 'top_gaps', 'top_strengths']` — full `AnalysisResponse` shape per spec §5.2 + §6.2 (15 fields). Matches re-issued IFD-2 wording (NOT `ScoreDeltaResponse` — that schema does not exist on disk).

- `scan_id = 64ffc773-11c3-4d7a-a955-bf8d99319d2a` (NEW; per spec §1.1 #12 + AC-9 the tracker row's scan_id stays the canonical anchor)
- `ats_score = 67` (was 64; +3 net)
- `score_breakdown = {keyword_match: 48.0 (+6), skills_coverage: 100.0 (+14), formatting_compliance: 69.0 (-27), bullet_strength: 60.0 (+14)}` — note formatting_compliance dropped because the longer doc tripped formatting heuristics
- `matched_keywords[:8] = ['stripe', 'backend', 'payments', 'backend engineer', ...]` — Stripe/payments now present per v2 additions

**DB delta:**

| | tracker.ats_score | tracker.scan_id | scores rows |
|---|---|---|---|
| Pre-P1 | 64 | 39c715ed-... | 0 |
| Post-P1 | **67** ✓ AC-4 | 39c715ed-... ✓ AC-9 (UNCHANGED) | **1** ✓ AC-4/AC-16 |

Score row landed:

| id | scan_id | overall | km | sc | fc | bs | jd_hash[:8] | resume_hash[:8] | scanned_at |
|---|---|---|---|---|---|---|---|---|---|
| `ebb7f0ab-c624-...` | `64ffc773-...` | 67 | 48 | 100 | 69 | 60 | c89af151 | 0e4662ff | 2026-05-01 16:24:44.648 |

All 4 axis fields non-null ✓ AC-16. jd_hash matches the tracker row's jd_hash (c89af151) — the dedupe key is anchored.

**`GET /api/v1/tracker/{id}/scores` (AC-10):**
```json
{
  "tracker_application_id": "a7c92c40-e62e-4321-b20e-7d9834e680a6",
  "history": [{"id":"ebb7f0ab-c624-...","scan_id":"64ffc773-...","overall_score":67,
               "keyword_match_score":48.0,"skills_coverage_score":100.0,
               "formatting_compliance_score":69.0,"bullet_strength_score":60.0,
               "scanned_at":"2026-05-01T21:24:44.648701Z"}],
  "delta": null
}
```
✓ HTTP 200, ✓ chronological history (1 entry), ✓ `delta: null` per spec §4.2 step 11 / §6.4 (len < 2 → no delta).

**PostHog events fired (verbatim from monkey-patched track):**
1. `rescan_initiated` — props `{tracker_application_id: a7c92c40-...}`
2. `ats_scanned` — props `{score: 67, grade: D, gaps_found: 0, matched_keywords: 19, missing_keywords: 21}` ← see IFD-6
3. `rescan_completed` — props `{tracker_application_id, scan_id, jd_hash, ats_score_before: None, ats_score_after: 67, ats_score_delta: None, keyword_match_delta: None, skills_coverage_delta: None, formatting_compliance_delta: None, bullet_strength_delta: None, short_circuited: False}` ← all delta fields `None` per IFD-5

**Findings table:**

| AC ref | expected | observed | status |
|---|---|---|---|
| AC-4 | 200 + AnalysisResponse + 1 score row INSERT + tracker.ats_score UPDATE | 200 + 15-key AnalysisResponse + 1 row in `tracker_application_scores` + tracker.ats_score 64→67 | ✅ |
| AC-9 | tracker.scan_id UNCHANGED across rescan | scan_id stayed `39c715ed-...` (P1 minted new `64ffc773-...` for the score row, tracker row not touched) | ✅ |
| AC-10 | GET /scores → 200 + chronological history + delta (null if len<2) | 200 + 1-entry history + `delta: null` | ✅ |
| AC-11 | rescan_completed payload includes per-axis delta envelope per §12 D-12 | All 5 delta fields present + `short_circuited: False` ✓; values are all `None` because history.length=1 (IFD-5) | ✅ shape / 🟡 values degenerate |
| (corrected) | per-axis delta envelope on POST /rescan response | NOT present on POST /rescan body — lives on GET /scores `ScoreHistoryResponse.delta` per spec §5.2 + §6.4 | ✅ matches IFD-2 corrected expectation |
| AC-16 | per-axis assertion: all 4 axis fields land non-null | km=48, sc=100, fc=69, bs=60 — all non-null ✓ | ✅ |

---

## §P2 — Short-circuit (AC-5 + D-2)

**Request:** identical to P1 (`tracker_application_id` + same `resume_text` v2).

**Response 200 (14ms — 263× faster than P1's 3.7s LLM call; proves no LLM call):**

```json
{"scan_id":"64ffc773-11c3-4d7a-a955-bf8d99319d2a","ats_score":67,"grade":"",
 "score_breakdown":{"keyword_match":48.0,"skills_coverage":100.0,"formatting_compliance":69.0,"bullet_strength":60.0},
 "matched_keywords":[],"missing_keywords":[],"skill_gaps":[],"bullet_analysis":[],
 "formatting_issues":[],"job_fit_explanation":"","top_strengths":[],"top_gaps":[],
 "keyword_chart_data":[],"skills_overlap_data":[],
 "resume_text":"<v2 resume verbatim, ~1600 chars echoed back>"}
```

This is the IFD-3 stub: scan_id + ats_score + breakdown populated from the cached
`tracker_application_scores` row; grade='', all keyword/skill/bullet/formatting
arrays empty, job_fit_explanation='', resume_text echoed back from request body.

**DB delta:** scores rows count UNCHANGED (1→1) ✓; tracker.ats_score UNCHANGED (still 67) ✓.

**Counter check:** Pro user has 0 rows in `usage_logs.feature_used='analyze'`
because `check_and_increment` short-circuits write at line 175 of
`usage_service.py` for Pro/Enterprise/Admin. The route also returns at line 274
of `analyze.py` BEFORE reaching the counter check at line 276 — so even a free
user wouldn't increment on a short-circuit hit (D-2 contract holds for free
tier too).

**PostHog events fired:**
1. `rescan_short_circuited` — props `{tracker_application_id: a7c92c40-..., jd_hash_prefix: 'c89af151'}` ✓
2. `rescan_initiated` — NOT fired (route returns at line 274 before line 291) ✓ correct
3. `rescan_completed` — NOT fired (route returns before line 341) ✓ correct

| AC ref | expected | observed | status |
|---|---|---|---|
| AC-5 | 2nd POST same (jd_hash, resume_hash) returns existing scores; no LLM call | 200 in 14ms, returns cached score row's data; 263× faster than LLM call | ✅ |
| D-2 | counter NOT consumed on short-circuit | 0 usage_log rows for Pro user (also Pro short-circuits in `check_and_increment` regardless); route returns before counter check | ✅ |
| analytics | `rescan_short_circuited` fires with `jd_hash_prefix` | Fired with `jd_hash_prefix: 'c89af151'` (8-char prefix per spec §9 catalog) | ✅ |
| (IFD-3) | capture short-circuit response body verbatim | See IFD section below | 🟡 see IFD-3 |

---

## §P3 — NULL jd_text 422 (AC-3 + D-9)

**Setup:**
```sql
INSERT INTO tracker_applications_v2 (id, user_id, company, role, date_applied, ats_score, status, jd_text, jd_hash)
VALUES ('e0430003-3333-3333-3333-000000000003', 'e0430000-0001-...0001',
        'P3 Test Co', 'P3 Test Role', '2026-05-01', 50, 'Applied',
        'Original JD', 'fakehash')
ON CONFLICT (id) DO NOTHING;
UPDATE tracker_applications_v2 SET jd_text = NULL, jd_hash = NULL
WHERE id = 'e0430003-3333-3333-3333-000000000003';
```
Verified: `jd_text IS NULL → t`.

**Request:**
```
POST /api/v1/analyze/rescan
Authorization: Bearer <PRO_JWT>
{ "tracker_application_id": "e0430003-3333-3333-3333-000000000003",
  "resume_text": "<200-char minimum-length resume body>" }
```

**Response 422 verbatim:**
```json
{"detail":{"error":"jd_text_missing",
           "message":"JD text not stored on this tracker — please run a fresh scan to populate."}}
```

Wording matches spec §11 AC-3 + §6.2 sketch verbatim. (Spec §6.2 sketch shows
"JD not stored on this application — re-scan unavailable. Please run a fresh
scan." — slightly different wording; AC-3 + the shipped impl agree on the
"JD text not stored on this tracker — please run a fresh scan to populate."
form. Spec §6.2 sketch text is stale relative to AC-3 / shipped impl. Minor —
captured for chat-Claude to reconcile in the spec body if desired.)

**PostHog events fired:** none. The route raises HTTPException at line 224
before reaching the analytics_track call sites. **No `rescan_failed` event
with `error_class: 'jd_missing'`** despite spec §9 listing 'jd_missing' in the
enum — IFD-1 sibling pattern. See IFD section.

| AC ref | expected | observed | status |
|---|---|---|---|
| AC-3 | 422 with explicit copy on jd_text=NULL | 422 + `{error: 'jd_text_missing', message: '...please run a fresh scan to populate.'}` | ✅ |
| D-9 | explicit FE-facing copy | Verbatim wording matches AC-3 | ✅ |
| analytics | (per spec §9 enum) `rescan_failed{error_class:'jd_missing'}` | NOT fired (sibling of IFD-1) | 🔴 |

**Bonus capture:** before the proper-length UUID worked, an earlier attempt with
a 39-char ID failed the String(36) constraint, the row never landed, and `/rescan`
returned `{"detail":{"error":"tracker_not_found"}}` with HTTP 404 — incidental
confirmation of AC-2 envelope shape.

---

## §P4 — Free-tier 402 at cap (AC-6 + D-11)

**Step 1 (consume 1/1):**

`POST /api/analyze` with CAP_JWT + v1 resume + JD → 200 + `scan_id =
a549f081-8fc0-4a39-a1be-e515b84a3899`, `ats_score=64`. `usage_logs` for cap
user: 0 → 1. Tracker row auto-created: `9595261a-7492-416f-9272-59cd30e22a01`
with jd_text + jd_hash populated (`/analyze` post-E-043 always populates).

PostHog events fired by `/analyze`:
- `ats_scanned` — `{score: 64, grade: D, gaps_found: 2, matched_keywords: 17, missing_keywords: 23}`
- `tracker_auto_created_from_scan` — `{ats_score: 64, gaps_count: 2}`

**Step 2 (cap fires):**

```
POST /api/v1/analyze/rescan
Authorization: Bearer <CAP_JWT>
{ "tracker_application_id": "9595261a-7492-416f-9272-59cd30e22a01",
  "resume_text": "<v2 resume>" }
```

**Response 402 verbatim:**
```json
{"detail":{"error":"free_tier_limit","trigger":"scan_limit",
           "scans_used":1,"scans_limit":1,"plan":"free"}}
```

✓ HTTP 402 (NOT 403). ✓ `trigger: 'scan_limit'` reused per spec §12 D-11.
Envelope shape matches `/analyze`'s 402 envelope at `analyze.py:71` exactly —
G-7 paywall reuse contract holds.

**PostHog events fired by P4 step 2:** **none**. The route raises HTTPException
at line 280 before reaching the `rescan_initiated` call site at line 291.
**No `rescan_failed{error_class:'paywall'}` event fired despite spec §9 listing
'paywall' in the enum.** IFD-1 CONFIRMED at runtime ✓.

| AC ref | expected | observed | status |
|---|---|---|---|
| AC-6 | 402 (NOT 403) with `trigger: 'scan_limit'` | 402 + `{error:'free_tier_limit', trigger:'scan_limit', scans_used:1, scans_limit:1, plan:'free'}` | ✅ |
| D-11 | reuse `'scan_limit'` PaywallTrigger | Same envelope shape as `/analyze` 402 | ✅ |
| (IFD-1) | `rescan_failed{error_class:'paywall'}` per spec §9 enum | NOT fired — route returns before analytics call site | 🔴 IFD-1 confirmed |

---

## §P5 — E-042 next_interview integration

**Setup:** `UPDATE tracker_applications_v2 SET interview_date = '2026-05-15' WHERE id = 'a7c92c40-...'`.

**Route discovery:** chat-Claude prompt said `GET /api/v1/home-state`; actual
route is `GET /api/v1/home/state` (slash, not hyphen) per `home.py:15`. Minor
prompt-drafting error; captured here.

**Request:**
```
GET /api/v1/home/state
Authorization: Bearer <PRO_JWT>
```

**Response 200 (relevant excerpt):**
```json
{ "persona": ..., "states": ..., "context": {
    "active_mission_id": ..., "current_streak": ..., "last_activity_at": ...,
    "last_review_at": ..., "last_scan_date": ..., "mission_target_date": ...,
    "plan": "pro",
    "next_interview": { "date": "2026-05-15", "company": "Unknown Company",
                        "tracker_id": "a7c92c40-e62e-4321-b20e-7d9834e680a6" }
}}
```

| AC ref | expected | observed | status |
|---|---|---|---|
| P5 | next_interview envelope contains tracker_id | `{date, company, tracker_id}` shape ✓; tracker_id matches the P1 tracker row | ✅ |
| (G-5 audit #13) | tracker_id available for HomeScoreDeltaWidget routing | Plumbed end-to-end | ✅ |

---

## §P6 — HomeDashboard composition (inspection-only)

**`src/pages/HomeDashboard.tsx`:**
- Line 11: `import { CountdownWidget } from '@/components/home/widgets/CountdownWidget'`
- Line 12: `import { HomeScoreDeltaWidget } from '@/components/home/widgets/HomeScoreDeltaWidget'`
- Line 155: `homeState.data?.context.next_interview ?? null` (assigned to local `nextInterview`)
- Line 223: `{user.persona === 'interview_prepper' && (...)}` — persona gate ✓ spec §8.2
- Lines 46–58 inside that block:
  - Line 47: `<CountdownWidget ... />`
  - Lines 52–53: comment `Spec #63 (E-043) §8.7 — D-5 mount: same tracker_id as CountdownWidget; self-suppresses when history.length < 2.`
  - Line 54: `<HomeScoreDeltaWidget ... trackerId={nextInterview?.tracker_id ?? null} ... />` — mounts immediately below `CountdownWidget` ✓ D-5

**`src/components/home/widgets/HomeScoreDeltaWidget.tsx`:**
- Line 35: `const { data, isLoading } = useScoreHistory(trackerId ?? null)`
- Line 37: `if (trackerId == null) return null`
- Line 38: `if (isLoading) return null`
- Line 42: `if (history.length < 2 || !delta) return null` ✓ spec §8.2 cold-start hide
- Lines 55-91: `<DashboardWidget ... action={{ label: 'View detail', href: '/prep/tracker?focus=...' }}>` — uses design tokens (text-success / text-danger / text-text-muted / text-text-secondary) ✓ R12

| AC ref | expected (spec §8.2 + D-5) | inspection finding | status |
|---|---|---|---|
| AC-14 | mounts in interview_prepper variant only | `user.persona === 'interview_prepper'` gate at line 223 ✓ | ✅ inspection |
| AC-14 | mounts immediately below CountdownWidget | line 47 (Countdown) + line 54 (HomeScoreDelta), same JSX block | ✅ inspection |
| AC-14 | cold-start hide when history.length < 2 | `if (history.length < 2 \|\| !delta) return null` line 42 | ✅ inspection |
| AC-14 | self-suppresses on null trackerId | `if (trackerId == null) return null` line 37 | ✅ inspection |
| D-5 | same tracker_id as CountdownWidget | both receive `nextInterview?.tracker_id` from line 155 | ✅ inspection |

⚠️ **Visual confirmation REQUIRED.** Inspection cannot verify whether the widget
actually renders correctly when conditions are met. A human walk-through —
log in as the Pro probe user, set up a 2-row score history (run `/rescan` a
second time with a v3 resume), set persona='interview_prepper', visit
`/home` — is needed to close AC-14. Note from IFD-5 that the cold-start
hide will fire indefinitely for users who only `/analyze` once + `/rescan`
once — they need TWO `/rescan` calls before this widget surfaces.

---

## §X — Summary

| status | count | items |
|---|---|---|
| ✅ pass | 16 ACs / sub-checks | AC-3, AC-4, AC-5, AC-6, AC-9, AC-10, AC-11 (shape), AC-14 (inspection 5×), AC-16, D-2, D-5, D-9, D-11, P5, IFD-2 (corrected expectation matched) |
| 🔴 finding | 2 | IFD-1 (rescan_failed not fired on 402 paywall — spec §9 enum mismatch); IFD-1 sibling on 422 jd_missing path |
| 🟡 capture-and-triage | 4 | IFD-3 (short-circuit stub shape); IFD-4 (jd_hash vs jd_hash_prefix in rescan_completed payload); IFD-5 (first-rescan delta gap → home widget UX implication); IFD-6 (`ats_scanned` fires from helper, not in spec §9 catalog) |
| ❓ requires manual confirmation | 1 | AC-14 visual render in browser |

**Recommended BACKLOG row triage candidates (do NOT file in this slice; chat-Claude
triages from this list):**

1. 🔴 **IFD-1** (sibling pattern across 422+402+404 paths): wire
   `rescan_failed` event fires for the non-2xx error paths so spec §9 enum
   aligns with impl. Smallest possible fix: add `analytics_track(...,
   "rescan_failed", {tracker_application_id, error_class: 'paywall'|'jd_missing'|'not_found'})`
   immediately before each HTTPException raise in `analyze.py::rescan_application`.
   Trade-off accepted: paywall analytics dimensioning loss surfaced in spec D-11
   ("rescan_initiated event upstream still preserves the dimension") was an
   intentional acceptance, but `rescan_initiated` doesn't fire on the 402 path
   either (route returns before line 291). So the dimension is fully lost.
2. 🔴 **IFD-5** (UX gap): `/analyze` does not backfill a baseline row in
   `tracker_application_scores`, so HomeScoreDeltaWidget hides indefinitely
   for the canonical scan-then-rescan-once flow. Spec §6.3 hints at "(optionally)
   from fresh-scan route to backfill the first history entry" — never wired.
   Smallest fix: in `analyze.py::analyze_resume` after the tracker auto-create
   block, `await tracker_application_score_service.write_score_row(...)` for the
   newly-created tracker row's first scan. Effect: first `/rescan` then has
   `history.length == 2`, delta becomes non-null, widget renders.
3. 🟡 **IFD-3**: short-circuit response stub fields (grade='', empty arrays).
   Either intentional (caller doesn't need parse details for cached data — saves
   memory + LLM-derived narrative is stale anyway) or sloppy. Possible fixes:
   (a) cache a slim response payload alongside the score row and return it
   here, or (b) document the stub behavior in spec §4.2 step 4 explicitly so
   future devs aren't surprised.
4. 🟡 **IFD-4**: spec §9 catalog says `rescan_completed` payload includes
   `jd_hash_prefix`; impl emits full `jd_hash` (64 chars). Pick one and
   reconcile — full hash is more useful for analytics joins; prefix is more
   privacy-friendly. The `rescan_short_circuited` event correctly emits
   `jd_hash_prefix` per catalog.
5. 🟡 **IFD-6**: `ats_scanned` event fires from inside
   `analysis_service.score_resume_against_jd` (between `rescan_initiated` and
   `rescan_completed`). Carryover from old in-route handler. Either add a row
   to `.agent/skills/analytics.md` catalog OR suppress the helper-side fire
   when called from `/rescan` (caller passes a flag).

**Probe artifacts left in DB (kept for re-runs per cleanup directive):**

| kind | id | notes |
|---|---|---|
| user | `e0430000-0001-...0001` | Pro probe user |
| user | `e0430000-0002-...0002` | Cap probe user (free tier) |
| subscription | `e0430000-1001-...0001` | Pro plan, active, no Stripe IDs |
| tracker | `a7c92c40-e62e-...e680a6` | P1+P2+P5 — Pro user, ats_score=67, jd_text+jd_hash populated, interview_date=2026-05-15 |
| tracker | `e0430003-3333-...0003` | P3 — Pro user, jd_text=NULL forced (kept) |
| tracker | `9595261a-7492-...2a01` | P4 — Cap user, jd_text+jd_hash populated |
| score row | `ebb7f0ab-c624-...3ff03` | P1 happy path, overall=67 |
| usage_log | (1 row) | Cap user, feature_used='analyze' — at the cap |

PostHog cloud received the probe events (the patched `track` wrapper still
called the original `analytics.track`). If chat-Claude wants probe events
filtered out of analytics, distinguish by `distinct_id ∈ {e0430000-0001-...0001,
e0430000-0002-...0002}`.

---

## §IFD — Inspection-found divergences (consolidated)

| ID | spec ref | inspection finding | runtime confirmation | recommended disposition |
|---|---|---|---|---|
| **IFD-1** | spec §9 lists `rescan_failed.error_class ∈ {'scoring_error', 'jd_missing', 'auth', 'paywall', 'not_found'}` | route only fires `rescan_failed{scoring_error}` on 502 LLM-failure path | P3 (422) + P4 (402) confirmed: NO `rescan_failed` event in PROBE_EVENT log | 🔴 BACKLOG candidate — wire fires for non-2xx paths OR shrink the spec §9 enum to just `'scoring_error'` |
| **IFD-2** | prompt P1 wording ("response body shape matches `ScoreDeltaResponse`") was a chat-Claude drafting error | spec §6.2 + shipped route both return `AnalysisResponse`; no `ScoreDeltaResponse` schema on disk | P1 response confirmed 15-key `AnalysisResponse` | ✅ resolved — chat-Claude updated P1 wording on re-issue; no spec/impl gap |
| **IFD-3** | spec §4.2 step 4 says "fire `rescan_short_circuited` … return existing scores envelope without re-running the LLM pipeline" — doesn't specify shape | shipped impl returns AnalysisResponse with scan_id+ats_score+breakdown populated; grade='', all keyword/skill/bullet/formatting arrays empty, job_fit_explanation='', resume_text echoed back | P2 captured verbatim — see §P2 above | 🟡 chat-Claude triage — intentional shortcut (cached parse-details would be stale) vs ad-hoc; if intentional, document in spec §4.2 step 4 |
| **IFD-4** | spec §9 catalog says `rescan_completed` payload uses `jd_hash_prefix`; spec §12 D-12 says full `jd_hash` | impl emits full `jd_hash` (64 chars) on `rescan_completed`; `jd_hash_prefix` (8 chars) on `rescan_short_circuited` | P1 + P2 events confirm | 🟡 minor — pick one (full vs prefix) and reconcile spec §9 with §12 D-12 |
| **IFD-5** | spec §6.3 hints at "(optionally) from fresh-scan route to backfill the first history entry"; not implemented | first `/rescan` after `/analyze` has `history.length=1`, all delta fields=None despite `tracker.ats_score=64` baseline existing | P1 confirmed: `ats_score_before=None, ats_score_delta=None, *_delta=None` despite v1→v2 yielding 64→67 | 🔴 BACKLOG candidate — `/analyze` should backfill score row so first `/rescan` already has delta + HomeScoreDeltaWidget renders without needing 2nd rescan |
| **IFD-6** | spec §9 catalog lists `rescan_initiated`, `rescan_completed`, `rescan_failed`, `rescan_short_circuited` as the new events; `ats_scanned` not listed | `analysis_service.score_resume_against_jd` fires `ats_scanned{score, grade, gaps_found, matched_keywords, missing_keywords}` on every scoring call (carryover from old in-route handler) | P1 + Pro `/analyze` + Cap `/analyze` all show `ats_scanned` between `rescan_initiated` and `rescan_completed` | 🟡 minor — add catalog row to `.agent/skills/analytics.md` OR suppress when caller is `/rescan` |

---

## Footer

- **HEAD at slice start:** `9f48cfd` (slice 6.13 spec-author SHA backfill).
- **HEAD at slice end:** `<this-slice>` (replaced in commit-2 SHA backfill).
- **Slice commit (validation report + SESSION-STATE entry):** `<this-slice>`
- **SHA-backfill commit:** `<this-slice-backfill>`
- **Env vars in effect (redacted):**
  - `DATABASE_URL=postgresql+asyncpg://hireport:dev_password@localhost:5432/hireport`
  - `JWT_SECRET_KEY=<set>`, `JWT_ALGORITHM=<set>`
  - `GEMINI_API_KEY=<set>` (real LLM calls fired during P1 + setup-B + P4 step 1)
  - `RESEND_API_KEY=<set>` (no email sends during this probe)
  - `POSTHOG_API_KEY=<set>`, `POSTHOG_HOST=<set>` (events routed to cloud + monkey-patched local log)
  - `FREE_LIFETIME_SCAN_LIMIT=1` (default — NOT bumped per chat-Claude directive)
  - `FREE_DAILY_REVIEW_LIMIT`, `FREE_MONTHLY_INTERVIEW_LIMIT` — defaults (not exercised)
- **Spec read:** `docs/specs/phase-5/63-ats-rescan-loop.md` (906 lines, full).
- **Skills loaded (SOP-4):** none read in full this slice — picked candidates
  (`backend.md`, `analytics.md`, `database-schema.md`, `payments.md`, `home.md`,
  `ats-scanner.md`) but verification was driven by reading the shipped impl
  source directly (`analyze.py`, `auth.py`, `security.py`, `usage_service.py`,
  `analytics.py`, `HomeDashboard.tsx`, `HomeScoreDeltaWidget.tsx`) — process
  slice on a feature already shipped + spec already read in full. **No
  skill-inventory gap flagged.**
- **R-rule applicability:** R14 exception (b) — process / validation slice, no
  test surface, no spec authoring. R15(c) — no BACKLOG flips this slice
  (findings forwarded to chat-Claude triage). R17 — B-088 next-free pre-slice;
  no IDs claimed; B-088 next-free post-slice. N8 working tree allowlist only.
  C1 explicit-path staging.
- **Cleanup:** BE + FE processes terminated post-probe (`pkill -f
  e043_launch_be`, `pkill -f vite`). Postgres + Redis left running per
  directive. Probe users + subscriptions + tracker rows + score rows + usage
  log row left in place per cleanup directive (cheap to re-use; documented
  above for later cleanup if desired).
