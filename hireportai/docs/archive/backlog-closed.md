# BACKLOG Closed Archive

Compacted from `BACKLOG.md` "Closed (most recent first)" table on
2026-05-01 (B-092 process compaction bundle). This file preserves
historical closed BACKLOG rows verbatim so the live `BACKLOG.md`
closed-table stays focused on the most-recent ~10 closures for active
audit linkage. Search here if `grep` on `BACKLOG.md` doesn't find a
historical closed row (every closed B-### / E-### / row note is
preserved below).

Append-only. Future compactions add new dated headings; never edit or
remove archived content.


## Cut 2026-05-01 from `d5602c3` — B-092 process compaction bundle

### Archived closed rows (44 entries spanning 2026-04-18..2026-04-28)

Cut point: keep the most-recent ~10 closed rows in live BACKLOG.md;
archive the rest. Row B-077 (2026-04-28) was the 11th-most-recent at
slice start and became the cut boundary. The stray markdown separator
`|----|-------|...` left over from B-076's 2026-04-28 SESSION-STATE
compaction is dropped during the cut (cosmetic; not a row).

| ID | Title | Closed by | Date | Notes |
| B-077 | Phase 6 slice 6.7 — persona Learn page (FE consumer of slice 6.6 ranker) | `c6d9274` | 2026-04-28 | Ships `src/pages/Learn.tsx` with D-5 three inline persona modes (`LearnInterviewMode` / `LearnHabitMode` / `LearnTeamMode`) mirroring HomeDashboard pattern; new `RankedDeckList` (D-1 card grid, D-2 actionable empty copy, D-7 verbose cold-start CTA → `/prep/analyze`); new `useRankedDecks` hook with `enabled` gate; new `fetchRankedDecks(opts)` API helper + `RankedDecksResponse` types in `src/types/index.ts`. App.tsx `/learn` mount swap to `<Learn />`; `src/pages/StudyDashboard.tsx` + `tests/StudyDashboard.test.tsx` deleted via `git rm` per D-3; `tests/App.redirects.test.tsx` updated. D-4 HabitMode "Curriculum suggestions" expanded by default; D-6 `learn_mode_rendered` useRef once-per-mount; D-8 `study_dashboard_source_hint_shown` preserved verbatim with `copy_variant: '6A'`; new `learn_page_viewed` + `learn_deck_clicked` events. Tests BE 636 unchanged; FE **375 → 397 (+22)** — within ±5 of envelope floor +25..+40. JC #1: D-5 inline-functions lock collapsed §10's 4-test-file plan to 2 files (Learn.test.tsx + RankedDeckList.test.tsx); AC coverage preserved. JC #2: -3 below test envelope floor (no ballast added per prompt + Q1). Two-commit pattern. CR marked stale (page deletion + new mount). |
| B-076 | SESSION-STATE compaction + drift archive (B-073 cohort items 3+4) | `6c130f3` | 2026-04-28 | Compacts SESSION-STATE.md 1536 → 491 lines (68% reduction); creates docs/archive/session-state-history.md (1107 lines) with §1 Last Completed history + §2 9 RESOLVED drift rows + §3 Resolved Decisions + §4 Obsolete Slices + §5 Recently Completed entries 6+. Zero info loss verified via ID-grep (BEFORE 271 / sum AFTER 275). Closes B-073 cohort items 3+4 of 5; item 5 (CR counts script) deferred. R14 exception (b) — pure doc reorganization. Two-commit pattern. |
| B-074 | Phase 6 slice 6.6 — Lens-ranked deck/lesson ordering (BE-only ranker + `curriculum_visibility.py` extraction) | `5011518` | 2026-04-28 | Ships heuristic v1 ranker (gap-match 0.55 + FSRS-due 0.25 + quality 0.10 + display_order 0.10 weighted sum per D-1); decks-only output v1 per D-5; `GET /api/v1/learn/ranked-decks` D-9; `lookback_days=30` / `max_scans=5` defaults D-14; substring matching on slug+title D-7; quality null→0.5 D-2; cold-start safe with `cold_start: true` + display_order ASC fallback D-15; partial-failure skip+log D-16. Slice 6.5 D-5 escape-hatch fires explicitly: `_persona_visible_to` / `_visible_persona_set` / `_allowed_tiers_for_user` + transitive `_resolve_plan` extracted to `app/services/curriculum_visibility.py` per D-6; lesson_service.py / quiz_item_study_service.py / lesson_view_events.py rewired to import from there. Zero FE, zero migrations, zero analytics events. Tests BE 612 → 636 (+24) within +19..+33 envelope; FE 375 unchanged. AC-1..AC-15 green. JC #1: tracker.created_at is naive timestamp; cutoff strips tzinfo. Two-commit pattern. |
| B-075 | Author `.agent/skills/curriculum.md` — Phase 6 domain skill + E-028 retrospective close | `49fc7e7` | 2026-04-28 | Bundles greenfield `curriculum.md` skill (10 sections: domain model, lifecycle filter chain, helper extraction status, substantive-edit cascade, analytics dual-write, seed corpus conventions, three-layer quality skeleton, ranker contract D-1..D-16, what-this-skill-is-NOT, cross-refs) with retrospective E-028 status flip 🔴 → ✅ referencing `84060b3` (B-073 cohort item 1 — `backend.md` ship satisfied E-028's original intent). Skill came in at 356 lines vs ~300 envelope (judgment call logged). Closes SOP-4 "no skill clearly applies" gap-flagging on Phase 6 slices from 6.7 onward. R14 exception (b) — pure skill codification, no design surface, no tests run. Two-commit pattern. |
| B-073 | Process optimization cohort — CLAUDE.md amendments (R15(d) BACKLOG row note budget + SOP-4 skill-gap close-loop + drift-archive cut rule) | `b468025` | 2026-04-28 | Closes the cohort floor (items 1+2 of 5). Item 1 `backend.md` skill author shipped at `84060b3`; item 2 (this slice) codifies three SOP rules in CLAUDE.md and exercises the new R15(d) shape on this very close-line. Drift table at 21 rows (9 ✅ RESOLVED + 12 active) < 30 threshold — cut rule codified but did not fire this slice. Items 3-5 (SESSION-STATE compaction, drift archive ongoing maintenance, CR counts script) deferred; spawn separate rows on activation. R14 exception (b) — pure SOP codification, no design surface, no tests run (BE 612 / FE 375 carried forward verbatim). Two-commit pattern (impl + SHA backfill). N8 preserve-and-coexist: pre-existing concurrent CLAUDE.md edits (Q1-Q4 + "This file is working if" + dangling rev-history line) stashed during edit and restored after commits. R17 watermark verified at slice start: B-073 highest in-use, B-074 free → no new IDs claimed. SOP-8 clean: HEAD `84060b3` at slice start. SOP-9 honored. |
| B-072 | Phase 6 slice 6.5 — read-time service-layer invariants on FSRS-progress + lesson/quiz_item lifecycle filters | `930a6a2` | 2026-04-28 | Closes the slice 6.4 D-19 persona-narrowing read-side punt + adds defense-in-depth `Deck.archived_at IS NULL` filter on R-6 (`list_lessons_in_deck`). Step 0 audit branch (a) clean (zero new §4.3 gaps). `quiz_item_study_service` R-1 gains persona+tier SQL filters on both queue passes; R-2 gains post-load persona check (raises new `QuizItemNotVisibleError` 404 per D-7) + post-load tier check (raises `QuizItemForbiddenError(reason='premium_deck')` 403 per D-2). `lesson_service` four public functions gain keyword-only `*, user: Optional[User] = None` + persona SQL filter + tier-mismatch raise (D-2 + **D-10** inline-amended this slice). `QuizItemForbiddenError` extended additively with optional `reason` kwarg (default `'archived'` preserves slice 6.2 call sites). Helpers `_persona_visible_to` / `_visible_persona_set` / `_allowed_tiers_for_user` duplicated between the two services per D-5 (escape-hatch NOT triggered — `deck_admin_service._PERSONA_EXPANSION` is the inverse direction, not semantically equivalent). Routes thread `user=` through and add `QuizItemForbiddenError` → 403 + `QuizItemNotVisibleError` → 404 mappings. Spec §12 +**D-10** lock (lesson_service tier-mismatch = `QuizItemForbiddenError(reason='premium_deck')` → 403); §11 AC-14 cross-ref D-2 → D-2+D-10. Tests: **BE 587 → 612 passed (+25)** under prod-default env vars across two new test files (`test_quiz_item_study_service_invariants.py` +11, `test_lesson_service_invariants.py` +14); **FE 375 unchanged**. AC-1..AC-14 all green. R16 consumer-graph audit: 3 route call sites updated (lessons.py:33, decks.py:34/54), zero script callers. JCs: (1) prompt's `ProDeckRequiresUpgradeError` is a phantom — resolved by reusing `QuizItemForbiddenError` per spec §5 (also honors prompt's "do not introduce others"); (2) D-5 escape hatch NOT triggered (different semantics); (3) `list_lessons_in_deck` tier check uses extra visibility-filtered SELECT so archive/persona take priority for 404. Two-commit pattern (impl + SHA backfill). R14 default — implements an authored spec (as amended this slice with R14(b)-bundled D-10). R15(c) closure: B-072 closed in this commit. R17 watermark verified at slice start: B-073 highest in-use, B-074 free → no new IDs filed. SOP-8 clean: HEAD `76acd21` at slice start; SOP-9 honored: single CC session on this tree. **B-073 process-optimization cohort activation gate fires after this slice's SHA-backfill commit lands** (per B-073 row trigger). |
| B-063 | Phase 6 slice 6.3 — lesson-card UX (FE-first, fixture-data, BE read-only routes) | `ba00331` | 2026-04-27 | FE consumer surface for the curriculum platform. Three new BE read-only routes returning fixture data (`GET /lessons/{id}`, `GET /decks/{id}`, `GET /decks/{id}/lessons`); new `app/data/lesson_fixtures.py` (D-2 — Pydantic instances at module import, 2 decks / 3 lessons / 6 quiz_items spanning all three question_type values; retires in slice 6.4 by deletion + DB-query swap). Thin wrapper `app/services/lesson_service.py` (D-4 — `db` parameter forward-compat affordance). Schemas extended additively (`LessonWithQuizzesResponse`, `DeckLessonsResponse`); slice 6.1 schemas reused as-is. FE: new `pages/Lesson.tsx` at `/learn/lesson/:id` (D-8 / D-10 eager-loaded inside `<ProtectedRoute>`); new `components/lesson/{LessonRenderer,QuizItemPanel}.tsx` (D-7 net-new — legacy `study/QuizPanel.tsx` untouched); new `hooks/useLesson.ts`; new TS types in `src/types/index.ts` mirroring BE field-for-field; four new `services/api.ts` helpers (quiz-submit reuses slice 6.2's POST /api/v1/quiz-items/review per D-5). `remark-gfm` net-new dep (singular per D-3 — `react-markdown` already present). Tests **BE 513 → 520 (+7)**, **FE 328 → 338 (+10)**. tsc clean. AC-1..AC-13 verified. No schema change, no Alembic migration, legacy quiz_items / study / QuizPanel routes + components unchanged (AC-8, AC-12). Three-commit pattern (Step 0 SESSION-STATE bookkeeping + impl + SHA backfill). R17 watermark verified at slice start (B-062 highest, B-063 free). SOP-8 clean. |
| B-062 | Phase 6 slice 6.2 — FSRS quiz-item binding (service + routes) | `7b654fb` | 2026-04-27 | First FSRS consumer of slice 6.1's `quiz_item_progress` table. New file `app/services/quiz_item_study_service.py` (D-1) + new router `app/api/v1/routes/quiz_items.py` mounted at `/api/v1/quiz-items` (OQ-2): `GET /daily` (two-pass `due_date ASC` then `created_at ASC`, sentinel `daily_status`), `POST /review` (404/403/409 error paths), `GET /progress` (aggregate by state). Service ports `study_service.review_card`/`get_daily_review` byte-for-byte modulo D-1/D-4 (no wall) / D-7 (no XP). Schemas extended additively in `app/schemas/quiz_item.py` (5 new shapes). `quiz_item_progress_initialized` + `quiz_item_reviewed` analytics declared. BE 494 → **513 passed** (+19: 12 service unit + 7 route integration); FE 328 unchanged. AC-1..AC-11 verified. Legacy `/api/v1/study/...` unchanged (AC-8). No schema change, no FE. Drift surfaced: `_resolve_plan` initially triggered async lazy-load on `user.subscription` → fixed via SQLAlchemy `inspect().unloaded` guard. R17 watermark verified at slice start (B-061 highest, B-062 free). SOP-8 clean. Two-commit pattern (impl + SHA backfill). |
| B-061 | Phase 6 slice 6.1 — foundation schema (decks, lessons, quiz_items, quiz_item_progress) | `a989539` | 2026-04-26 | First Phase 6 slice — schema-only foundation. One Alembic migration `57951e9f4cdc` adds 4 tables (decks/lessons/quiz_items/quiz_item_progress); no routes, no services, no FE per AC-8. `quiz_item_progress` is byte-identical to `card_progress` modulo FK swap per D-1 + AC-6 (verified by `test_quiz_item_progress_mirrors_card_progress`). Self-referential FK on `quiz_items.superseded_by_id` ON DELETE SET NULL per AC-4. `lessons.source_content_id` ships String(36) NULLABLE WITHOUT FK per D-2 (slice 6.9 adds it once `source_content` exists). Legacy `cards`/`categories`/`card_progress`/`card_feedback` untouched per AC-7 (drop deferred to slice 6.15). 7 new tests in `tests/test_phase6_schema.py` (6 default-selector + 1 integration-marked round-trip); BE 489 → **494 passed** (delta +5; round-trip deselected from default count); FE 328 unchanged. Shell round-trip clean. Pydantic schemas added in `app/schemas/{deck,lesson,quiz_item,quiz_item_progress}.py` per spec §5.3 #4. Drift surfaced (no STOP, spec wins): prompt's `tests/unit/` + `tests/integration/` subdirs don't exist; spec §8 specifies flat `tests/test_phase6_schema.py` — followed spec. Prompt omitted Pydantic schemas from Files-expected list; spec §5.3 #4 requires them — followed spec. mypy/ruff not installed in repo. CODE-REALITY §5 targeted regen (head `30bf39fa04f8 → 57951e9f4cdc`). R17 watermark verified at slice start (B-060 highest, B-061 free). SOP-8 clean. Two-commit pattern (impl + SHA backfill). |
| B-060 | CODE-REALITY.md full regen at HEAD `8a0402e` — capture 28-commit delta since `ce60ea6` | `eb2c1c7` | 2026-04-26 | Full regen escalated from a targeted-regen attempt that R19-stopped on prompt-side staleness misreading — chat-Claude treated `ce60ea6` as the regen-commit baseline; on disk it is the SHA the prior regen content referenced, with the regen commit itself one parent later at `3f43927`. 28 commits unreflected, 7+ code-touching slices captured (B-051 / B-052+B-053 / E-023 spec #27 / LD-001 amend / B-055 / d19103c env-config / B-057 / B-058 / B-059). LD-1..LD-5 carried forward from B-049: full regen, filesystem-enumerated counts, ambiguous fields flagged "unknown — flag for next regen", sections replaced (not appended). §1 sha `ce60ea6`→`8a0402e`, components 65→67, specs 76→80 (phase-5: 32→36), tests BE 472→489 / FE 280→328; models / alembic / routers / endpoints / pages unchanged (verified zero delta). §3 study/daily response-shape note. §4 study_service + usage_service refactor notes. §6 B-057 auth-aware chrome carve-out. §7 component graph + 4 page rows rewritten. §8 +`DailyStatus` + `DailyQueueResponse.daily_status?`. §11 +2 drift items (B-058, env-vars-not-in-AGENTS-md). §13 phase-5 +4 specs + #50 amendment annotation + status-format inconsistency flagged. Doc-only; no code; tests unchanged. R14 exception (a). R17 watermark verified at slice start (B-059 highest, B-060 free). Two-commit pattern (regen + SHA backfill). |
| B-055 | `/prep/results` Analysis Results page — large vertical void at xl breakpoint between Skills Radar / Jump-nav and Bullets / CTAs row | `4b7f862` | 2026-04-26 | Layout/alignment regression visible at xl scrolled past the fold on real scans. Prompt sticky-hypothesis falsified at audit (`grep` returned zero `sticky`/`top-N` matches in `Results.tsx` / `PanelSection.tsx` / `MissingSkillsPanel.tsx` / `ImprovementSuggestions.tsx`). Actual root cause: CSS Grid row-height coupling at xl — row-3's `auto` height = `max(Jump-nav, Skills Radar, Improvements)` = Improvements (3-5 stacked recommendation cards). Fix: added `xl:row-end-5` to Improvements (`Results.tsx:482`) so its height distributes across rows 3-4 of col-3 instead of inflating row-3 alone. DOM, ordering test invariants, 9 section IDs, and verbatim spec #21 tooltip copy all unchanged. Regression test in new `tests/pages/Results.layout.test.tsx` (Improvements row-span pin + Skills Radar default-span pin + anti-sticky guard). FE 308 → 311 (+3). tsc clean. R14 exception (b). Two-commit pattern (impl + SHA backfill). |
| E-023 | Backfill spec — Geo-based pricing | `c16f544` | 2026-04-26 | Spec `docs/specs/phase-5/27-geo-pricing.md` authored consolidating `.agent/skills/geo-pricing.md` (implementation patterns) + `docs/audit/2026-04-E-020-geo-pricing.md` (Gap B / D / E + Finding 0 from B-054) + PHASE-5-STATUS rows 5.3 / 5.13 / line 100. Closes Finding 0 (phantom-path drift on E-020 + E-023). Spec hard-locks Gap D 60s failure-TTL (DD-2-A, anchored at §5) and Gap E literal `Free` rendering (DD-1-A, anchored at §6); Gap B LoginPage `usePricing` consumer anchored at §7 + LD-7. Per LD-7 from authoring slice prompt, no impl-tracker BACKLOG IDs filed in this slice — three follow-up impl slices (Gap B / Gap D / Gap E + a phase-5-status sweep slice) sketched at spec §11 without IDs; file at impl time per R17. E-020 stays 🟡 — gaps still aren't fixed. R14 exception (b) pure spec codification, not a feature. R17 watermark verified at slice start (B-054 highest, B-055 free; not claimed — spec authoring needs no tracker row). Two-commit pattern (impl + SHA backfill). Files touched: `docs/specs/phase-5/27-geo-pricing.md` (new, ~250 lines), `BACKLOG.md` (this row + E-020 Notes amend + Closed-table entry), `docs/audit/2026-04-E-020-geo-pricing.md` (one-line footer note). Doc-only; no code; tests unchanged. Template: spec #60. |
| B-054 | E-020 geo-pricing deferred-gaps re-verification — B/D/E truth table + Finding 0 (spec #27 phantom) | `4aaeda4` | 2026-04-26 | Audit-only artifact at `docs/audit/2026-04-E-020-geo-pricing.md`. 3 gaps audited (Gap B CONFIRMED-STILL-OPEN HIGH / Gap D REFINEMENT HIGH / Gap E CONFIRMED-STILL-OPEN HIGH). Finding 0 surfaced above truth table — spec #27 path stale on E-020 + E-023 rows since filing (HIGH drift). Per LD-1, no statuses flipped in this slice — chat-side review needed before Mode 4 spec-author slice for spec #27. Trio substituted for missing spec #27 per B-050 precedent (Option A): `.agent/skills/geo-pricing.md` + `docs/PHASE-5-STATUS.md` rows 5.3/5.13/line 100 + SESSION-STATE Known-Broken table. LD-5(i) relaxed for Gap E. Per LD-4, no additional BACKLOG row filed (no fourth USD-hardcoded surface surfaced). Doc-only; no regressions surfaced; no code; tests unchanged. R14 exception (a). R17 watermark verified at slice start (B-053 highest, B-054 free). Two-commit pattern (impl + SHA backfill). |
| B-053 | Implement spec #62 — StudyDashboard `?source=last_scan` hero hint | `df035e1` | 2026-04-26 | Implements spec #62 §3-§7 with LD-OQ1 locked variant 6.A neutral copy. `StudyDashboard.tsx` reads `searchParams.get('source')`, gates banner on `isLastScanSource && !sourceHintDismissed`, fires `study_dashboard_source_hint_shown {source, persona, copy_variant: '6A'}` once on mount via `useRef` idempotency. Banner is a thin `motion.div` between header (line 137) and "Your Goal" card; dismiss × button uses lucide `X` icon. Component-state dismissal (no sessionStorage). URL preserved (no `setSearchParams`). New test file `tests/StudyDashboard.test.tsx` (page had zero coverage pre-slice) with 5 ACs: param-present render, param-absent skip, dismiss interaction, `?source × ?category` orthogonality, telemetry once-fire. AC-4 confirmed `?source` is orthogonal to existing `?category` consumer (spec #09). `.agent/skills/analytics.md` row added. Closes B-053 + B-052 per spec §10 OQ-5 dual-tracking. FE 303 → 308 (+5). tsc clean. |
| B-052 | StudyDashboard hero hint for `?source=last_scan` query param (Q4 follow-up from spec #61) | `df035e1` | 2026-04-26 | Q4 follow-up work tracker filed by P5-S61-impl (`ecef895`); spec authored `ffd66f7` (spec #62 + B-053 filed); impl this slice. Closes alongside B-053 per spec #62 §10 OQ-5 (single-row work tracker pattern; spec-author convention split work into B-052 work-tracker + B-053 impl-tracker; both close together on impl-merge per established B-045/E-047 precedent). |
| B-051 | Implement spec #61 — HomeDashboard composition rules + free-tier study surface | `ecef895` | 2026-04-26 | Implements all 9 audit findings from E-048 (`docs/audit/2026-04-E-048-home-dashboard.md`) per spec #61 §7 coverage table. New `StudyGapsPromptWidget` (`hirelens-frontend/src/components/home/widgets/StudyGapsPromptWidget.tsx`); `HomeDashboard.tsx` refactored to single `useHomeState` call + composition suppression flags; `StateAwareWidgets.tsx` switched from internal hook to prop-driven; `CountdownWidget`, `InterviewTargetWidget`, `LastScanWidget` gained suppression props; `MissionActiveWidget` gained §6 Pro stale-scan footer; `ResumeStaleWidget` switched to PaywallModal route for free users. Spec #61 §4.2 amended one-line in same commit per LD-S6/LD-S7: `trigger='study_engine_unlock'` → `trigger='skill_gap_study'` (existing on-disk PaywallTrigger union; semantically closer copy). 2 new PostHog events catalogued. Closes B-051 + E-048 (impl half). Q4 follow-up filed as B-052. FE 280 → 303 (+23). tsc clean. |
| E-048 | HomeDashboard widget composition for free users mid-scan-history shows contradictory state (audit + impl) | `ecef895` | 2026-04-26 | Audit half: `47b21df` (2026-04-26 — 9 findings, recommendation = new spec #61). Spec authored: `1262c26` (spec #61 + B-051 filed). Impl half: this slice — closes B-051 by implementing all spec sections. Audit symptom (Mission/Countdown duplicate-render, InterviewTarget empty alongside active Mission, LastScan buried, no on-home study CTA, no upgrade-to-study CTA, mission_active suppressing resume_stale, no plan-branching in state-aware layer) all addressed. Two-commit pattern: impl + SHA backfill. |
| B-050 | Phase-5 partial-items audit — 5.13 / 5.19 / 5.20 truth table | `f7e4c0b` | 2026-04-26 | Audit-only artifact at `docs/audit/2026-04-partial-audit.md`. 3 items audited (5.13 still 🟡 / 5.19 propose ✅ / 5.20 propose ✅). Per LD-5, no statuses flipped in this slice — chat-side review needed before doc-only sweep on `PHASE-5-STATUS.md`. Original prompt's source-of-truth file `claude-code-prompts-all-phases-v2.md` did not exist; substituted `docs/PHASE-5-STATUS.md` per H3-recovered chat-side restated scope. 3 items dropped from scope (5.15/5.16/5.18 already ✅). Doc-only; no regressions surfaced; no new BACKLOG row filed for broken behavior. R14 exception (a). |
| B-049 | CODE-REALITY.md full regen at HEAD `ce60ea6` — refresh §1-§12 + add §13 specs inventory | `3f43927` | 2026-04-26 | Full regen against post-B-048 SOP state. Carry-forward §9 + §12 content (Option B). New §13 specs inventory (76 specs across 6 phases). Net-new findings: 3 untracked directory-style skills under `.agent/skills/`, 32 specs lacking `## Status`, phase-4 duplicate `error-monitoring` spec numbering, phase-5 spec #1 superseded by #38. Tests unchanged (doc-only). R14 exception (a) regenerated audit artifact. |
| B-048 | CLAUDE.md revision — merge R18→R15(c), R3 split to R19, add Chat-Claude Handoff + SOP-9 | `e2714b4` | 2026-04-26 | Doc-only SOP simplification. Full CLAUDE.md rewrite + 12 R3→R19 sweep replacements across SESSION-STATE.md/BACKLOG.md/spec #53 + 2 R18-merge footnotes in BACKLOG.md (B-039 row body + close-line). N1-SUPPLEMENT→N9 promotion deferred (6 callsites). Spec section header rename (`## 12. R15` / `## 13. R15` × 5 specs) deferred to next doc-hygiene slice. R14 exception (b) pure SOP codification. R17 watermark verified at slice start (B-047 highest, B-048 free). No code; tests unchanged. |
| E-002 | Restructure routes to `/learn/*` and `/prep/*` namespaces | `e8f9d05` | 2026-04-25 | Audit-close — `/learn/*` (5 routes) and `/prep/*` (5 routes) verified live in `hirelens-frontend/src/App.tsx:86-97`. Row was stale 🔴; routes shipped in P5-S12/S13/S14 but status was never flipped. No code change in this slice. |
| E-004 | Add `interview_target_company` field to User model | `e8f9d05` | 2026-04-25 | Audit-close — `interview_target_company: Mapped[str \| None] = mapped_column(String(100), nullable=True)` verified live in `hirelens-backend/app/models/user.py:29`. Row was stale 🔴. No code change in this slice. |
| E-005 | Persona-aware HomeDashboard at `/home` | `e8f9d05` | 2026-04-25 | Audit-close — persona-keyed widget composition verified live in `hirelens-frontend/src/pages/HomeDashboard.tsx` (all four home widgets receive `persona` prop; dedicated `CareerClimberMode` branch present). Row was stale 🔴. No code change in this slice. |
| B-017 (dup) | InterviewTargetWidget empty-state copy duplicate-row removal | `e8f9d05` | 2026-04-25 | Removed duplicate 🔴 B-017 row from Active table — the canonical ✅ B-017 row (closed 2026-04-22 by `d835fb8`) remained intact. Same ID had two rows in the Active table (one ✅, one 🔴 with identical title); the 🔴 was a leftover not flipped at close time. Bookkeeping only. |
| B-045 | Implement spec #60 — pre-flight scan-exhausted gate on `/prep/analyze` | `3c962d8` | 2026-04-25 | Locked-copy gate card replaces upload form when free user is at scan cap (`!canScan && plan==='free' && !isAdmin`). Dead `— Upgrade for more` `<span>` removed. `paywall_hit { trigger:'scan_limit', surface:'analyze_page_load', plan:'free' }` once-on-mount via `useRef` idempotency guard. Upgrade CTA → `setShowUpgradeModal(true)` (existing app-root mount). Closes E-047. **LD-4 (`scansRemaining` field) deferred to B-047** — pre-existing `UsageContext.tsx` working-tree dirt + permission-denied stash blocked safe inclusion; spec §4.1 line 147 authorizes the deferral. Tests: FE 272 → 280 (+8). |
| E-047 | Free user post-scan-limit lands on `/prep/analyze` with no working upgrade path | `3c962d8` | 2026-04-25 | Spec authored 2026-04-25 (commit `45959a8`), impl shipped this slice (closes B-045). LD-4 cleanup tracked as B-047. |
| B-039 | Codify recurring workflow rules into CLAUDE.md SOP | `f0d3129` | 2026-04-25 | Promoted three repeat-incident workflow rules from prompt re-statement to durable on-disk SOP: R17 (live BACKLOG ID watermark check, refs B-037), R18 (pre-commit BACKLOG verification — closed + filed-only buckets, complements R15) — merged into R15(c) on 2026-04-26, C5 (sharpens C1 — no `git add -A` from above `hireportai/`, refs B-034), N8 (SESSION-STATE.md preserve-and-coexist, refs D-019/D-022). Existing rule numbers preserved. R14 exception (b) doc-only SOP codification. |
| B-037 | Countdown widget "Add interview date" CTA navigates to `/onboarding/persona` instead of inline date editor | `961ee3f` | 2026-04-25 | New `InterviewDateModal` component opens inline on CTA click. Read-and-preserve workaround for non-partial PATCH (filed as B-038). Spec #53 §Supersession appended. Reuses `interview_target_date_added` PostHog event with new `surface='home_countdown'` property. Tests: CountdownWidget 6 → 10 (+4). R14 exception (b) pure UX bug fix. N5-class re-ID note: this slice initially used B-035/B-036 in commit before catching the concurrent-session conflict; soft-reset and re-IDed before push. |
| B-035 | LastScanWidget View-results dead-ends for returning free user — scan payload never persisted | `0b35440` | 2026-04-24 | Spec #59 persistence + hydration. Alembic `30bf39fa04f8` adds `analysis_payload JSONB NULL` (deferred load), new `GET /api/v1/analyze/{scan_id}` 200/410/404 ownership-checked, Results.tsx hydrates from URL scan_id with three-way empty-state. R15 file+close one slice. Tests: BE 463 → 472 (+9), FE 256 → 263 (+7). |
| B-034 | Bare `/prep` route falls through to `/home` (free-user nav bug) | `c2ce17d` | 2026-04-23 | One-line `<Route path="/prep" element={<Navigate to="/prep/analyze" replace />} />` inside ProtectedRoute. `/prep/analyze` is already the canonical Prep entry elsewhere in the codebase. Tests: FE 255 → 256 (+1 in `App.redirects.test.tsx`). R14 exception (b) pure bug fix. |
| B-029 | Duplicate Profile link in desktop app shell after B-028 | 8ed1775 | 2026-04-23 | Dropped Profile from TopNav NAV_ITEMS — lives in UserMenu dropdown on desktop, stays as MobileNav tab. SaaS-canonical (GitHub / Stripe / Linear / Notion / Vercel). TopNav tests updated (tab counts 4→3 / 5→4). |
| B-028 | No sign-out affordance in app shell | 7da0943 | 2026-04-23 | New `UserMenu` (avatar dropdown) in TopNav + Profile "Account" section for mobile users. `sign_out_clicked {source}` PostHog event. Tests: FE 218 → 227 (+9). |
| B-027 | HomeDashboard greeting flips "Welcome" → "Welcome back" mid-session on first visit | e792bb4 | 2026-04-23 | Follow-up to B-016. `useState` initializer snapshots `isFirstVisit` on mount so post-stamp `updateUser` doesn't mutate rendered copy within a single session. Tests: FE 217 → 218 (+1). |
| B-003 | "Generate My Experience" button non-functional on Profile page | 0719fa1 | 2026-04-23 | Root-cause fix already shipped in P5-S11 (`66c1814`): tier move to FAST + `max_tokens` 500→2048 + empty-response 503 guard. This slice is a re-test close: FE wiring verified intact; regression coverage added in `tests/Profile.experience.test.tsx` (happy / no-history empty-state / 503 error). No FE source change. Tests: FE 214 → 217 (+3). R14 exception (a) retrofit / test-coverage backfill. |
| B-021 | Cover letter prompt always receives "your company" — extract_job_requirements never populates company_name | e7c6d73 | 2026-04-22 | Heuristic `_extract_company_name` in nlp.py (5 high-precision regex patterns). Two consumers benefit: cover-letter prompt + tracker autopopulate. Tone-agnostic fix. Tests: BE +19. |
| B-019 | TodaysReviewWidget completed-state missing — "5 cards due" after review | ac33ed0 | 2026-04-22 | Fork A: additive `completed_today: bool` on `DailyReviewResponse`. UTC window matches daily_complete XP bonus. Small-library clamp via `min(_DAILY_GOAL, reviewed_today + len(result))`. Widget flips to empty state "Done for today — great work." Tests: BE +5, FE +3. Wall-state surfacing deferred to B-020. |
| B-018 | `interview_target_date` treated as required downstream — no-date interview_preppers coerced | 724e5cd | 2026-04-22 | Finding #7 from 2026-04-21 E2E walkthrough. Spec #53 Option β. CountdownWidget Mode 1 → link-only unlock CTA. New `MissionDateGate`. PersonaPicker `return_to` URL-param + whitelist. 4 new PostHog events. Tests: BE +2, FE +14. |
| B-016 | HomeDashboard greets every user with "Welcome back" — wrong on first visit | d835fb8 | 2026-04-22 | Finding #1 from 2026-04-21 E2E walkthrough. Alembic 508df0110037 added `users.home_first_visit_seen_at`. New idempotent `POST /api/v1/users/me/home-first-visit`. HomeDashboard greeting fork keyed off pre-stamp NULL. Tests: BE +4, FE +5. |
| B-017 | InterviewTargetWidget empty-state copy references non-existent Countdown company field | d835fb8 | 2026-04-22 | Finding #16 from 2026-04-21 E2E walkthrough (also flagged in PERSONA-FLOWS-VERIFICATION-2026-04-21.md:146). Three-case `emptyCopy()` split — truthful per gap. PersonaPicker stays canonical company capture. Tests: FE +3 branch tests + 1 regression guard. |
| B-002 | Cover letter generation format malformed (headers, paragraphs, signature block) | 08f0873 | 2026-04-21 | spec docs/specs/phase-5/52-cover-letter-format-enforcement.md — closed by 08f0873 on 2026-04-21 — structured cover letter response end-to-end (BE schema + service + FE consumer migration + telemetry + AC-4b `integration_llm` marker). SHA rewritten during 2026-04-21 recovery from concurrent-session amend contamination (see D-015). |
| B-005 | Stripe webhook handler is non-idempotent — duplicate delivery risk | f615eb6 | 2026-04-19 | spec docs/specs/phase-5/43-stripe-webhook-idempotency.md — closed by f615eb6 on 2026-04-19 — spec backfilled retroactively; idempotency was shipped prior to backlog creation (original impl 43d40d9 on 2026-04-10). |
| E-003 | Mandatory PersonaPicker on first login + persona model | 2c01cc7 | 2026-04-18 | P5-S17 full FE migration. closed by 2c01cc7 on 2026-04-18 |
| B-007 | StudyDashboard PERSONA_CONFIG undefined for snake_case persona values | 2c01cc7 | 2026-04-18 | Closed alongside E-003. closed by 2c01cc7 on 2026-04-18 |


## Cut 2026-05-03 from `4e2e26f` — Mode-1 light hygiene

### Archived closed rows (14 entries spanning 2026-04-29..2026-05-01)

Cut point: keep the 27 most-recent (2026-05-02..05-03) closed rows in
live BACKLOG.md; archive everything dated 2026-05-01 or earlier. B-084
(2026-05-01) was the oldest 2026-05-* row at slice start; the
2026-04-29..30 cluster (E-017, E-042, B-086, B-086b, E-043) cut
alongside as a date-band sweep.

| ID | Title | Closed by | Date | Notes |
| B-087 | Phase 6 slice 6.13 — Pro digest opt-out + `email_log` dedup table (precondition for slice 6.14 cron) | `d020f4d` | 2026-05-01 | Migration `f1a2b3c4d5e6` adds `email_preferences.daily_digest_opt_out` (server-default false) + new `email_log` table (UNIQUE `(user_id, email_type, sent_date)` + composite index + ON DELETE CASCADE FK). New `email_log_service` (`record_send` + `was_sent_today`) ready for slice 6.14 cron. Route PUT extended with Pro-tier guard (admin bypass via `user.role == "admin"` — disk-reality adjustment from spec §6.4's `user.is_admin`) returning 403 on free-user opt-out write. FE `<EmailPreferences />` Pro-gated digest toggle. Tests: BE CI-canonical 745 → 766 (+21, my +18 of which) + 1 marker-gated integration; FE 446 → 451 (+5). 16/16 ACs green. **D-028 expanded** with slice-6.13 cascade documentation (full-suite non-CI shows 25 cascade fails from chain-extension exposing pre-existing alembic-roundtrip brittleness; 3 fix attempts reverted per R10; CI subset clean). R14 default; all 12 OQ author hints accepted as decisions per chat-Claude triage (§12 amendment slice not run separately — JC). Two-commit pattern. |
| B-095 | Phase 6 slice 6.13.5 §12 amendment — D-1..D-14 locked from §14 OQ-A..OQ-N | `4bf5220` | 2026-05-01 | Closed `4bf5220` 2026-05-01. §12 amendment for slice 6.13.5; single-slice file+close (B-091/B-092/B-093 precedent). Locks D-1..D-14 from §14 OQ-A..OQ-N 1:1 (Dhamo single-admin disposition; zero ambiguous hints). Notable: D-2 no `quiz_items.quality_score` column add (row-only); D-3 critique-consumer write-time hook; D-7 lesson-level thumbs UI only (per-quiz_item DROPPED v1); D-14 alembic `down_revision` verified at impl Step 0. §14 rewritten to RESOLVED form; ~26 body cross-refs flipped `§14 OQ-X` → `§12 D-N`. Spec 1714 → 1681 lines. R14 exception (b). R15(c): B-095 filed + closed same slice; B-094 stays 🔴 (impl-ready). R17: B-095 claimed; B-096 next-free. Two-commit pattern. |
| B-093 | Phase 6 slice 6.13.5 spec authored — `card_quality_signals` + critique-consumer + per-quiz_item writeback + user-thumbs ingestion | `b93beb8` | 2026-05-01 | Closed `b93beb8` 2026-05-01. Spec-author single-slice file+close (B-091/B-092 precedent). 1714-line spec at `docs/specs/phase-6/12-quality-signals.md` with 15 numbered sections + Status. Closes 4 §13 deferrals from slice 6.11: `card_quality_signals` table per LD J2; layer-2 critique-score consumption (R2 → table); per-quiz_item user-aggregate writeback; user-thumbs ingestion. §12 empty placeholder; §14 carries 14 OQs (A..N) with author hints for §12 amendment slice. R14 exception (b) — pure spec authoring. R15(c): B-093 filed + closed in same slice; B-094 forward-filed at 🔴 for impl. R17 watermark: B-093 + B-094 claimed; B-095 next-free. Two-commit pattern. |
| B-092 | Process compaction bundle — SS+BACKLOG archives + process-health-check.sh pilot + R15(d) sharpening + SOP-gate-narration rule | `d5602c3` | 2026-05-01 | Closed `d5602c3` 2026-05-01. Single-slice file+close (B-091 precedent). Three bundled concerns: (a) SESSION-STATE entries 5+ → `docs/archive/session-state-history.md` §6 (cut at LIGHT MODE inflection `b85bfd1`); BACKLOG closed-table rows pre-2026-04-29 → NEW `docs/archive/backlog-closed.md` (44 rows). (b) `scripts/process-health-check.sh` pilot — 5 caps; exit 1 on fail; portable bash 3.2+. (c) CLAUDE.md R15(d) hard-ceiling 250 words (= health-check fail threshold) + LIGHT MODE SOP-gate-narration rule. Pre/post line counts: SS 794 → ~250-350; BACKLOG 260 → ~120-180. R14 exception (b). |
| B-089 | Phase 5 spec #63 §16.6 R-5 — `/analyze` baseline-row backfill (slice 2 of 2; closes IFD-5 from validation-probe) | `2cf7c89` | 2026-05-01 | `/analyze` now calls `tracker_application_score_service.write_score_row` for the auto-created tracker per §16.6 R-5 / §6.3 (REQUIRED). Effect: first `/rescan` after `/analyze` lands `history.length=2`; HomeScoreDeltaWidget renders without two rescans; `rescan_completed` `ats_score_before` + `*_delta` fields non-null on first rescan. Tests: BE 761 → 764 (+3, within +2..+4 forecast); FE 445 → 446 (+1, at floor — single regression test names §16.6 R-5 contract; existing `'renders the delta envelope when history.length >= 2'` covers the rendering path so no duplicate). New: `tests/test_analyze_baseline_score.py` (3 — happy-path baseline write / first-rescan two-row history+non-null delta / per-scan_id idempotency). **D-029 cascade-close ✅** at this slice — Slice 1 (B-088 at `2c92f11`) + Slice 2 (this slice) close all 5 IFDs from validation-probe `35350ea`. R14 default; R15(c) closure: B-089 🔴 → ✅. R17 watermark unchanged: B-091 highest in-use, B-092 next-free. R16 leaf-surface verified: `analyze.py::analyze_resume` only + 1 new BE test file + 1 FE test extension; no schema, no migration, no shared types, no FE component code. Two-commit pattern. |
| B-091 | `scripts/sha-backfill.sh` pilot — slice-SHA placeholder substitution per audit §5 | `c77861f` | 2026-05-01 | Closed `c77861f` 2026-05-01. ~72-line bash pilot per audit `docs/audits/process-bloat-2026-05-01.md` §5 (Thread B optimization sprint). Usage: `./scripts/sha-backfill.sh <slice-sha> [--dry-run]`. Scope = BACKLOG.md + SESSION-STATE.md + docs/specs/. Portable macOS bash 3.2 + Linux bash 4+ (replaced `mapfile` with `while IFS= read` after smoke-test catch). Smoke test 6/6 pass; eat-own-dogfood — this slice's commit-2 backfill ran the pilot against itself for end-to-end validation. Single-slice lifecycle (B-091 filed + closed in same slice). R14 exception (b). |
| B-090 | LIGHT MODE reporting discipline impl — CLAUDE.md "Reporting discipline (LIGHT MODE)" subsection appended | `e0e9b29` | 2026-05-01 | Closed `e0e9b29` 2026-05-01. AC-1..AC-6 met per spec docs/specs/process/01-light-mode-reporting.md §5. (a) §4's verbatim text appended below the existing Final Report bullet list as new `### Reporting discipline (LIGHT MODE)` subsection (AC-1); (b) revision history line added (AC-2); (c) this slice's Recently Completed entry follows the compact shape — no SOP-1..9 enumeration, no N8 allowlist file list (unchanged), one prior HEAD only — second canonical example after `b85bfd1` (AC-3 + AC-4). **§4 addendum (Step 2(b) per impl-prompt):** appended one sentence to the "When in doubt, drop" paragraph codifying that sidecar rows mirroring compacted content are themselves bloat — captures the lesson from the spec-author slice's experimental "(prior, kept for archaeology)" Session Header row that was removed before commit. R14 exception (b) — pure rule codification, no test surface (BE 761 / FE 445 carry-forward). R15(c) closure: B-090 🔴 → ✅. R17 watermark unchanged: B-090 highest in-use, B-091 next-free. Two-commit pattern. |
| B-088 | Phase 5 spec #63 §16.1+§16.2+§16.3 — analytics reconciliation impl (slice 1 of 2; closes IFD-1+IFD-4+IFD-6 from validation-probe) | `2c92f11` | 2026-05-01 | Three reconciliations shipped per §16.1 R-1 + §16.2 R-2 + §16.3 R-3: `rescan_failed` event-fires wired in `rescan_application` for 404/422/402 paths (502 carry-forward) per §16.1 R-1; `rescan_completed` payload key `jd_hash` → `jd_hash_prefix` (8-char) per §16.2 R-2; analytics.md 3 catalog rows reconciled (`rescan_failed` enum widened, `rescan_completed` key renamed, `ats_scanned` source + payload reconciled to actual helper emission per §16.3 R-3). Tests: BE 757 → 761 (+4) — `captured_events` monkeypatch fixture mirroring `tests/test_wall.py:174-181` canonical pattern. **D-029 stays 🟡 PARTIAL** — slice 1 of 2; D-029 closes when Slice 2 / B-089 ships. R14 default; R16 leaf-surface verified (analyze.py route only + analytics.md doc-only + no shared types / FE / migration). Two-commit pattern. |
| B-084 | Phase 6 slice 6.11 — content-quality retention dashboard + first non-NULL `lessons.quality_score` writeback | `95104d2` | 2026-05-01 | New admin-only `GET /api/v1/admin/content-quality` envelope (per-deck rollup + worst-lessons + worst-quiz_items) over a configurable 7-90 day window of `quiz_review_events` + `lesson_view_events`; first non-NULL emitter of `lessons.quality_score` via Bayesian-smoothed pass_rate writeback (idempotent IS DISTINCT FROM gate, MIN_REVIEW_THRESHOLD=10 per D-4). Spec §6.4 archived-helper claim corrected in same commit per Step 1 audit JC #2. Tests: BE 729 → 752 (+23); FE 428 → 445 (+17). 4 pre-existing alembic-roundtrip integration failures unchanged. AC-1..AC-18 all green. Skill catalogs updated: `analytics.md` (new `admin_content_quality_viewed` event) + `curriculum.md` §7 (layer-3 flipped from forward-looking-skeleton to active). Two-commit pattern. |
| B-086b | Phase 5 spec #63 — E-043 orchestrator + admin route + UI (consumes B-086a foundation) | `4aab0bf` | 2026-04-30 | Orchestrator + UI half of E-043. POST `/api/v1/analyze/rescan` + GET `/api/v1/tracker/{id}/scores` + `tracker_application_score_service` + `<ScoreDeltaWidget>` + `<HomeScoreDeltaWidget>` + `useScoreHistory` + 4 new PostHog events. Cascade-closes B-086 umbrella + E-043. Tests: BE 695 → 713 (+18); FE 417 → 428 (+11). Two-commit pattern. |
| B-086 | Phase 5 spec #63 — ATS re-scan loop per tracker application (umbrella) | `4aab0bf` | 2026-04-30 | Cascade-closed at B-086b ship — B-086a `210dcb2` shipped foundation half (migration + extraction + scaffolding); B-086b `4aab0bf` shipped orchestrator + admin route + UI. Closes drift D-020 (closed at B-086a). |
| E-043 | ATS re-scan loop per tracker application (close PRD scan→study→re-scan→improve loop) | `4aab0bf` | 2026-04-30 | Cascade-closed at B-086b ship. PRD "scan → study → re-scan → improve" loop now first-class on the tracker focused-row + interview_prepper home dashboard. Spec authored 2026-04-30 at `da14c01`; §12 amendment `71a77e3`; §5.3 + §6.1 corrections `1b86bf0`; foundation `210dcb2`; orchestrator + UI `4aab0bf`. |
| E-042 | Tracker-level interview date + company (FE migration; deprecates user-level fields) | `b13f410` | 2026-04-29 | Closed by `b13f410` on 2026-04-29 — Phase 5 E-042 FE migration completes spec #57. BE shipped 2026-04-23 (column add + partial index + backfill migration `eb59d4fc1f7e` + Pydantic validators + `home_state_service.get_next_interview` + tracker route invalidation + `PATCH /users/me/persona` dual-write). FE migration this slice: `src/types/homeState.ts` (`NextInterview` + `HomeStateContext.next_interview`); `src/types/index.ts` (`TrackerApplication.interview_date`); `src/context/AuthContext.tsx` (`@deprecated` JSDoc on `interview_target_*`); `CountdownWidget` reads `nextInterview` prop, no-date CTA → `/prep/tracker?new=1`, date-present link → `/prep/tracker?focus={tracker_id}`, "Today" copy when days===0; `InterviewTargetWidget` reads `nextInterview` prop; `MissionDateGate` CTA target flipped to `/prep/tracker?new=1`; `MissionMode` reads `homeState.next_interview` for the gate predicate; `HomeDashboard` passes `nextInterview` down + flips spec #61 §3.1 carve-out source to `next_interview.date`; `FirstAction` re-sources `computeCta` from `next_interview` (now fetches home state); `Tracker.tsx` gains `interview_date` field on create form + `?new=1` / `?focus={tracker_id}` query-param handlers + per-row inline date editor; `InterviewDateModal.tsx` deleted (centralised capture in tracker per spec §6.1); analytics catalog (`.agent/skills/analytics.md`) documents `countdown_widget_rendered` / `countdown_widget_add_date_cta_clicked` / `tracker_interview_date_set` / `tracker_interview_date_cleared` (new) + retires `interview_target_date_added` CountdownWidget call site (PersonaPicker call site stays until follow-up UI cleanup slice per spec §7.2). **Cascade-closes E-017** — Profile-side editor for `users.interview_target_date` is subsumed by the tracker-level refactor. Tests: BE 692 unchanged (no BE files touched); FE 414 → 417 (+3 net; below predicted +10..+20 envelope — JC #1 surfaced). PersonaPicker NOT touched in this slice per spec §6.1 (deferred to follow-up UI cleanup). |
| E-017 | Verify interview date picker + add Profile-side editor (subsumed by E-042) | `b13f410` | 2026-04-29 | Closed `b13f410` on 2026-04-29 (subsumed — E-042 FE migration shipped, Profile-side editor for `users.interview_target_date` is obsolete because interview targets now live per-application on `tracker_applications_v2.interview_date`). The user-level `interview_target_date` / `interview_target_company` columns are read-deprecated; new code reads `homeState.context.next_interview` and writes via the tracker row editor. Phase-6 cleanup slice will drop the deprecated columns once the dual-write window closes. |
